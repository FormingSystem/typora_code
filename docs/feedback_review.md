# 已提出问题的复查记录

## 2026-09-19 R034 面包屑与正文分界线

共享面包屑原先border:0遗漏固定VS Code浅色底边；现在整行下沿使用1px主题分界线，明暗适配集中维护。22px导航和内部行盒保持，绘制层不拦截点击，隐藏/销毁不留横线。

build/check、面包屑与两项相关UI回归、原始Typora80项和明暗截图通过。2026.09.19.6已安装且check OK，27资产一致、5项保护摘要不变，用户窗口待手动重启；未推送。见[设计与验证入口](editor_header.md#r034-面包屑与正文分界线)，夹具错误与复跑、历史未完成项均保留。

## 2026-09-19 R050 提交图列分隔线

拖动方向反转源于单列增宽挤压弹性说明列。三条分隔线改为相邻可见列共同调整，提交编号从左侧边界拉宽/拉窄；隐藏列、键盘、取消、刷新/切库及保存失败共用同一事务。复验发现并修正了初次挂载及隐藏后恢复时的无障碍宽度同步问题，失败证据保留，开发skill同步更新。

最终单元、真实Chromium鼠标/键盘功能、20/100/1000次重绘压力、原始Typora78项通过；明暗原生截图已视检。build/check和4项相关UI回归通过；全量检查在末次无障碍同步调整前执行，最终候选已重跑列宽目标、压力、原生及构建/部署检查，未把两者混为同次全量验收。2026.09.19.5已安装且check OK，27资产一致、5项保护摘要不变；用户窗口未重启、未推送。见[设计](../enhancements/git_graph_features.md#r050-提交图列分隔线)与[证据](../enhancements/tests/evidence/git_columns_20260919.json)，历史未完成项保持。

## 2026-09-19 R049 共享滚动条样式

SCM直角滚动条来自分散的宿主/业务样式。原生纵横条统一8px、滑块4px及明暗三态；Monaco、xterm与阅读缩略图集中适配圆角，隐藏条及实际滚动所有者保持。需求先设计再实现，开发技能和公共协作规则同步更新。

10个套件/12份选用报告、三档压力、原始Typora62项与截图通过；build/check及安装检查OK。2026.09.19.4的27资产一致、5项保护摘要不变；用户窗口未重启、未推送。设计、失败记录和证据入口见[R049交付](workspace_interaction.md#r049-本轮验证与交付)。历史未完成项保持。

## 2026-09-19 R039—R045 稳定性与证据闭环

最近目录、回收适配、后缀识别与编辑模式、章节顶部遮挡、功能菜单图标和Git底栏状态已修复；普通已知目标Git动作取消通用预览层，参数表单一次提交。需求、影响设计、测试分类和问题处理正式入库，开发skill同步更新。

本轮110个目录套件通过、4项环境测试未执行；20/100/1000档、原始Typora两主题21项及安装核对见[权威验收记录](stability_validation_20260919.md)。首次失败和修复后运行各自保留，不覆盖为成功。23项资产安装一致，五项宿主/配置保护摘要不变，用户窗口没有重启；保存后正常重启加载。另一电脑实际删除环境和既有平台缺口仍需对应证据，未宣称全面VS Code等价。


本表对应用户截至 2026-09-12 的截图和反馈，覆盖顶栏、活动栏、Git、文件操作、终端、文件识别、链接、大纲、搜索性能与多窗口拖动。前面的记录保留对应构建历史，最新结果见文末；此前通过的计数不能替代新构建验收。

<a id="本次发现的遗漏"></a>

## `94b47b2` 复查发现的遗漏

- 活动栏实际位于宿主 `header` 内，旧样式限定为 `body` 的直接子节点，因此没有应用固定定位；第一槽被顶栏遮挡。此前测试只检查 Explorer 没有越过顶栏，没有验证整个活动栏的起点和连续槽位。
- Git Graph 的“未提交改动”详情没有复用 SCM 的默认打开分流，新文件仍进入空基线差异视图。
- 搜索结果和快速打开列表仍使用通用文件图标，没有调用 Explorer 使用的同一 Seti 图标函数。
- 快捷键切换标签按全局路径查找，同一文件在多个编辑组打开时可能取错标签位置；系统空页也未按真实 URI 排除。
- 快速打开未忽略输入法组字期间的 Enter，可能在中文选词时提前打开文件。
- 菜单与快捷键打开文件夹使用不同根目录来源，顶部搜索标签、Explorer 与文件操作可能不同步。删除局部 `chosen_root` 覆盖，统一使用已核对的宿主挂载目录接口及其变化通知。
- 更换根目录时，快速打开仍可能展示旧目录的异步扫描结果；现在关闭旧选择器并使旧扫描失效，盘符根目录也能显示有效标签。
- 保留的旧仓库差异页可能在切库后调用新仓库上下文；现在刷新、上下文件、打开及已生成菜单中的暂存／丢弃均在执行时校验原仓库身份。

## 反馈覆盖表

| 用户反馈 | 应有行为及本轮检查入口 |
| --- | --- |
| 保留七类菜单，各自打开功能 | 顶栏七个入口分别组织对应菜单；`test_workspace_titlebar_entries.cjs`、`test_workspace_titlebar.cjs` |
| 段落／格式菜单过长，遮住主菜单 | 从 35px 顶栏下沿展开，限制可用高度，普通滚轮和 Shift+滚轮只滚动菜单内容；titlebar 目标及独立原生实例 |
| 搜索、左右跳转放在上方 | 顶栏中央保留 Markdown 阅读历史后退、前进及文件搜索；搜索沿用已有选择器样式；titlebar、shortcuts、reading lifecycle 目标 |
| 文件夹与搜索显示错位 | 菜单、快捷键和 Explorer 共用打开文件夹动作；成功后各面板读取同一挂载根，取消／失败保留原根和草稿 |
| 资源管理器图标不见、位置不对 | 实际宿主层级下活动栏从顶栏下沿开始，每槽 48px；默认 Explorer、搜索、大纲、SCM 连续排列，已有拖动顺序优先；activity 目标及独立原生实例 |
| 侧栏上方多出空白、模块白成一片 | 侧栏与编辑区使用共同顶边；活动栏、侧栏、标签、底栏应用共同明暗边界色；activity、sidebar sash、first frame、titlebar 目标 |
| Typora 左上图标太小 | 原始图标在 24px 方框中完整显示，清除宿主透明边框压缩；titlebar 目标及独立原生实例 |
| 右上角窗口按钮比例 | 最小化、还原／最大化、关闭按钮高 35px，各宽 46px；titlebar 目标 |
| 设置入口丢失 | 底部原有齿轮打开 Typora 原生偏好设置；preferences、activity 目标 |
| 多个底部 Git 按钮一起选中 | 分支、同步、Graph 各自 hover/focus，容器不产生整组高亮；footer、SCM geometry 目标 |
| SCM 顶部重复分支／刷新按钮 | SCM 总标题保留更多菜单，Graph 独立工具栏处理图操作；SCM sidebar/geometry 目标 |
| 计数徽章颜色不对 | 当前浅色主题采用蓝底白字；SCM geometry 目标 |
| stash 断开的辅助节点、线条颜色 | stash 每项仅一个节点，内部 index/untracked 辅助提交不独立显示；支线到共同父节点保持同色，引用徽章跟随节点色；graph 数据、ref colors、interaction 目标 |
| Git Graph 详情位置、背景不对 | 详情插在所选节点之后，图线贯穿详情高度，内容与节点共同滚动；Graph interaction、history layout 目标 |
| 新文件没有比较对象仍做 diff | SCM 和 Graph 工作区入口的新增／未跟踪文件默认直接打开；Markdown 渲染，其余文本进入源码页；已有修改保留 diff，历史版本比较仍为明确的比较动作 |
| Git 文件行和差异页缺操作 | SCM 行提供打开／丢弃／暂存，差异页提供打开文件；SCM、diff status、graph interaction 目标 |
| 文件与文件夹图标不一致 | 所有真实文件入口共用 `workspace_file_icon`：Explorer、搜索、快速打开、SCM、历史、Graph 详情、文件及 diff 标签；Seti 未定义文件夹字形，树保留展开箭头；file icons、SCM icons、files search、shortcuts 目标 |
| 文件名太大、活动笔记不明显 | 标签采用 13px 字号、35px 高度及独立活动背景／顶线；tab controls、core smoke、titlebar 目标 |
| 没文件却有 New tab | 隐藏真实系统空页 `typ://core.empty/`，保留用户创建的 Untitled 草稿；core smoke、tab controls、shortcuts 目标 |
| VS Code 快捷键配置 | 核对已有命令映射、当前编辑组切换及中文组字 Enter 不提前打开文件；shortcuts、titlebar entries、files search 目标。本轮未完成真实宿主 accelerator／物理键盘输入验收 |
| YAML/yml 和其他代码文件打不开 | 文件名及语言识别共用注册表，普通文本进入源码页；file language、file URI、files search、file editing 目标 |
| 相对链接跳错目录、失败清空文档 | 相对文件以来源文档父目录解析；切换前检查目标，缺失时保留正文、草稿、标签和磁盘字节；reading lifecycle、files search、file URI 目标及独立原生实例 |
| 链接悬停改为 1 秒 | 等待 1 秒再显示，提前离开取消；link hover 目标 |
| 提示路径写死本机地址 | 生成位置使用项目根起点的 `/path`，原始链接不改写；项目外目标明确标记；link hover 目标 |
| 鼠标移入提示应保持，支持复制 | 浮层与链接间保留移动宽限，提示文字可选择、Ctrl+C，复制按钮复制原始链接；link hover 目标 |
| 链接提示的中文锚点显示成百分号编码 | 原链接显示及目标锚点只解码一次，保留 URI 分隔符和损坏转义；显示文字与实际跳转／复制值分离；link hover 目标 |
| Markdown 与代码分别识别大纲、可定位 | Markdown 原生标题目录；代码使用语言符号范围，点击定位当前模型；outline、source outline、真实 clangd UI 目标 |
| C/C++ 必须使用编译器分析、有类别颜色和配置 | C/C++ 唯一解析路径为 clangd，读取工程编译数据库与当前内存正文；提供解析环境设置和符号分类颜色；clangd 服务、clangd UI、source outline settings 目标 |
| 同章节、相邻标题滚动时大纲反复闪烁 | 自动高亮统一判定并传入明确标题，12px 双向滞回防止边界微滚动反复改选；点击目录保留定位，大纲只滚动自身；outline 目标及独立原生实例 |
| 新标题已在屏幕中间，大纲仍停留前一节 | 优先选择完整进入可读视口的标题，边缘保留 12px 进入／退出缓冲；没有可读标题时才回退正文所属章节，文末短节无需滚到顶边；outline 目标及独立原生实例 |
| 恢复正文边距百分比 | 恢复底栏原有单侧 0%–24% 控件及原持久化值，正文宽度为 100%−2×边距；不改变源码、差异和搜索预览；document margin 目标 |
| 底栏字数被右侧缩略图遮挡 | 原生 content 的边界可能与底栏重叠，大纲和缩略图统一扣除可见底栏覆盖区；minimap、outline 目标及原生点击命中检查 |
| 文件多时搜索卡顿 | 文件名分片评分与渐进扫描，普通文本／正则使用 Worker，最多4个异步预读；分批DOM、慢Git装饰不阻塞内容首屏；search、Worker、performance、files search目标 |
| 左键选中拖住动作生硬，侧栏工具也要有效 | 文件标签使用原生HTML拖放与原节点预览；活动栏保留6px启动阈值的工具排序，均在释放时提交；drag、activity目标 |
| 标签像浏览器一样拖出建窗、拖入合并 | 同窗排序、指定组和序号合并，按实际窗口边界判断新窗；安全文内锚点，不唤起外部协议；草稿及虚拟标签限制见[拖动与多窗口](drag_and_windows.md) |
| SCM操作图标与名称挤在一起 | 名称省略，操作与状态独立列，悬停／选中／键盘焦点可见；SCM geometry目标 |
| 提交行缺少查看全部改动 | 提交行提供完整改动列表入口，差异页可前后切换文件；Graph interaction目标 |
| 历史文件行与diff打开了当前文件 | 打开所选提交的只读正文；Markdown渲染，相对链接和图片读取同一提交，删除文件读父版本；revision reader目标 |
| Diff顶部太大，宽窄两种视图应自动切换 | 标签同行图标操作、紧凑版本行；编辑组≤900 CSS px行内、>900左右，手动选择独立；diff status目标 |
| 测试弹出EPIPE主进程错误 | 隐藏Electron由统一Node runner持有输出管道并等待退出，断管受控退出；不直接从PowerShell启动GUI测试进程 |
| 末级大纲仍显示折叠箭头 | 没有子标题时不显示折叠箭头，增删子标题及重新打开后同步更新；outline 目标 |

## 验证边界

隐藏 Electron 目标使用真实 DOM、Monaco 和临时工作区；独立原生检查使用未修改的 Typora ASAR 和私有桌面，不能以 fixture 中的简化宿主结构替代原生几何。用户已有文件、草稿和 Git 索引不用于破坏性测试。

C/C++ 大纲显示 clangd 实际返回的符号。编译参数诊断、未返回的宏与局部变量、当前未提供的跨文件补全／重构等范围，见[代码大纲说明](source_outline.md)。本表不会把大纲接入表述成完整 VS Code C/C++ 扩展。

<a id="本轮验证与安装结果"></a>

## `94b47b2` 验证与安装结果

- 最终 `npm run build`、`npm run check` 通过，包含核心来源摘要、发布摘要、Git 操作、路径与文件安全检查。
- 完整隐藏界面套件通过 **40/40**。随后补充的盘符根标签、快速打开旧扫描及跨仓库差异页保护，分别重跑 titlebar、files search、Graph interaction、SCM file icons，**4/4** 通过。
- 真实 clangd 服务 **16 项**、clangd 与 Monaco 界面 **11 项**通过；使用当前文件内存正文验证函数、声明、变量、类型和定位。
- 最终构建的独立 Typora 原生实例 **80 项**通过，观察 60 秒，未切换用户桌面。活动栏四槽顶边为 **35／83／131／179px**；实际 Explorer 点击、搜索、设置、长菜单、大纲与草稿保护均已核对。证据保存在本机忽略目录 `.cache/native_single_row_compare/activity_feedback_release/`，包括 `checks.json`、`verification.json` 及 8 张阶段截图。
- 原生实例的 **23 个发布文件**与最终构建摘要相同；原始 ASAR、应用图标及 7 个测试文件字节未变。早先两次夹具失败（选择器隐藏断言、截图助手缺程序集）保留失败记录，不计入通过结果。
- 2026-09-10 已运行安装脚本及 `check_configuration_windows.ps1`，结果为 `OK`，安装事务已自动备份。实际用户配置、原始 ASAR、已核对的 Zephyr 文档／YAML 及另一个笔记仓库的 6 个暂存文件和索引均保持原样。当前打开的 Typora 未强制刷新；保存后正常重启载入本次构建。

快捷键的现有命令接线、输入法及编辑组场景已检查，**宿主原生 accelerator 与物理键盘输入尚未完成验收**；不得把上述通过数量解释成与 VS Code 所有快捷键完全等价。

<a id="相邻 Markdown 标题的后续修复"></a>

## `36aef2d` 相邻 Markdown 标题的后续修复

用户继续报告在两个相邻标题间滚动时闪聚焦。原修复只关闭 `blink`，没有消除两个标题判定的竞争：工作台选择当前阅读章节，Typora 原生默认高亮却可能选择下一个可见标题；原生延迟回调还会再次改选。

现在自动回调与工作台共用一次判定，调用原生接口时传入已选定的标题。相邻边界使用 12 CSS px 双向滞回，小幅往返不改选；跨多章节跳转立即跟随。显式点击标题和手动高亮保留原生语义，非标题／旧文档延迟目标不锁住旧章节。显示当前大纲行只调整大纲自身滚动位置，卸载恢复原生接口。

本次构建与 `check` 通过，outline、启动、阅读生命周期及源码大纲共 4 个目标通过。新增 outline 回归用旧版源码红测，确实复现同章节延迟回调反复重写选中样式，再以修复源码通过同一行为检查。

独立原生对照各执行 58 次边界采样及 10 次真实宿主 `post-wheel` 高亮。旧版 6 组微滚动均出现选中往返，其中 5 个采样在正文位置未变时两次改选；修复版重复改选为 0，6 组微滚动抖动为 0，正文位置漂移为 0。证据保存在 `.cache/native_single_row_compare/outline_boundary_baseline_observed/` 和 `outline_boundary_fixed/`，不沿用前次 80 项计数作为本次证据。

<a id="可见标题定位、边距与链接显示的后续修复"></a>

## `5622258` 可见标题定位、边距与链接显示的后续修复

上一轮 `36aef2d` 消除了高亮竞争，但按“越过视口顶边”判断章节，导致新标题已经位于屏幕中间时仍高亮上一节。现在优先选择完整进入可读视口的标题；当前标题顶端仍在上沿 12px 缓冲内时保持选择，向上滚动时更早的标题完整进入后可重新接管。长段落或表格中没有可读标题时，回退到正文所属章节。原生自动／延迟回调仍共用同一判定；点击后的旧目标已滚离阅读区域时，延迟回调不能再把它选回并锁住新的位置。有效显式点击与手动高亮保留原生语义，不移动正文或焦点。

Word 官方说明导航窗格随文档滚动高亮当前标题，但未公布具体像素阈值。核对本机原始 Typora 1.14.9 实现，原生规则会考虑视口内标题，且含文末标题优先分支。这里明确采用上述可读视口与边缘缓冲规则，不声称复制 Word、WPS 或 Typora 的全部内部算法。参考：[Word 导航窗格说明](https://support.microsoft.com/en-us/word/use-the-navigation-pane-in-word)。

正文边距恢复原有底栏控件和单侧百分比含义，不恢复无关外观设置。链接提示将编码的中文路径与锚点转换为可读文字；文件路径已解析的部分不再次解码，复制按钮仍保留原始链接，悬停展示不会修改 Markdown 或触发打开文件。

本轮首次原生验证发现：已移入底栏的原生新建按钮也带有 `.footer-item-right`，普通后代选择器误把该嵌套节点作为 `insertBefore` 参照，导致边距及后续大纲初始化中断。两处插入入口已限定为底栏直接子节点，并在 margin、footer 夹具中加入真实嵌套类名，先复现失败后通过修复。失败实例 `outline_visible_fixed/` 保留，不计入通过结果，也未安装到用户实例。

原生截图还发现缩略图遮住字数。冻结旧版本使用相同窗口几何也能复现：content 的底边比可见底栏顶边更低。共享 `reading_viewport_bounds` 现在扣除这个覆盖区，缩略图不越过底栏，大纲也不把被底栏遮住的标题当成完整可读。验收同时检查矩形、截图和 `elementFromPoint` 实际命中，不能只凭控件矩形未重叠判定可见。

最终 `build`、`check` 和相关 **9/9** 隐藏界面目标通过：大纲、链接提示、缩略图、边距、底栏、启动、首次绘制、阅读生命周期及源码大纲。新增目标覆盖同文档 500ms 旧点击回调、底栏遮住标题、原始编码复制、边距保存失败与草稿／选区保护。

独立原生最终对照 **30/30** 标题取样通过，冻结旧版本 25 个定位错误降为 0，正文漂移与同次反复改选均为 0；保留 5 次真实 `post-wheel` 及实际目录点击。边距 0%→12%→24%→0% 的正文宽度与持久化符合原规则，顶部段落偏移最大 0.10px，侧栏宽度保持；字数三点和滑块中心命中、截图均通过。证据为 `.cache/native_single_row_compare/outline_visible_comparison.json` 与 `outline_visible_final/`。

最终原生实例的 23 个发布文件与构建摘要一致，原 ASAR、图标及 7 个临时工程文件字节保持，私有窗口受控清理且未切换用户桌面。2026-09-10 安装事务及 `check_configuration_windows.ps1` 返回 `OK`；安装前后原 ASAR、宿主配置、既有工作台设置和另一个笔记仓库的 6 个暂存文件／索引核对一致。保存文档后正常重启 Typora 载入本次构建。

## `68f1af2` 搜索性能、统一拖动与文档跨窗（历史记录）

以下为该构建当时的验证范围。后续用户实测发现自定义协议弹窗，且pointer-only拖放不能完成浏览器式跨窗合并，故该版本不能作为当前拖动验收依据；最新修复见下一节。

这一轮由 `5622258` 继续实现用户明确增加的需求，同时全量同步本仓库说明。修正了“当前能力”中残留的旧7种Tree-sitter语言、旧发布文件数、旧窗口模式与多处重复测试数字，当前源码、安装说明、功能矩阵和开发交接各自维护对应事实。

搜索曾把全部候选评分／排序和普通正文匹配放在renderer，并等待Git状态和整轮搜索才显示。现在文件名分片处理，普通匹配进入既有Worker，最多4个文件异步预读，结果逐步加入界面；完成时保留已有节点和焦点。50000名称对照中首批结果449→179ms，事件循环最大间隔248→20ms；1200个暖缓存小文件总时间566→563ms基本持平。人工延迟场景单独标记，不作为真实磁盘性能；原始方法和可复现命令见[搜索性能](search_performance.md)。

拖动不再局限于标签编辑区。活动栏、标签与既有排序控件共用一次指针会话，原图标文字跟随，达到6px才启动，松开才更新模型。取消、Esc、失焦、来源消失和卸载均清理。标签跨组处理对象身份，同组排序同步布局模型，长标签栏支持边缘滚动。

原始Typora 1.14.9已核对 `app.openFile(null, {mountFolder, anchor})` 创建空窗、返回窗口ID并传递初始锚点；`window.open` 被宿主拒绝，未采用。跨窗正文经随机频道在内存传递，接收确认之后仍要重新校验来源内容、磁盘基线和叶子身份；失败保留来源，不写临时正文、不修改ASAR。原生dirty Markdown限制明确保留在[功能说明](drag_and_windows.md)，不能称为完整VS Code辅助窗口移植。

统一构建与 `npm run check` 通过，完整隐藏Electron套件 **45/45** 通过（253.355秒）。随后独立复查补充接收期间自动保存变化、ACK后释放超时、异步文件读取与导航取消边界；重建与 `check` 再次通过，相关 **8/8** 目标复跑通过（62.597秒）：窗口通信、文档快照、阅读生命周期、文件编辑、搜索、启动、拖动和活动栏。文档快照包含21个场景，窗口通信包含14项检查；不把首轮45项目标替代后续变更验证。


最终构建的独立原生 Typora 实例 **20/20** 检查通过：后台Markdown整文移交，源码未保存正文、语言、编码和换行保留，原生Markdown草稿两窗均保持未保存，源标签在确认前不关闭。活动栏验证按住时预览原图标、释放后排序与持久化。证据为 `.cache/native_single_row_compare/pointer_drag_windows_native_final/checks.json`、`verification.json` 与阶段截图。

原生检查使用私有桌面和未修改的ASAR，真正运行宿主加载、JSBridge、BroadcastChannel及多renderer；输入来自fixture的PointerEvent，不声称物理鼠标／宿主trusted事件已实机验收。隐藏Electron拖动目标另外使用trusted输入验证指针捕获、阈值、取消与落点。最初原生调试参数被宿主拒绝，以及横幅挡住标签的夹具失败保留记录，未绕过宿主限制或计入通过。

最终原生实例运行26.9秒后受控清理，没有切换用户桌面。23个发布文件与当前构建一致，原始ASAR、图标及4个临时工程文件字节不变。实际用户安装事务及 `check_configuration_windows.ps1` 已完成并返回 `OK`，自动生成备份；安装前后ASAR、原生profile与既有工作台设置摘要一致。未强制刷新现有窗口，保存后正常重启载入。日志为 `.cache/search_drag_live_install.log`、`search_drag_live_check.log`。

本次全量同步覆盖15份自有Markdown，核查相对文件和标题入口；源码、预构建、158个核心来源摘要及功能标记同步。没有把研究缓存、用户文档或其他仓库纳入提交。

## 2026-09-11 原生标签移交与历史差异页

标签已改用原生HTML拖放：同窗排序、跨组移动、另一窗口指定位置合并，以及按实际窗口边界拖出建窗。原生拖动数据仅含随机令牌，初始化使用安全的文内片段；不再向宿主传递会唤起Windows协议选择器的地址。未保存源码与关闭自动保存的Markdown均核对正文、保存基线及目标接收确认。来源正常移除触发的迟到取消不再中断已确认移交；关闭空附窗前后都复查真实宿主状态。

SCM的长名称、行动作和状态分别占列，图标在悬停、选中和键盘聚焦时可见。提交行新增查看全部改动，历史文件行和历史diff右上角打开所选提交的只读版本；Markdown渲染正文，链接及图片从同一提交读取，删除文件读取父侧存在的版本。真实Git夹具专门把工作区与历史内容写成不同值，核对打开结果及HEAD、索引、磁盘和未保存正文保持。

Diff顶部收为标签同行动作和26px版本行。自动切换使用Monaco固定版本默认值：编辑组实际宽度≤900 CSS px为行内，>900px为左右；不是屏幕宽度百分比，也不是正文边距滑块。左右初始分配0.5只控制并排两侧比例。手动行内和自动窄屏选项独立，模式、版本行、菜单勾选及选区／滚动位置同步；更多菜单接入实际的未变区域折叠、移动代码块、查找和F7无障碍差异查看。

完整构建与`npm run check`通过；隐藏Electron套件 **46/46** 通过（275.547秒）。专项包括真实Chromium拖动数据和工作区模型、两个renderer的握手／确认／取消、异步关闭期间状态变化、SCM明暗主题与窄栏命中、历史正文和资源身份，以及899／900／901px边界、手动模式、双组隔离和阅读位置。测试统一由Node runner持有输出管道并等待退出，修复此前直接启动GUI测试导致的EPIPE错误弹窗。

独立原生Typora检查分为两组，均使用未修改ASAR、私有桌面和隔离文件，不切换用户桌面：

- `.cache/native_single_row_compare/native_tab_transfer_ack_verified/`：11项通过。后台Markdown拖出、未保存源码和Markdown往返合并、保存基线保留、原始文件字节不变；末次只剩原主窗口与仍有后台文件的窗口，两个空附窗已关闭。全部桌面窗口清单无外部协议选择器。
- `.cache/native_single_row_compare/native_git_diff_verified/`：6项通过。核对真实标签行动作、版本行、分组缩窄自动行内，以及历史读取与当前文档隔离；截图额外发现新分组标签仍显示编码URI和默认图标，不能只以这6项通过宣布视觉完整。随后按核心真实生命周期修正：直接访问所属组已有但尚未挂到document的标签，不用全局DOM搜索冒充本组标签。

第一次Git原生夹具因启动参数拼写未启用长等待而超时，记录保留在`native_git_diff_final/`，不计入通过。跨窗输入为程序化DragEvent加真实原生窗口通信；隐藏Chromium另验证trusted拖放数据。最大化／全屏边界采用隐藏窗口尺寸模拟，尚未完成物理鼠标在三种OS窗口状态下的人工往返验收；不将这些不同层次的证据混为一项。

新组生命周期修复后，构建和`check`再次通过，历史阅读（含新组脱离DOM的真实调用顺序）、diff状态和SCM图标 **3/3** 目标通过。最终原生实例`.cache/native_single_row_compare/native_git_diff_release/` **7/7** 通过并检查截图，新组标题正常显示版本箭头和共享文件图标，历史渲染正文正确；23个运行资产与最终发布摘要逐项一致，原ASAR及当前文档字节不变。没有用前次完整46项目标代替这次后续验证。

2026-09-11 已完成实际安装及`check_configuration_windows.ps1`，返回`OK`并自动备份。安装前后的原始ASAR、应用图标、Typora原生profile与既有工作台设置摘要一致；没有关闭或刷新用户现有窗口。保存后正常重启Typora载入本次构建。安装与校验日志为`.cache/tab_history_live_install.log`、`tab_history_live_check.log`。同步核对15份自有Markdown的175个本地链接与64个标题锚点，无缺失目标；没有改动用户文档或其他仓库。


## 2026-09-12 文件命令、资源管理器与终端职责收敛

文件菜单、快捷键和资源管理器共用已核对的原生系统选择入口；移除手填路径替代“打开文件夹”的对话框。选择或关闭目录只更新挂载根，取消及迟到返回不替换正文或丢弃草稿。生产 Explorer 补齐已有创建、复制／移动与回收站服务接线，增加系统定位、所选目录搜索／终端和文件比较；比较读取当前未保存正文。树缩进采用固定上游8px规则并去掉文件行空展开槽，共用紧凑菜单几何。

保留 Typora 原生新建、Markdown 保存、导入／导出和打印，补充代码另存为、保存全部、还原、关闭目录与快捷键。代码另存为保留同一模型、撤销、编码和换行；新文件发布、目标竞争、后续保存基线、取消和失败清理均有专项验证。职责、操作及文件系统边界见[文件说明](file_operations.md)。

终端拆分为配置、进程、xterm、面板几何与会话协调器。默认底部面板具备35px紧凑工具栏、22px会话列表、拆分／合并、身份编辑、查找／剪贴板、隐藏／最大化及设置；面板与编辑器之间搬移同一会话。普通新建跟随工作区目录，明确的仓库入口才查询 Git。配置统一校验和通知；重启取消旧启动，卸载释放全部会话。完整操作与仍未实现差异见[终端说明](terminal_operations.md)。

已运行完整 `npm run check`，最后一次源码调整后再次通过。完整隐藏 Electron 套件首次47项中44项通过；3个失败分别是终端注册数量断言未更新、两个单独打包夹具缺少公共CSS loader。修正后4个相关目标全部通过；工作区目录规则收敛后，终端主题／面板、生产启动和文件菜单4个目标再次通过。保留首轮失败日志，不把首次47项全绿作为结果。日志为 `.cache/check_final_20260912.log`、`ui_20260912.log`、`ui_followup_20260912.log`、`ui_context_20260912.log`。

真实终端交互使用隐藏 Electron 与真实 ConPTY，输入、方向键历史、Ctrl+C中断、Ctrl+Shift+C复制、查找与Escape、右键菜单和尺寸变化通过。运行由Node持有管道，未出现此前EPIPE主进程弹窗。原生宿主使用未改ASAR、私有桌面和隔离文档，分别核对：

- `open_dialog_20260912_a`：系统文件／文件夹窗口实际出现，取消保留文档与挂载目录；选择结果路由另由生产绑定测试覆盖。
- `terminal_native_20260912_c`：11项通过，包括当前原生工作区目录、两个实际PTY、隐藏保留PID、面板／编辑器搬移同一容器与进程、最大化、设置页、终止清理和原生正文／文件字节保持。已检查阶段截图，CSS视口1187×737、系统DPI1.75；这是宿主几何取样，不代表全部显示器和物理拖动已覆盖。

终端原生夹具的前两次取样保留在同名前缀的 `a`／`b` 目录：初次选择器表达式拼写错误，第二次目录参数未按核心要求使用数组；最终 `c` 按真实API签名运行并增加目录断言。三种系统窗口状态下的物理标签拖放、UAC人工交互、Linux与ARM64实机仍保留独立验收缺口；完整VS Code终端、任务／调试／扩展能力并未全部移植。

全局职责规则写入AGENTS，命令、服务、适配器、状态与生命周期的实际分工见[工作台架构](workspace_architecture.md)。需求台账位于Git忽略的 `.cache/issue_tracking/requirements.md`，保留本轮编号及此前16项反馈；台账不随代码提交。

收尾审查修正临时界面引用累积：公共菜单／对话框在关闭时通知所属模块，终端只保存仍打开的界面；关闭后释放引用，卸载仍清理当前菜单与设置。修改后重新构建、完整 check 通过，widgets、terminal_panel、terminal_theme、workspace_startup **4/4** 通过，日志 `.cache/check_lifetime_20260912.log` 和 `.cache/ui_lifetime_20260912.log`；关闭、失焦及重复清理只通知一次，不用前面的原生取样代替这次后续验证。

2026-09-12 最终构建已安装，安装事务自动备份，`check_configuration_windows.ps1` 返回 `OK`。安装前后原 ASAR、应用与文件图标、Typora 原生 profile、既有工作台设置及主题摘要一致；没有关闭或刷新用户现有窗口，保存后正常重启载入。最终日志为 `.cache/unified_workbench_install_final_20260912.log`、`unified_workbench_check_final_20260912.log`，保护项核对为 `unified_workbench_install_final_verification.json`。已核对18份自有Markdown的196个本地链接和63个标题锚点，无缺失目标；用户需求台账及研究缓存未纳入提交。


## 2026-09-12 Shell 发现与底栏浮层、字数位置修复

R006.3.1 移除终端四项固定菜单，新增独立异步发现服务；安装注册表、PATH 与有限候选目录共同生成可用配置，菜单、设置、默认选择与会话启动共用同一结果。支持重检、局部失败提示和取消；保存的默认项失效时明确提示，不偷偷改开其他 Shell。审查发现设置刷新可能覆盖扫描期间的新选择，已改为保留用户当前选择并增加交互回归。

R012 统一适配原生底栏目录、大纲、字数详情及拼写浮层，按触发按钮定位并修正终端层叠和 containment 坐标；R013 修正字数按钮旧底部留白、负 margin 和行高。保留宿主统计、菜单数据和原节点事件。设计分别见[终端](terminal_operations.md#shell-自动识别设计)、[底栏](statusbar_layout.md)。

构建和完整 `npm run check` 通过（`.cache/terminal_footer_check_20260912.log`）。终端面板、主题、底栏、生产启动、公共菜单 5/5 UI 通过；设置刷新选择修正后再次构建，面板15项通过。底栏另有编辑器状态、边距目标回归，三目标共76项，覆盖22px/30px底栏、多位计数、100%/125%缩放和源码隐藏。截图确认字数文字与箭头居中，图钉弹层位于底栏按钮上方。UI日志为 `.cache/terminal_footer_ui_20260912.log`、`terminal_selection_ui_20260912.log`。

真实机器／用户 PATH（剔除当前工具私有 PATH）发现10个配置，672ms：Git Bash、Cygwin、系统PowerShell、CMD及6个MSYS2环境。隔离真实ConPTY对10项全部验证输出、初始工作目录、MSYSTEM、退出码0及子进程清理；证据 `.cache/terminal_profile_detection_20260912.json`、`terminal_discovery_pty_20260912.json`。使用校验后的私有Node与临时运行包，禁止加载用户启动脚本；不把启动成功当作登录脚本或完整工具链验证。此机器没有独立PowerShell 7／WSL发行版；WSL与其他安装形态仅由探测和UI夹具覆盖。


原生 ASAR 隔离取样首次复现最近目录只剩5px阴影：宿主菜单的 `li.show` 规则不能匹配真实DOM，导致已开放条目仍隐藏。已在底栏适配范围恢复宿主 `show` 状态；未开放条目保持隐藏。补充真实选择器回归后，底栏44项及生产启动再次通过；重新构建和完整检查也通过，日志 `.cache/terminal_footer_final_check_20260912.log`、`terminal_footer_native_fix_ui_20260912.log`。

最终原生取样 `footer_native_20260912_e` 的 10 项通过：系统原ASAR、隔离用户数据和文档、从未切换到用户桌面的私有Windows桌面；最近目录可见可点击，原生菜单选择改变隔离挂载根，图钉大纲定位底栏，字数与详情行为保持。各阶段截图及精确几何留在该case目录。中间轮次保留菜单显隐复现、侧栏过渡未稳定时的取样和夹具字符串转义错误；均未修改用户文档或冒充生产异常。此次是原生宿主程序化验收，不代表所有显示器、DPI或物理鼠标场景已覆盖。

修复版已用既有事务安装器安装，配置检查返回 `OK`，备份正常建立。安装前后ASAR、应用图标、原生profile、工作台设置与主题摘要一致；未关闭、刷新或重启用户现有窗口。安装日志 `.cache/shell_footer_install_20260912.log`、`shell_footer_install_check_20260912.log`，保护项证据 `shell_footer_install_verification_20260912.json`。用户保存后正常重启Typora载入。


## 2026-09-12 Ctrl+= 与 Ctrl+- 窗口缩放修复

R014：用户再次明确 Ctrl+= 放大、Ctrl+- 缩小，后续取消插件／更新适配工作，本次只改快捷键及其共享菜单命令。`workspace_zoom` 集中适配已核对的原生 ClientCommand，`workspace_shortcuts` 在终端与普通模态退出分支之前处理窗口缩放；每个按下事件执行一次，松键不重复执行，保留图表局部缩放及 Typora Ctrl+0 正文操作。主键盘、Shift加号与小键盘变体共用同一命令，比例与持久化仍由宿主管理。设计见[窗口缩放](workspace_zoom.md)。

构建、完整 `npm run check` 通过，日志 `.cache/zoom_check_20260912.log`。隐藏 Electron 的 `test_workspace_zoom.cjs` 32项通过，含真实 webFrame、实际 Ctrl+= / Ctrl+- 输入与 xterm 字符隔离、模态优先级和卸载；既有快捷键35项、菜单25项通过。生产启动、源码编辑保存与底栏3个UI目标通过，日志 `.cache/zoom_ui_integration_20260912.log`。其余无关历史缺口没有在本次关闭。

原生 Typora 已更新为1.14.10，核对本机原始缩放实现后，用未修改ASAR、隔离配置与临时文档验证17项：Markdown、Monaco源码和diff、真实ConPTY焦点中的缩放与恢复；原生设置与比例一致、相同模型和草稿版本不变、终端PID不变、文件字节不变。证据 `.cache/native_zoom_1_14_10/zoom_native_b/checks.json` 及阶段截图。100%→120%时CSS视口1187×737→989×614，系统DPI1.75，窗口比例分别1/1.2；未通过修改CSS字号或主题实现窗口缩放。

原生取样使用 renderer KeyboardEvent 调用实际宿主接口，隐藏 Electron 另覆盖真实 sendInputEvent 输入；不声称覆盖全部物理键盘／OS accelerator。首个原生case `zoom_native_a` 被宿主拒绝生产主线程调试参数，已保留失败记录；后续正常启动，不修改或绕过宿主检查。测试从未切换到用户桌面，也没有关闭用户窗口。

修复构建已通过既有事务安装器安装到当前1.14.10，配置检查返回 `OK`，备份已建立。ASAR、应用图标、原生profile、工作台设置和主题摘要保持不变；没有刷新或重启用户窗口。安装记录 `.cache/zoom_install_20260912.log`、`zoom_install_check_20260912.log`、`zoom_install_verification_20260912.json`。21份正式文档与本地台账的293个链接、135个锚点和37项需求关联通过；台账继续被Git忽略。


## 2026-09-12 底栏统一布局与菜单状态修复

R008.1、R019、R015统一到`workspace_footer_layout`的布局组、操作和文本角色。Git、文件动作、源码模式、边距输出、字数、语言和源码/diff状态采用共用高度、内边距与文字基线；业务宽度、内容、状态和事件仍由各模块维护。清除Typora1.14.10 Bootstrap的output顶部7px内边距、语言/字数底部8px内边距，以及实机发现的字数右侧-8px外边距，避免文字偏移和命中区域重叠。AGENTS及[架构](workspace_architecture.md)已要求同类问题先修共用规则并做整体回归。

R012.1按用户最终要求取消底栏重复大纲操作，保留原DOM供宿主访问及卸载恢复，仍从活动栏或视图菜单进入。R016恢复24px官方终端图标与既有48px活动槽。R017新增顶部终端菜单；`terminal_state`只读取领域协调器的实时会话身份，旧菜单在会话切换后不能操作新会话。R018视图勾选读取实际可见状态，状态栏隐藏释放编辑/终端空间，原生Markdown工具栏限制在当前阅读区；它的节点、开关和配置仍由宿主管理。

最终构建及完整`npm run check`通过，日志`.cache/footer_menu_build_final_20260912.log`、`.cache/footer_menu_check_final_20260912.log`。收敛共享布局后的11个相关UI目标全部通过，日志`.cache/footer_menu_ui_contract_20260912.log`；实机发现负横向间距后，最终底栏、边距、编辑器状态和diff四个直接受影响目标再次全部通过，日志`.cache/footer_menu_ui_final_20260912.log`。包含公共规则卸载恢复、明暗状态、缩放、长文字/窄栏、侧栏与终端状态，以及原生和自有菜单动作。

前两轮UI失败记录保留在`.cache/footer_menu_ui_20260912.log`与`footer_menu_ui_followup_20260912.log`：修复过期会话动作捕获、终端border-box几何后，更新八菜单和清理序列化夹具。首轮原生`footer_menu_after`因测试脚本选择器转义错误停止；修正脚本后的`footer_menu_after_b`通过23项，发现负横向间距后最终`native_zoom_1_14_10/footer_menu_final`通过25项。未将这些失败改写为一次通过。

原生验证使用Typora1.14.10原始ASAR（SHA256 `4dbee896f9d5a7f393c69611f57bd877a6b9da895f3884028215c2da7894fb53`）、私有桌面、独立配置和测试文档；renderer事件调用实际菜单及宿主动作。核对100%→120%→100%缩放、6%居中、字数/语言点击区不重叠、显示/隐藏状态栏及工具栏、大纲勾选和真实ConPTY新建/拆分/隐藏/终止；隐藏保留两个PTY，初始PID不变，正文/修改状态及全部原始文件摘要保持。原始阶段截图已逐张核对。此证据不冒充用户桌面人工操作或全部平台验收，测试未切换用户桌面。

此前终端持久化、跨窗口、Shell集成、完整VS Code配置及Tasks/Debug/远端能力，以及系统文件剪贴板、多根工作区等未完成项仍在台账中；插件/更新适配仍按用户要求取消。

最终构建已事务安装，`check_configuration_windows.ps1`结果OK；日志`.cache/footer_menu_install_20260912.log`、`footer_menu_install_check_20260912.log`及`footer_menu_install_verification_20260912.json`。原生ASAR、图标、profile、工作台设置与主题摘要不变，用户窗口未关闭或刷新，保存文档后正常重启载入。22份仓库文档、44个需求编号、314个本地链接、152个锚点验证通过；本地台账被Git忽略。


## 2026-09-12 底栏目录菜单与语言常显复修

R012、R008.1、R015按再次反馈重新打开。旧验收漏掉大纲上下文对文件菜单后代的隐藏、语言入口的非悬停opacity，以及文本裁剪/原生块布局图标；本次补齐真实宿主状态再修共享规则。底栏只把侧栏模式映射到直接操作行，菜单仍依据宿主show状态；公共文字继承底栏字体并使用自然行高，单图标在宿主块布局中居中。语言入口在当前文档激活期间持续显示，原生内容、状态图标及语言菜单保持。正式设计见[复查](statusbar_layout.md#2026-09-12-底栏重复缺陷复查)。

新增测试在旧实现明确失败于“大纲上下文隐藏可用菜单行”，日志`.cache/footer_regression_reproduced_20260912.log`；此前夹具引号错误和一次错误测试文件名分别保留在`footer_regression_before_20260912.log`及`footer_regression_ui_20260912.log`，未当作产品缺陷。修复后底栏、边距、编辑器状态与diff四个UI目标通过，日志`footer_regression_ui_valid_20260912.log`；构建和完整check通过，日志`footer_regression_build_20260912.log`、`footer_regression_check_20260912.log`。

原生1.14.10使用未修改ASAR、当前cpp主题、独立profile与临时文档，在从未切换到用户桌面的私有桌面验证。`.cache/native_zoom_1_14_10/footer_regression_before/`记录菜单5.90px复现，`footer_regression_after/checks.json`记录修复后的14项通过：大纲显示/隐藏、文件树和真实ConPTY下目录菜单均可见可命中，原生最近目录选择成功且文档和PTY身份不变；语言离开hover、关闭菜单、终端聚焦及无关布局变动后仍可见。100%→120%→100%文字Range中心差最大0.24px，字形无裁剪，源码图标居中。已核对修复后菜单、语言和终端三张原始截图，所有原始文档摘要保持。程序化宿主事件与几何证据不等同全部平台/物理鼠标人工验收。


最终构建已通过既有事务安装器安装，配置检查OK；日志`.cache/footer_regression_install_20260912.log`、`footer_regression_install_check_20260912.log`与`footer_regression_install_verification_20260912.json`。原生ASAR、应用图标、profile、既有工作台设置和主题摘要均保持；用户窗口没有关闭或刷新，保存后正常重启载入。22份仓库文档、44个需求编号、316个本地链接和154个锚点验证通过，本地台账保持Git忽略。


## 2026-09-12 统一悬停与独立交互管理

R020/R021/R022按用户连续截图和最新澄清实施。普通按钮由工厂默认接入，模块根范围覆盖动态语义控件；菜单、活动入口、标签及行使用共同角色颜色，禁用、选中、展开与焦点分别处理。领域用变量或显式独立边界覆写；公共规则只绘制已有形状，不改变尺寸或圆角。移除已接管控件的重复hover/focus声明，正文及Monaco/xterm等保留自己的交互。标签和底栏共同居中实际内容，各自保持区域几何。Git提交行收回隐藏操作空位，结构化卡片复用浮层外观与生命周期；详情仍从真实Git读取，不复制独立数据模型。

构建和完整检查通过，日志`.cache/interaction_check_final2_20260912.log`；本轮10个相关UI目标通过，日志`interaction_ui_final_20260912.log`。默认接入夹具验证56项，卡片及布局夹具在补齐局部时序和失败清理后验证72项，涵盖真实指针、明暗主题、100%/120%/125%缩放、200/280/420px侧栏、动态按钮、形状保持、局部覆写、禁用/嵌套隔离、键盘焦点、复制、窗口边界、刷新/滚动/Esc及异步身份。真实Git检查包含首个父提交、根提交、重命名和二进制统计。最后清理后的8个目标全部通过，日志`interaction_ui_cleanup_final_20260912.log`；最终完整check再次通过，日志`interaction_check_delivery_20260912.log`。

原生Typora1.14.10使用原ASAR、隔离profile/文档和未切换到用户桌面的私有桌面。`native_zoom_1_14_10/interaction_native_verified/checks.json`记录13项通过：原生Markdown和源码标签字形中心、顶栏/活动入口角色、窄宽底栏、Git卡片实际数据和边界、Esc恢复焦点，以及正文、修改状态、文件摘要和ConPTY身份保持。已核对原始卡片/终端截图；新profile的原生欢迎提示保留，标签几何另有DOM与Range取样。这是程序化原生宿主证据，真实指针反馈由隔离Electron夹具验证，不冒充用户桌面人工操作。

失败证据保留：初始交互规则的层叠、隐藏动作槽溢出、提交卡片图标资源遗漏已修复；测试夹具的缩放坐标、隐藏窗口焦点模拟及跨进程函数返回分别修正。第一次图标元数据不符合LF要求，规范化后完整检查通过。两次原生底栏断言误把原有900px紧凑隐藏当成居中失败，核对生产规则后按窄宽两个状态验证，没有为了测试强行显示控件。此前未完成需求仍保留，已取消的插件/更新研究与Git同步未恢复。


最终候选在`native_zoom_1_14_10/interaction_native_delivery/checks.json`再次通过同一组13项原生检查。既有事务安装器已安装，配置检查OK；`.cache/interaction_install_20260912.log`、`interaction_install_check_20260912.log`和`interaction_install_verification_20260912.json`记录安装及保护摘要。原生ASAR、应用图标、profile、既有工作台配置与主题均保持，用户窗口未关闭或刷新，保存后正常重启载入。23份文档、47项需求及本地链接/锚点检查通过，台账保持Git忽略。此次交付不包含插件更新适配或远端推送。

## 2026-09-13 Graph配置、卡片避让与预览重选

R022/R023/R024承接9月12日反馈。用户最终撤销“等待1秒”的判断，确认只对齐截图中的按钮、悬停和布局，并记录其余Graph配置差异。核对本机VS Code1.137.0固定提交及本地用户设置后，采用完整列表避让、居中带指示角的紧凑卡片；动作随悬停/焦点立即出现，现有设置新增Graph行操作常显开关。首次提示默认500ms，已显示的同组提示即时切换；未建立单独的1秒策略。共享定位与生命周期不含Git逻辑，领域只提供边界和数据。全部5项`scm.graph.*`及关联规则见[配置核对](git_graph_configuration.md)。

搜索重复选择旧对象曾直接返回，跳过预览定位。现复用`reveal_match`重设Markdown匹配或Monaco精确选区；不重新读取文件、不重建DOM/模型，不打开中央文档。基线新增回归明确复现两项问题，日志`.cache/graph_preview_baseline_20260912.log`。最终构建`graph_preview_build_final_20260913.log`和完整检查`graph_preview_check_final_20260913.log`通过。

8个相关UI目标的最终结果均通过：搜索重选与预览见`graph_preview_ui_20260912.log`；Git完整交互/设置、SCM几何、默认交互与阅读链接见`graph_preview_ui_suite_20260912.log`；该套件中两个初次失败项修正后分别在`graph_preview_geometry_final_20260913.log`（94项）和`graph_preview_history_focus_20260913.log`通过。覆盖真实指针动作点击、200/280/420px侧栏、明暗与缩放、长消息和无可用区域、左/右/上/下回退、异步提交身份、配置持久化、复制、Esc及清理。MD/代码同结果、文件行与折叠重开验证原节点/模型与读取次数；未将一张截图或旧通过记录代替本轮验证。

原生Typora1.14.10在独立profile、临时Git文档和未切换到用户桌面的私有桌面通过17项，证据`native_zoom_1_14_10/graph_preview_native_final_20260913/checks.json`。终端开启时卡片不覆盖列表/滚动条与行末按钮，12px/19px、2px 8px、3px圆角与指示角取样符合规则；重选搜索结果回到实际匹配，原文、修改状态、文件摘要与ConPTY身份不变。已核对卡片及预览截图。原生部分为程序化宿主事件/焦点与几何验证，真实指针由隔离Electron夹具覆盖。

失败与修正保留：常显开关的CSS优先级冲突、ResizeObserver同步尺寸反馈已修复；边界夹具自动滚屏、旧历史夹具把展开当焦点且未模拟窗口焦点、首次原生测试用整数严格比较12.00000095px，均根据实际状态修正断言。一次完整检查在无改动的安全保存测试遇到Windows临时文件占用，单目标与最终完整检查均重跑通过；未为本轮改动无关保存逻辑。首次UI调用中的错误测试名只属于调用错误。

最终构建已通过既有事务安装器安装，配置检查OK；证据`.cache/graph_preview_install_20260913.log`、`graph_preview_install_check_20260913.log`及`graph_preview_install_verification_20260913.json`。原生ASAR、应用图标、profile、既有工作台设置与主题摘要保持；用户窗口未关闭或刷新，保存后正常重启Typora载入。24份仓库文档、49项需求、344个本地链接和171个锚点检查通过，台账继续被Git忽略。此前未完成项保持，本次不包含插件更新适配或远端推送。

## 2026-09-13 原生源码按钮悬停补齐

R020再次反馈：底栏源码模式按钮遗漏悬停。原生div只取得共同布局类，未声明交互角色；此前只验证自有按钮的默认接入，遗漏了这个宿主边界。本次在原生底栏既有control角色登记处一起接入公共action，覆盖源码、字数、语言、新建、目录菜单和列表切换；不新增按钮专用hover样式。显式独立角色保持，卸载只清理适配器新增属性，原节点、提示、处理器及业务状态不变。设计见[原生底栏接入](workspace_interaction.md#r020-原生底栏操作接入)。

新增既有底栏回归在基线明确失败于源码按钮悬停，证据`.cache/source_hover_baseline_20260913.log`。修复后底栏127项通过，含真实指针、六类原生操作的明暗/100%与125%缩放、邻居隔离、几何与移出恢复、源码切换、终端和独立角色卸载；证据`source_hover_footer_final_20260913.log`。公共默认56项通过，见`source_hover_ui_20260913.log`；其中第一次底栏重跑因新增夹具点击缺少坐标报Invalid event object，补齐真实输入参数后通过，未改业务代码迎合测试。

构建与完整check通过，证据`source_hover_build_20260913.log`、`source_hover_check_20260913.log`。原始ASAR的Typora1.14.10在私有桌面及隔离profile/文档中通过9项，见`native_zoom_1_14_10/source_hover_native_20260913/checks.json`：全部原生操作接入、100%/120%缩放几何、真实宿主源码切换、终端下按钮命中、文档/修改状态及PTY身份和磁盘字节保持。已核对原始终端截图；原生部分是程序化DOM/事件/几何验证，真实指针反馈由隐藏Electron回归覆盖。未切换用户桌面或重启用户窗口。

修复构建已事务安装，配置检查OK；安装及保护摘要记录在`.cache/source_hover_install_20260913.log`、`source_hover_install_check_20260913.log`、`source_hover_install_verification_20260913.json`。原生ASAR、图标、profile、工作台配置与主题保持，保存后正常重启Typora载入。正式需求索引与设计同步，R020沿用原编号记录这次遗漏和验收。

## 2026-09-13 默认圆角与文件行悬停布局

R020/R022/R024按最新截图修复。公共交互层统一4px控件圆角，动态按钮、原生底栏、标签、终端与Git行共用默认；圆形、分裂接缝和顶栏搜索通过局部变量保留独立形状。固定VS Code1.137.0的来源及Modern UI开关边界见[圆角设计](workspace_interaction.md#r020-默认圆角与独立形状)。

用户最终澄清比较的是悬停和移出。核对上游资源标签结构后，源码改动与历史文件列表复用一个名称/目录裁切区；不再分别flex压缩名称和目录。历史文件行取消普通态常驻动作空位，悬停/键盘焦点或常显设置时才分配操作空间，状态列保持原位。混合字号的目录行盒不再抬高文件行。默认、悬停、移出均保持名称起点、字号和自然字形宽度，长名称末端省略，动作不盖文字。详情计时与业务选择状态不变。

构建及完整check通过，日志`.cache/round_hover_build_20260913.log`、`round_hover_check_20260913.log`。11个相关UI目标最终均通过：`round_hover_ui_20260913.log`首轮10项通过；Graph完整交互在更新旧直角断言和原名称节点裁切断言后通过`round_hover_graph_final_20260913.log`。公共默认56项、底栏127项；真实指针覆盖明暗/缩放、窄宽列表、文件行移入移出、动作点击与键盘、动态控件、局部形状、禁用及卸载恢复。失败复现与首次结果保留，不记作首轮全通过。

原生Typora1.14.10隔离实例通过15项，证据`native_zoom_1_14_10/round_hover_native_final_20260913/checks.json`。原生底栏/终端默认圆角、分裂接缝、文件行22px与动作命中、源码模式切换及文档/修改状态/PTY身份均保持，已核对原始截图。首次取样发生在宿主侧栏入场动画中，名称横坐标尚在窗口外；修正夹具等待实际布局稳定后通过，未为该夹具修改业务行为。原生部分为程序化宿主验证，真实鼠标状态由隔离Electron测试覆盖。

本次构建已事务安装，配置检查OK；`round_hover_install_20260913.log`、`round_hover_install_check_20260913.log`和`round_hover_install_verification_20260913.json`记录结果。原ASAR、图标、profile、工作台设置与主题摘要不变，用户窗口未重启，保存后正常重启载入。后续安装文档规范化及未完日常操作分别继续实施。


## 2026-09-13 安装入口规范化

R025：公开入口现统一为install/check/restore，Windows加_windows，CMD只转交安装；事务脚本归scripts/。本文历史记录中的configure和check_configuration等名称保留原时点含义，当前用户操作统一见[安装指南](installation.md)。首页改为产品介绍与Windows快速开始，平台、离线Node、更新覆盖、卸载与回退备份用途集中说明。恢复对比剥离托管入口后的宿主页面，外部变化时在写入前拒绝；2026-09-13：Windows PowerShell 5.1 公开 install/check/restore 入口在含空格的隔离目录通过首次/重复安装、损坏清单、宿主变化零写入拒绝、原偏好恢复和故障回滚；Python 事务全部通过。完整 npm run check 通过；26份文档、50项需求、354本地链接与187锚点核对通过。Bash 命令所在环境未具备，未运行 UCRT64/Linux 入口或 ARM64 原生实例，已在用户指南明确。日志为 `.cache/install_entry_windows_final_20260913.log`、`install_entry_python_20260913.log`、`install_entry_check_20260913.log`；文档证据为 `.cache/issue_tracking/requirements_install_entry_verification_20260913.json`。测试仅使用临时安装和用户数据目录，未卸载或重启用户 Typora。

## 2026-09-13 终端分隔条与列表排序

R006.1：列表默认120px，可拖至46px图标模式或80px～500px文字宽度；分屏宽度按会话身份保留，同组排序跟随会话，同窗跨组拖放移动整个组。列表与分屏使用共同分隔条交互，正常松开提交，取消恢复。搬到编辑器时解除分屏宽度，保持同一个PTY和缓冲。

最终 `test_terminal_panel.cjs` 通过真实Electron指针拖动、取消、左右列表、窄窗口/125%缩放、同组/跨组/空白排序与编辑器满宽验证；`test_terminal_theme.cjs` 覆盖明暗与卸载。列表DnD由浏览器DragEvent/DataTransfer事件链验证，未冒充物理鼠标拖放实测。隔离原生Typora1.14.10的 `terminal_layout_native_20260913/checks.json` 通过11项，包含真实PTY身份、缩放几何、整个组移动与原文档字节保护；截图已检查。发现的border-box分屏偏差和编辑器残留宽度已在共同几何边界修复。

构建与完整检查日志为 `.cache/terminal_layout_build_final_20260913.log`、`.cache/terminal_layout_check_final_20260913.log`，目标UI日志为 `.cache/terminal_layout_ui_final_20260913.log` 与 `.cache/terminal_layout_theme_final_20260913.log`。此条仅记录已实现的同窗布局，跨窗PTY、进程恢复、任务与远端仍保留原未完成编号。安装在本次日常操作功能验证后统一执行，不重启用户窗口。

## 2026-09-13 系统文件剪贴板实现与待验收项

R009已接入共享文件剪贴板服务、Windows CF_HDROP适配及Linux文件URI适配；外部来源复制，同窗同根剪切经过原文档事务。没有把正文路径文本当作文件列表，没有自动保存草稿，也没有恢复已取消的插件迁移或Git远端同步。

本轮 `clipboard_ui_first_20260913.log` 中Explorer与生产启动两个目标通过；`clipboard_document_ui_20260913.log` 中文档编辑目标通过，核对复制磁盘字节、异步写入阻止卸载、剪切保留模型/撤销/草稿及后续保存路径。文件引擎14组、剪贴板服务11组回归和Windows PowerShell 5.1 C#编译通过。最终构建和完整检查也通过，日志为 `.cache/clipboard_build_final_20260913.log` 和 `.cache/clipboard_check_final_20260913.log`；26份正式文档、50项需求、357个本地链接及190个锚点核对无误。

真实剪贴板隔离测试首次因非管理员不能创建命名窗口站返回Win32 5；系统自动命名的窗口站已存在，创建专用实例返回183，启动器拒绝复用，未触碰用户剪贴板。已准备管理员启动的新窗口站测试，当时等待系统确认；原生Windows互通、Linux桌面互通均未标为通过。R009尚未完成原生验收及安装收尾，后续从上述测试入口继续。终端R006.1的已验证提交f90b409已单独事务安装，配置检查OK，ASAR、图标、原生偏好、工作区设置和主题摘要均不变，未重启用户窗口。安装只使用该提交的发布文件，之后完整恢复仓库中R009待验收构建，证据为 `.cache/terminal_verified_release_20260913.json`、`.cache/terminal_daily_install_verification_20260913.json`；R009未混入本次安装，其他大能力不关闭。

## 2026-09-13 Markdown 字体颜色与明暗主题适配

R026已实现原生选区右键菜单的常用色、自定义色和恢复默认，使用同色系亮度调整适配背景。一次格式操作进入一个原生撤销事务，主题切换不改 Markdown 或撤销栈。表格源码缓存与单元格写入共同撤销，完整行内代码、转义字符和实体保留语法；不可安全映射的选区保持原文。使用说明与具体边界见[字体颜色设计](markdown_text_color.md#r026)。

最终独立发布包的 `npm run build` 和完整 `npm run check` 通过；新增纯逻辑4组通过，字体颜色菜单6组、共享终端主题9组及工作台启动12组隐藏Electron回归通过。菜单测试使用实际公共对话框样式，检查常用／自定义颜色、无效输入、方向键、Esc、换文档拒绝、4px圆角、窄窗口及卸载恢复。日志为 `.cache/text_color_release_build_20260913.log`、`text_color_release_check_20260913.log` 和 `text_color_release_ui_20260913.log`。

Windows Typora1.14.10私有桌面的最终发布包通过原生11组回归，覆盖中文与emoji、格式与链接、重复／局部恢复、跨段落、行内代码、表格保存、转义实体、过期身份及真实GitHub／Night主题。在新实例重新打开上一实例保存的文件，颜色与单个菜单入口保留，主题切换后文件字节和dirty状态不变；另用真实菜单动作验证常用／自定义色及原生撤销。原生交互由DOM事件和宿主API驱动，没有将其称为物理鼠标人工验收；截图已检查。完整证据为 `.cache/native_zoom_1_14_10/text_color_release_native_20260913/checks.json`，Linux／macOS原生实例未验证。

本功能从已验证终端基线 `f90b409` 独立构建并事务安装，未带入其他待验收的文件操作代码。安装检查返回OK，ASAR、图标、原生偏好、工作区设置和主题摘要全部不变；已保存备份，未重启用户窗口。安装证据为 `.cache/text_color_install_20260913.log`、`text_color_install_check_20260913.log` 和 `text_color_install_verification_20260913.json`；隔离发布的输入与摘要记录在 `.cache/text_color_release_20260913.json`。

## 2026-09-13 SCM Graph 操作补齐

R027按固定VS Code 1.137.0源码核对工具栏、仓库分区、显隐／快捷键及文件／提交菜单。引用范围支持全部／自动／多选搜索，定位HEAD可解除排除当前提交的筛选；隐藏与窄栏溢出动作从“更多”执行，列表／树形、仓库分区状态持久化。仓库菜单补充工作树列举、当前／新窗口打开、创建及受保护移除；提交比较使用真实上游／基线对象，复制完整消息。文件菜单接通只读HEAD与侧栏定位，重命名／新增和已跟踪忽略规则有对应状态。操作说明与设计见[SCM操作](git_scm_actions.md#r027)。

独立发布目录从已交付字体颜色基线 `2b86932` 加入本需求生成，未带入R009待验收的文件剪贴板。最终构建和完整 `npm run check` 通过，新增真实Git／宿主入口14组与SCM界面11组通过；既有Graph交互以及历史布局、侧栏布局、文件图标、VS Code几何和设置视图回归通过。覆盖多远端、上下游异名、分离HEAD／空仓、工作树保护、切库／销毁后的旧动作、输入／快捷键、明暗、180／240／380px侧栏以及150%／200%小窗口。原生验证发现窄图头裁断文字，已改为独立标题省略、徽标按可用空间隐藏并重跑相关测试。

最终隔离Typora1.14.10通过10项原生宿主检查：仓库行和图头均为22px、隐藏／更多／恢复、实际上游操作预览取消、列表／树形、工作树主目录保护、HEAD源码、历史Markdown与侧栏选中文件。实际工作树新窗口已打开正确目录，原窗口、文档和dirty状态保留。文档字节保持不变，截图已复查。原生操作由DOM事件驱动；Electron目标另用sendInputEvent验证指针和键盘，不冒充物理设备人工验收。Linux／macOS原生实例未在本次验证。

最终完整检查日志为 `.cache/scm_actions_bridge_final_check_20260913.log`，相关界面日志为 `.cache/scm_actions_release_ui_20260913.log` 、`.cache/scm_actions_title_final_ui_20260913.log` 与 `.cache/scm_actions_bridge_ui_20260913.log`；前一轮侧栏夹具仍引用已删除的下拉框，更新为真实仓库行后在标题最终日志通过。原生最终结果为 `.cache/native_zoom_1_14_10/scm_actions_bridge_native_20260913/checks.json`；此前失败尝试保留，最终验收修正了刷新等待及Shadow DOM定位。开窗专项另发现编辑器路由截获空文件调用，已改为与标签移交共用主进程 `app.openFile` 通道和安全 `#` 片段；新增入口回归及既有标签移交22组均通过。

已经事务安装此独立构建，安装后检查OK，ASAR、原生图标、偏好、工作区设置及主题摘要全部不变，保留事务备份且未重启用户窗口。安装证据为 `.cache/scm_actions_install_20260913.log`、`scm_actions_install_check_20260913.log` 和 `scm_actions_install_verification_20260913.json`，发布输入及摘要见 `.cache/scm_actions_release_20260913.json`。截图中的聊天／解释／评审和定义／引用属于外部扩展或语言服务，本轮不标为完成；此前分页等配置差异和R009继续保留原状态。

## 2026-09-13 文件行整行开关与双击重命名

R028：搜索文件组的箭头、图标、名称、路径和空白都可单击开关；移除按钮保持独立，匹配片段单击重新定位下方预览，双击或 Enter 打开。资源管理器文件夹整行立即开关，已单独选中的文件／目录再双击进入改名。首次双击未选中项只打开／展开一次，F2、菜单与原改名事务保持可用。实现及职责见[文件行设计](file_operations.md#r028-文件行的单击双击与加载反馈)。

迟缓原因是目录展开等待读取完成，以及刷新替换可见行导致浏览器连续点击目标丢失。现在先更新展开和读取状态，可见行按节点复用，动画帧合并更新；读取中反复开关只发起一次请求，完成后服从最后一次开关。没有为单击引入等待双击的定时器。

独立发布包从 `a983aee` 构建，未带入 R009 待验收的系统文件剪贴板。构建与完整检查通过，日志为 `.cache/tree_interaction_build_20260913.log` 和 `.cache/tree_interaction_check_20260913.log`。最终 Explorer／Search 两个完整 UI 目标通过，覆盖真实 Chromium 指针双击、名称／图标／空白、2000项虚拟列表、慢读取消和重开、缓存复用、刷新节点身份、多选、键盘、改名冲突／取消、重复预览、Markdown／源码、明暗变量、220px窄栏及100%／125%页面缩放，见 `.cache/tree_interaction_hit_matrix_ui_20260913.log`。生产启动、文件搜索和文档编辑三个相关目标通过，见 `.cache/tree_interaction_related_ui_20260913.log`；文档编辑继续覆盖草稿、模型／撤销与保存路径。

隔离的原生 Typora 1.14.10 最终六项通过：整行命中与节点复用、目录双击改名取消、已打开 Markdown 实际改名并同步原生路径、搜索整行开关、预览复用及双击打开、全部夹具正文字节保留。结果与截图保存在 `.cache/native_zoom_1_14_10/tree_interaction_native_verified_20260913/`，实际资源管理器行高仍为26px，未改动原几何。截图已检查。原生检查由 DOM 事件及实际命中区域驱动，不代表物理鼠标人工验收；Linux／macOS原生实例未验证。

早期原生脚本先遇到侧栏过渡期间命中等待、Python标准输入编码及夹具位于上级 `.cache` 忽略目录的问题；最终脚本显式使用 UTF-8，并通过搜索的忽略设置开关读取临时夹具。早期失败记录保留，未据此修改生产搜索范围或绕过实际文件读取。完整验收以最终六项结果为准。

本次独立构建已经事务安装，安装后检查返回OK；ASAR、原生图标、偏好、工作区设置和主题摘要均不变，已保存配置备份，未重启用户窗口。安装证据为 `.cache/tree_interaction_install_20260913.log`、`tree_interaction_install_check_20260913.log` 和 `tree_interaction_install_verification_20260913.json`。需求索引与全部本地文档链接检查通过；R009及此前其他未完成项保留原状态。


## 2026-09-13 顶栏搜索取消整框悬停选中色

R029：鼠标移入顶栏搜索时保留中性背景及原主题文字颜色，旁边的历史导航和其他按钮继续使用公共悬停。保留原按钮节点、公共action角色、提示、5px圆角、22px内容高度、Tab焦点和点击／Enter／Space搜索。固定来源与局部覆写职责见[搜索入口设计](workspace_interaction.md#r029-顶栏搜索入口的悬停外观)。生产修改仅为两个公共外观变量；没有新增全局监听或搜索状态。

顶栏回归先复现旧版从透明变为 `rgba(0,0,0,0.12)` 的背景变化，再验证最终结果。八种组合覆盖明暗自定义前景、1280／500px窗口、100%／125%页面缩放，比较背景、前景、边框、几何、焦点与调用次数；旁边导航继续变色，每次鼠标／键盘激活只打开一次。最终日志为 `.cache/titlebar_search_verified_ui_20260913.log`。旧夹具缺失doctype造成怪异模式，现使用标准模式和无边框窗口；输入使用完整鼠标与键盘事件序列。标题、原菜单、选区、窄窗及卸载回归同时通过。

最终构建与完整检查通过，日志为 `.cache/titlebar_search_final_build_20260913.log`、`titlebar_search_final_check_20260913.log`。相关生产启动通过，见 `.cache/titlebar_search_final_ui_20260913.log`；该轮公共悬停测试因旧Graph夹具缺少branches字段失败，补齐现有字段并将几何布局与聚焦分步后，94项公共悬停检查通过，见 `.cache/titlebar_search_related_final_ui_20260913.log`。没有为测试修改生产Graph或浮层行为。

隔离原生Typora1.14.10最终七项通过，结果和已检查的截图位于 `.cache/native_zoom_1_14_10/titlebar_search_native_final_20260913/`，包含实际主题变量、整框命中、可见焦点、真实搜索打开／取消、125%缩放及正文字节保持。原生使用DOM激活和实际计算样式；真实Chromium指针及键盘序列由Electron夹具覆盖，不冒充物理鼠标人工验收。原生检查发现自定义属性直接写inherit并不能传递颜色，已改为引用 `--tc-title-fg` 并增加不同于默认色的夹具；高DPI轮廓按实际可见性及亚像素精度核对。宿主拒绝额外调试启动参数的早期尝试保留为失败证据，最终使用原有普通启动／文件RPC入口，未修改ASAR。

本次从 `ae30dff` 独立构建并事务安装，安装后检查OK；原程序、原生图标、偏好、工作区设置和主题摘要全部不变，保留配置备份，未重启用户窗口。证据为 `.cache/titlebar_search_install_20260913.log`、`titlebar_search_install_check_20260913.log`、`titlebar_search_install_verification_20260913.json` 和 `titlebar_search_release_20260913.json`。R009系统文件剪贴板原差量及其他未完成项保持独立状态。

## 2026-09-13 文档标签右键菜单

R030按固定VS Code 1.137.0源码完善文档标签菜单：关闭其他／右侧／已保存／全部，路径与面包屑复制、文件定位、Markdown预览／重新打开、保持打开／固定、四方向分屏与相邻组移动、移动／复制到新窗口。菜单绑定右键目标，批量关闭仅作用于所属组且保留固定项，取消或保存失败停止后续关闭。使用与能力边界见[标签菜单](editor_tab_menu.md)。

同窗源码副本共享Monaco模型、格式及保存基线，另存为同步全部路径；关闭最后一个副本才释放草稿。Markdown分屏使用当前原生内存并订阅编辑更新。原生保存等待实际写盘结果，放弃修改重新加载磁盘。原生复查发现已经加载的同一Markdown重激活后不会再发`file:open`，旧等待使保存超时；现按当前路径及实际加载状态决定等待，后台已加载文档也覆盖该路径。测试夹具曾提前恢复保存接口造成一次误报，已修正恢复顺序，原失败日志保留。

最终构建与完整 `npm run check` 通过，日志为 `.cache/tab_menu_release_20260913/enhancements/build_tab_menu_final.log` 和 `check_tab_menu_verified.log`。文档动作28项与启动12组最终通过（`ui_tab_menu_save_final.log`）；相关源码编辑、关闭保护、快捷键、标签控件、核心、文档移交和原生标签拖动目标通过，早期相关日志为该发布目录的 `ui_tab_menu.log`、`ui_tab_menu_final.log`，后者的启动夹具失败由最终启动日志替代。跨窗测试通过真实双renderer的复制不释放、确认后移动及失败／取消保护；保留原接收确认与来源身份复查。

最终隔离Typora1.14.10通过18项，包含后台右键、实际保存／放弃／取消、编辑方式切换、当前内存分屏、实时预览及固定图标，证据 `.cache/native_zoom_1_14_10/tab_menu_native_verified_20260913/checks.json`。菜单截图已检查。原生使用私有桌面、原ASAR、隔离文档、宿主API和DOM动作；实际Chromium指针与键盘由Electron测试覆盖，未冒充物理设备人工验收。未验证Linux／macOS原生环境。

最终构建已事务安装，安装后检查OK，ASAR、应用图标、原生偏好、工作台设置和主题摘要不变。日志为 `.cache/tab_menu_install_20260913.log`、`tab_menu_install_check_20260913.log` 和 `tab_menu_install_verification_20260913.json`；用户窗口未重启，保存后正常重启载入。R009系统文件剪贴板的18项原工作区差量独立保留，未带入本次安装。Share、Chat／Codex及语言服务引用提供者仍属于未接入能力，不以无效菜单冒充实现。


## 2026-09-13 图片按钮与双击放大

R031：原生 Markdown 与 Git 历史阅读中的已加载图片增加“全屏查看”按钮，图片本身只响应左键双击放大，单击保留 Typora 选中。图片与 Mermaid 共用查看器，支持缩放、适应宽度／屏幕、100%、拖动、Ctrl+滚轮和 Esc；图片首次完整适应窗口且不放大小图。入口位于正文外的阅读层，切换来源、资源变化和销毁均清理。具体设计与边界见[图片与图表查看器](reading_media_viewer.md)。

本次从 `a21af8d` 独立构建，未带入 R009 系统文件剪贴板的原有18项未提交差量。最终 `npm run build` 与完整 `npm run check` 通过，日志为 `.cache/image_viewer_build_final_20260913.log`、`image_viewer_final_check_20260913.log`。图片专项26项以及 Mermaid 交互、窗口缩放、阅读生命周期、历史阅读共5个 UI 目标通过，见 `.cache/image_viewer_complete_ui_20260913.log`；使用真实 Chromium 指针和键盘，覆盖单击／双击、大图低于20%、小图与微小图片、原始选区、拖动、明暗、窄窗口、加载失败及卸载。修复了 Mermaid 复制到按钮SVG而非图表的问题，并在共同捕获入口保持首次点击有效。

隔离的原生 Typora 1.14.10 最终12项通过，证据与已复查的四张截图位于 `.cache/native_zoom_1_14_10/image_viewer_native_visible_20260913/`。实际本地图片、按钮与双击、100%／Ctrl缩放、主题背景、Mermaid、切换文档清理均正常；Markdown、dirty状态、来源图片节点和文档字节保持。原生测试使用宿主API与DOM激活，指针与键盘输入由独立Electron测试覆盖，未验证Linux／macOS原生环境。早期原生脚本的启动超时、首帧尺寸假设及隐藏图表副本定位失败保留在缓存；最终脚本等待截图完成并选择实际可见入口，不据此修改宿主渲染行为。

最终构建已经事务安装，安装后检查OK；原ASAR、应用图标、原生偏好、工作区设置和主题摘要全部不变，保留事务备份，未重启用户窗口。日志为 `.cache/image_viewer_install_20260913.log`、`image_viewer_install_check_20260913.log` 和 `image_viewer_install_verification_20260913.json`。保存文档后正常重启即可载入；R009与其他未完成项保持原状态。

## 2026-09-13 提交标题、作者与引用排版

R022。作者原来作为右侧独立列，短标题后留下大块空白，且有引用时不再渲染作者。按本机固定 VS Code 1.137.0的 HistoryItemRenderer 与 IconLabel 规则，标题／作者现在是同一裁切区内的连续行内文本；长内容从末端省略。引用与动作继续放在行尾，作者与引用同时存在，分支描述最大100px，当前提交标题／作者字重600。浅色作者透明度0.95、暗色0.7，键盘聚焦为1。详细来源、边界和配置差异见[权威设计](workspace_interaction.md#r022-提交标题与作者连续排列)。

此次先用新增断言在旧实现复现“作者应紧随标题”失败，再应用修复。`npm run build`、完整 `npm run check` 通过；`npm run check:ui -- test_scm_history_layout.cjs test_scm_vscode_geometry.cjs test_git_graph_interaction.cjs test_workspace_interaction.cjs test_scm_sidebar_layout.cjs` 五个目标通过。提交标签新增16组场景覆盖220/300/480/600px、明暗、100%/125%、短／长标题、长／空作者、多空格、引用共存、hover/移出、焦点和常显。真实 Chromium 指针验证名称起点、自然宽度、共同裁切边界和按钮命中，原有多轨拓扑／文件行动作测试保持通过。测试截图与JSON保存在忽略的本地证据目录；暗色夹具同时提供实际前景和背景变量，不能用仅切主题标记代替可读性检查。

隔离原生 Typora 1.14.10通过5项检查，使用真实六提交仓库和已构建资产。通过已有侧栏键盘命令分别调整为300／500px，实测提交区约299.43／499.43px；作者紧接标题，间隔约5.85px，聚焦动作22px、可命中；长标题统一省略，提交单击展开／折叠正常。截图已逐张检查。首次夹具过早读取正文基线，最终在初始文档加载完成后重取基线，全文与所有夹具磁盘文件保持不变。原生检查通过DOM焦点、点击和真实布局完成，不冒充物理鼠标验证。最终原生证据为 `commit_row_native_verified_20260913/checks.json`，初次失败记录保留供审查。

交付：已从独立干净发布目录完成事务安装；安装后检查 `status: OK`，24项已安装资产（含摘要清单）与本次构建逐项一致。原ASAR、应用图标、原生配置、工作区设置及主题摘要不变，未重启用户窗口。修改只合并并提交本轮R022文件；原有18项R009文件剪贴板差量保留，未纳入构建或安装，也未据本轮检查将其标为完成。


## 2026-09-13 全屏查看入口不遮挡图片

R031追加反馈：图片入口原来投影在图像可见部分的右上角，滚动后仍贴着图像，遮住内容。现在图片与Mermaid共同使用 `reading_media_entry` 控件工厂和样式：32px按钮位于图像上方，留出8px间隔，流内工具行随内容滚动；工具行离开视口后不吸附到图像上。按钮仍在正文外，原生 `.md-image` 只增加空占位，普通HTML／历史图片使用可释放包装，原图节点和属性保持。单击选中、双击放大继续有效。见[统一设计](reading_media_viewer.md)。

从 `b986b36` 独立构建；`npm run build`、完整 `npm run check` 通过，日志为 `.cache/media_entry_release_20260913/build_initial.log` 和 `check_final.log`。图片入口专项38项以及Mermaid交互、阅读生命周期、历史阅读、窗口缩放共5个UI目标通过，日志 `ui_second.log`、`ui_related.log`。使用Chromium指针／键盘覆盖首次点击、选区、单击／双击、滚动到图片中段、同段多图、极小图片、失败和销毁；1000／440px、100%／125%、明暗组合实测工具行不覆盖内容。修正了回归夹具中对悬停样式比较及滚动目标的旧假设，首次失败日志保留。

隔离原生Typora1.14.10通过19项，证据 `.cache/native_zoom_1_14_10/media_entry_native_complete_20260913/checks.json`。验证图片与Mermaid首次打开、滚动、不遮挡、原图节点和属性、Markdown／dirty与磁盘字节、含图段落编辑后撤销／重做、原生HTML图片尺寸、实际Night主题和切换文档。图片、Mermaid与Night截图已逐张检查。原生通过私有桌面中的宿主API／DOM激活完成；真实指针与键盘由Electron测试覆盖，未验证Linux／macOS原生环境。早期原生夹具对隐藏Mermaid副本总数和缩放后整数宽度的错误假设已修正，未据此改变宿主行为。


最终构建已事务安装，安装后检查 `status: OK`；ASAR、应用图标、原生偏好、工作区设置和主题摘要不变，保留本次事务备份，用户窗口未重启。安装日志为 `.cache/media_entry_install_20260913.log`、`media_entry_install_check_20260913.log` 和 `media_entry_install_verification_20260913.json`。保存文档后正常重启即可载入。原18项R009系统文件剪贴板差量保持独立，未带入本次构建与安装；其他未完成项维持原状态。


## 2026-09-13 Esc恢复与媒体悬停入口

R032按固定VS Code1.137.0的焦点归还、对子菜单逐层取消及Esc按下／释放配对规则实现。菜单、对话框、文件搜索、核心输入框／选择器和图片／Mermaid查看器复用同一焦点与退出管理。鼠标打开后Esc可回到原光标／选区继续输入；键盘从入口打开则返回入口。焦点已转移、来源更换或销毁时仅清理，不恢复失效位置。R031最后确认外侧右上方悬停显示，保留稳定空工具行，按钮默认隐藏、移入显示、移出隐藏，键盘焦点可见；单击图片仍选中，双击打开。见[退出设计](workspace_interaction.md#r032-esc退出与原操作恢复)和[媒体设计](reading_media_viewer.md)。

从 `30755cd` 独立构建，`npm run build`、完整 `npm run check` 通过。完整UI首次执行50个目标通过；四处旧夹具只发送Esc按下而没有释放，补全后通过。另两处Git夹具在上一交付版也失败：视图菜单多期待一个已移除的入口，同步子进程未将status映射到正式运行器的code契约；修正夹具后均通过。生产Git逻辑未改。最终56个UI目标全部完成复查，日志为 `.cache/escape_focus_release_20260913/escape_full_ui.log`、`escape_corrected_ui.log`、`escape_full_check.log`，旧版对照在 `.cache/media_entry_release_20260913/escape_baseline_ui.log`。

新增Esc专项21项使用真实Chromium键盘，覆盖反向输入选区、contenteditable、Shadow DOM输入、真实Monaco与xterm、父子菜单／对话框、重复按键、组合输入、外部失焦和文档更换；退出后立即输入验证无需再点击。核心烟雾测试验证两个实际构建包共享退出所有权。媒体专项44项覆盖悬停移入移出和跨间隙移动、不移动排版、鼠标与键盘返回、原生单击／双击、明暗、宽窄、缩放和生命周期。

隔离原生Typora1.14.10通过29项，证据 `.cache/native_zoom_1_14_10/escape_focus_native_round3_20260913/checks.json`。实际Markdown选区在查看器／文件搜索／核心输入框Esc后恢复；紧接着输入替换选中文字，宿主撤销还原正文。图片、Mermaid、Night截图逐张检查，入口与图像保持外侧间隔、原图属性不变。早期夹具的文本节点选择及离屏入口激活已修正，失败记录保留；原生验证使用私有桌面的宿主API／DOM，真实鼠标键盘由Electron测试覆盖，未据此宣称其他平台实机验收。

最终构建已事务安装，安装后检查 `status: OK`，24项安装资产逐项核对；ASAR、应用图标、原生偏好、工作区设置和主题摘要不变，保留安装备份，用户窗口未重启。日志为 `.cache/escape_focus_install_20260913.log`、`escape_focus_install_check_20260913.log`、`escape_focus_install_verification_20260913.json`。保存文档后正常重启即可载入。原18项R009系统文件剪贴板差量保持独立，未带入此次构建与安装；其他未完成项维持原台账。


## 2026-09-13 弹窗外部关闭与媒体入口取消占位

R033：文件搜索、核心命令输入／选择器、普通对话框和右键菜单接入共同关闭所有者。外部点击、宿主mousedown及焦点离开走取消；内部空白和子菜单维持打开。兼容鼠标和焦点事件共享一次手势，不连带关闭父层。Esc继续逐层恢复原焦点与选区；外部点击保留新目标的默认操作。普通设置不因打开系统颜色选择器而取消。设计及固定VS Code源码见[关闭与焦点](workspace_interaction.md#r033-弹窗外部关闭与焦点转移)。

R031：用户再次指出预留一行会拉大正文与图片距离。本次删除共享占位和图片包装，图片／Mermaid按钮只在正文外的固定层悬停显示，位置取自原图矩形；不增加margin、padding或行高。浮层命中桥仅限按钮，不挡住同一行的其他正文。原生Mermaid的横向滚动壳带overflow-y:hidden，旧裁剪算法误将外侧入口隐藏；现在分别判断来源可见性和阅读视口范围，无需重加空白行。图片单击选中、双击放大保持。见[媒体设计](reading_media_viewer.md)。此前“保留稳定工具行”的交付记录属于旧实现，本节及权威设计取代该约定。

独立发布基于 `c91ded7`，未带入原18项R009差量。最终 `npm run build`、完整 `npm run check` 通过，证据为 `.cache/outside_dismiss_release_20260913/.cache/build_outside.log` 和 `check_outside.log`。完整UI共57个目标全部通过（`ui_outside_full.log`）；后续对按钮命中宽度、核心菜单族焦点及原生Mermaid裁剪修正，分别复跑受影响媒体／核心和媒体／Mermaid目标全部通过（`ui_outside_final.log`、`ui_outside_clip.log`）。完整检查再次基于最终构建通过。

新增关闭专项19项先复现旧行为，再通过真实Chromium外部点击及键盘验证；显式FocusEvent与宿主mousedown兼容路径单独标注，隐藏Electron不提供操作系统焦点事件，不能混称物理焦点验收。Esc专项21项保留Monaco、xterm与原生风格输入连续编辑。最终媒体专项46项覆盖挂载与悬停前后正文DOM／图片／段落矩形不变、宽窄／明暗／缩放、命中桥、无效资源、Shadow DOM、滚动裁剪、单击／双击及卸载。发布校验将被删除的旧工具行marker替换为真实共享浮层marker。

隔离原生Typora1.14.10最终31项通过，证据 `.cache/native_zoom_1_14_10/outside_dismiss_native_verified_20260913/checks.json`。图片和Mermaid没有正文占位／包装，悬停前后几何及DOM一致，实际Night主题仍可读；按钮、图片双击、Esc和直接编辑／撤销正常。文件搜索及核心命令框内部点击保持、外部点击取消，实际核心菜单外部点击关闭，Markdown与全部原始夹具文件摘要保持。图片常态／悬停、Mermaid和Night截图已核对。早期原生失败定位到上述Mermaid裁剪问题并保留证据；此次原生验证使用私有桌面中的宿主API／DOM激活，Chromium指针与键盘证据另记，不宣称原生物理鼠标或其他平台验收。

最终构建已事务安装，安装后检查 `status: OK`。ASAR、应用图标、原生偏好、工作区设置和主题摘要不变，保留事务备份，用户窗口未重启；保存后正常重启Typora即可载入。安装证据为 `.cache/outside_dismiss_install_20260913.log`、`outside_dismiss_install_check_20260913.log`、`outside_dismiss_install_verification_20260913.json`。R009与其他未完项继续保持原状态，本轮只交付关闭与媒体占位修复。

## 2026-09-13 文件标签与编辑器顶部对齐

R034按用户提供的VS Code截图，采用固定1.137.0的Modern标签规则：32px连续命中行、24px圆角底色、13px标题；完整文件名由可用宽度裁剪，关闭动作悬停覆盖且不引发标题或相邻标签移动。Markdown阅读预览按真实模式显示官方图标与“预览”前缀；未保存圆点、固定和关闭仍共用原文件状态和命令。源码与diff显示22px路径栏，diff工具按钮位于所属编辑组的标签行右侧。来源及职责见[文件顶部设计](editor_header.md)。

最终构建和完整check通过。57个UI目标已复查：完整首轮54项通过，两个源码测试仍要求隐藏路径栏、一个标签夹具缺少工作区对象；按新设计更新夹具与断言后，四个受影响目标全部通过。生产core覆盖完整Unicode／特殊字符名称、动态标签、关闭SVG子节点、键盘激活、窄组横向滚动、明暗状态与拆卸恢复；真实Chromium指针验证悬停时标题矩形不变。回归日志为独立发布中的 `.cache_editor_header_ui.log`、`.cache_editor_header_ui_retry.log` 和 `.cache_editor_header_check_final.log`。

隔离原生Typora1.14.10最终20项通过，验证单／多组、阅读预览、未保存与取消关闭、保存、固定／取消固定、diff模式切换保持模型、明暗顶部颜色及125%窗口缩放；六张最终截图逐张核对，夹具正文和源码最终磁盘字节保持。原生为私有桌面宿主API／DOM操作，不冒充人工鼠标验收；175%系统DPI的22.000002px浮点布局按22px CSS值验收。证据为 `.cache/native_zoom_1_14_10/editor_header_native_verified_20260913/checks.json`。

已使用经过验证的独立发布执行事务安装，安装检查OK，ASAR、图标、原生偏好、工作区设置和主题摘要不变，保留安装备份，用户窗口未重启；保存后正常重启即可加载。原18项R009未提交差量独立保护，未混入安装，系统文件剪贴板及其他较大能力的剩余验收不由本轮关闭。最终逐文件合并与提交证据位于 `.cache/editor_header_audit_20260913/`。


## 2026-09-13 SCM分隔条覆盖显示

R035：更改区与提交图区原先被一个7px网格轨道隔开，分隔条自身还带常态底色。现删除该轨道，让两个面板直接相接；同一分隔条绝对定位在实际第二轨道起点，4px命中区域两侧各覆盖2px，随网格最小尺寸和比例变化。悬停300ms后显示强调色，拖动／键盘焦点即时显示，交互结束清除反馈。折叠、显隐、方向键、Home及比例持久化仍由原业务处理；没有添加布局占位、计时器或观察器。固定参考与职责见[覆盖分隔条设计](workspace_interaction.md#r035-更改与提交图覆盖式分隔条)。

本次布局回归先复现7px间距，再验证无间距、真实鼠标悬停不重排、即时拖动反馈及松开离开后无残留；另覆盖极端比例与90px区域下的22px最小提交图标题、150%／200%缩放、隐藏任一面板及折叠恢复。独立发布build与完整check通过，SCM侧栏／历史／几何、终端、主侧栏分隔条、公共交互共6个UI目标全通过。

隔离原生Typora1.14.10通过18项检查，300／500px侧栏的15%／55%／85%比例、透明常态、键盘焦点、折叠／显隐、Night与125%缩放保持真实交界；4张实际截图已核对，全部夹具文件和Markdown正文不变。原生验收使用私有桌面DOM／API和实际渲染，真实Chromium指针回归独立完成，不称作原生物理鼠标操作。安装与提交摘要核对、原18项R009差量保护继续记录在本次本地需求台账。

## 2026-09-13 标题函数导航与详情分支全名

R034追加实现可交互的目录、文件和标题／函数面包屑，替代仅静态路径的范围。各编辑组独立显示22px导航，文件目录按需读取，符号树可折叠、过滤和排序；选择后定位到实际标题或模型中的函数名称。Markdown原位、分栏阅读、源码及差异两侧分别使用所属内容。大纲与导航共用模型版本和解析环境，C/C++复用clangd；配置集中解析用户、工作区及允许的语言覆盖。Esc恢复焦点与选区，外部点击保留目标默认操作。设计与固定来源见[标题函数导航](editor_header.md#r034-标题与函数面包屑)。

R022按用户补充的两张截图区分显示：提交列表长分支用省略号，详情卡直接显示完整名称。共享徽章在详情中取消100px名称限宽；极长全名换行后自动撑高，图标和文字均不裁切。没有另建Git状态或悬停计时器，见[完整名称设计](workspace_interaction.md#r022-长分支徽章完整名称)。

独立发布基于 `4b00f6c`，原18项R009差量未混入构建。最终build及完整check通过，日志为 `.cache/breadcrumbs_verified_build_20260913.log` 与 `breadcrumbs_verified_check_20260913.log`。完整UI首轮58个目标中57个通过，顶栏菜单夹具因新增设置的传递CSS依赖缺少text loader失败；更新该夹具后目标54项通过，并加入菜单开关读写覆盖验证。58个目标均已完成验证，首轮与重跑分别记录在 `breadcrumbs_ui_20260913.log` 和 `breadcrumbs_final_ui_20260913.log`，不将首轮写成全部通过。

最终面包屑专项35项通过，含原生式Markdown选区恢复、源码函数与差异两侧、非活跃原生组标题归属、配置覆盖／回退、Unicode／CRLF、异步失效和销毁。真实Chromium指针验证明暗及100%／125%缩放下的悬停，选择项文字对比度不低于4.5；源码大纲17项、提交布局16组场景和共同交互102项通过，详情徽章验证可见文本与实际字形矩形，覆盖短名、完整分支及极长换行。最终专项日志为 `breadcrumbs_ownership_ui_20260913.log`；相关截图和断言证据路径由日志记录。

隔离原生Typora1.14.10最终22项通过，证据为 `.cache/native_zoom_1_14_10/breadcrumbs_native_verified_20260913/checks.json`，覆盖原位标题、分栏、Esc后直接编辑、源码／差异双方、实际clangd的C函数、明暗与125%缩放。截图复查发现并修复Night选择项背景及非活跃原生组标题链丢失，最终分栏与clangd截图再次核对；夹具磁盘字节和Markdown内存正文不变。原生为私有桌面DOM／宿主API与实际渲染，真实指针回归单独记录，不称为用户窗口人工操作或其他平台验收。

另用独立真实Git仓库在原生Typora中验证R022，13项通过：明暗与100%／125%缩放下，列表使用省略号，详情卡中的完整分支名及换行字形全部位于徽章内；普通长名超过100px，极长名撑高。最终明暗代表截图已核对，正文磁盘与内存保持不变。证据为 `.cache/native_zoom_1_14_10/breadcrumbs_refs_native_20260913/checks.json`；该临时仓库仅用本地提交，不访问远端。

最终构建已通过现有事务安装器部署，安装后检查 `status: OK`，24项常驻安装资产与验证发布清单核对；ASAR、应用图标、原生偏好、工作区设置和主题摘要保持，保留事务备份。用户窗口未重启，保存后正常重启Typora即可加载。安装证据为 `.cache/breadcrumbs_install_20260913.log`、`breadcrumbs_install_check_20260913.log` 和 `breadcrumbs_install_verification_20260913.json`。最终合并、提交与保护审计在 `.cache/breadcrumbs_audit_20260913/`；原18项R009差量及其他未完成项继续保持独立。


## 2026-09-13 文件夹单击与连续响应纠正

R028按用户最新纠正取消文件夹双击重命名。箭头、名称和行内空白的每次单击立即切换，包括浏览器双击序列中的第二击；删除旧的目录双击选择快照和展开状态恢复。文件夹改名继续使用 F2 或右键，已选中文件双击改名保持。慢读重开仍复用同一请求，完成后遵守用户最后的折叠状态。固定 VS Code 来源、交互与失败边界见[文件行设计](file_operations.md#r028-文件行的单击双击与加载反馈)。

隔离发布基于 `fa6f490`，未混入原18项R009差量。原代码在新增 Chromium 鼠标回归中复现目录双击进入改名；修复后build、完整check及5个相关UI目标通过，覆盖资源管理器、文件编辑、工作台启动、搜索和Esc。目录测试的11次快速点击均在下一渲染帧更新，当前夹具测得0.5～15.8ms；这是本轮夹具结果，不代表任意设备或目录的枚举耗时。慢读、2000项虚拟列表、未选中／已选中目录、文件双击、F2／菜单、明暗／窄栏及缩放均通过。验证观察器修正事件顺序后通过，首轮与最终日志分别保留于独立发布 `enhancements/.cache_folder_click_before.log`、`.cache_folder_click_after.log`、`.cache_folder_click_verified.log`、`.cache_folder_click_related_ui.log` 和 `.cache_folder_click_check.log`。

隔离原生Typora1.14.10最终18项通过，明暗与100%／125%截图已核对，全部临时文件内容及路径保持。原生采用私有桌面DOM事件及实际命中区域，Chromium真实鼠标／键盘证据单独记录；测试行尾不覆盖已有侧栏拖动边界。前两次夹具等待／命中点问题已保留，最终证据为 `.cache/native_zoom_1_14_10/folder_click_native_verified_20260913/checks.json`，汇总在 `.cache/folder_click_audit_20260913/test_summary.json`。

事务安装与安装后检查通过，24项常驻资产与隔离发布摘要一致；ASAR、应用图标、当前原生偏好、工作区设置和主题摘要保持，用户窗口未重启，保存后正常重启Typora加载。首次安装因运行中的原生配置并发更新触发事务回滚，重新读取最新配置后安装成功；没有覆盖新的原生设置。安装证据为 `.cache/folder_click_install_20260913.log`、`folder_click_install_check_20260913.log` 与 `folder_click_install_verification_20260913.json`，最终提交及保护审计在 `.cache/folder_click_audit_20260913/`。原18项R009差量与其他未完成项继续独立保留。

## 2026-09-13 版权与来源声明

R036：按用户要求采用linux-note同样的版权声明结构和GPL-2.0-only许可。根LICENSE保存原许可证正文，COPYRIGHT.md保留原创署名、当前开源方式、官方贡献／非官方分叉、未来版本与第三方边界；署名FormingSystem及联系邮箱沿用参考声明，产品名及项目地址对应Typora Code。根README、增强模块说明和包／锁文件根条目指向同一声明。按用户追加要求明确标注布局、交互方式及部分功能设计参考并模仿VS Code，链接已有设计基线，并说明非Typora或VS Code官方产品。

本轮核对许可证与参考文件逐字节一致，82个本地Markdown链接目标存在，包元数据除根许可字段外不变，第三方包条目及第三方许可文件不变；独立只读复核通过。参考源三个文件无未提交修改，固定提交及设计见[版权声明设计](copyright_notice.md)。证据保存在`.cache/copyright_notice_20260913/`。

本轮仅声明文档与包许可元数据变更，未更改运行代码或部署资产，无需重建、UI回归、安装或重启；不以旧功能测试结果代替本轮核对。提交仅含本次差量，原18项R009未提交工作保留。


## 2026-09-13 Markdown标签恢复普通文件图标

R034：按用户对Typora文档标签的纠正，Markdown活动、后台和分栏标签统一保留现有Seti文件图标及实际文件名，移除阅读视图专属的预览图标和“预览”前缀。标签层不再根据`isEditor()`改变文件身份呈现；原生编辑／阅读机制、临时斜体、固定、未保存与关闭行为继续由既有所有者管理。名称本身包含“预览”的文件保持原名，搜索及其他明确预览功能不在本次修改范围。来源与验收见[标签语义设计](editor_header.md#r034-markdown文件标签语义纠正)。

独立发布基于`40591df`，build与完整check通过；5个相关UI目标通过，其中标签33项、共享文件图标、编辑动作、diff状态和拖动均已复查。首次完整check在既有Save As测试遭遇临时文件写入占用，独立复跑和完整复跑通过，未修改该保存实现。新增标签用例第一次点击了另一组已选中的标签，核心提前返回；将夹具改为先选同组另一标签、再切换目标后通过。本次未修改或宣称解决既有跨组已选中标签再点击的焦点边界。首次日志与复跑日志均保留在独立发布`enhancements/.cache_md_tab_*.log`。

隔离原生Typora1.14.10最终15项通过：活动／后台切换、同文件分栏、文件名保留、明暗100%／125%缩放、图标真实可见和32px标签高度；夹具文件字节不变。首次RPC派发迟于隔离窗口退出，改为宿主就绪后自动执行；截图复查发现首次启动提示遮挡标签后，通过原生关闭按钮关闭提示并增加命中检查，最终截图已核对。原生证据为私有桌面DOM／API事件和实际渲染，不冒充用户窗口物理鼠标验收；结果在`.cache/native_zoom_1_14_10/md_tab_native_final_20260913/checks.json`，过程汇总在`.cache/md_tab_audit_20260913/test_summary.json`。

事务安装与安装后检查OK，安装资产与独立发布摘要一致；ASAR、应用图标、原生偏好、工作区设置及主题摘要保持，用户窗口未重启，保存后正常重启Typora加载。证据为`.cache/md_tab_install_20260913.log`、`md_tab_install_check_20260913.log`及`md_tab_install_verification_20260913.json`。提交只含本次任务差量，原18项R009未提交工作与其他未完成项继续独立保留。

## 2026-09-13 系统文件剪贴板阶段提交复核

R009本次复核此前保留的文件剪贴板差量，并修正三处并发边界：Windows写入在释放剪贴板锁前取得本次版本；Linux返回自身唯一内容的版本，不在写入后认领其他应用的新复制；旧焦点刷新失败只能取消自身剪切，不能清除后来建立的新剪切。Linux缺少原子版本核对与清空接口，因此移动完成只取消本地意图，保留系统列表，避免覆盖其他应用的新复制。UI、文件引擎和文档事务仍由现有所有者承担。

本次build、完整check和3个相关UI目标通过，分别记录于`.cache/commit_pending_build_20260913.log`、`commit_pending_check_20260913.log`、`commit_pending_ui_20260913.log`。文件引擎14组、服务13组、平台6组检查通过；新增旧刷新失败用例先复现问题，再验证新剪切仍执行受保护移动。平台回归使用纯内存系统端口，覆盖同路径外部覆盖后按复制处理及不清空新内容；UI覆盖生产启动、Explorer键盘／菜单、复制UTF-16磁盘字节、未保存草稿与移动后模型／撤销／保存路径。Windows PowerShell5.1嵌入C#只编译通过，日志为`.cache/commit_pending_clipboard_compile_20260913.log`。

真实Windows互通尚未验收：本次隔离启动器仍在创建独立窗口站时返回Win32 5，尚未进入剪贴板测试，也未读写用户剪贴板，见`.cache/commit_pending_native_clipboard_20260913.log`。Linux原生桌面互通也未验证。按用户“应入库的修改及时提交”的要求，本次将已核对的阶段实现、测试、设计和对应dist一并提交；提交不代表R009完成全部平台验收。本次不替换当前已安装版本，后续从独立窗口站和Linux桌面互通继续验收。

## 2026-09-13 每轮及时提交规则

R011按用户追加要求明确每轮完成后及时提交需要入库且已经验证的代码、测试、设计和构建，开始下一项工作前检查前项收尾；阶段实现可以附验收缺口入库，不能以提交代替需求完成或安装验收。该规则由AGENTS与需求维护流程共同约束，无关用户修改仍不纳入，推送继续需要独立授权。

本次先将R009阶段实现及对应构建提交为`08b39ad`，再单独提交本规则。纯文档修改通过差量、入口与约束一致性检查，无需重复构建或UI回归。

## 2026-09-13 选中行随明暗主题切换

R020：修复Night资源管理器选中目录出现浅底浅字。共同交互层补齐VS Code Light／Dark 2026非活动选中背景与前景，沿现有真实主题观察链切换；Explorer文字、箭头和焦点边框引用共同颜色。Graph／SCM现有选中行共用配色，公共hover识别既有选中按钮状态，避免SCM文件选中底色被普通action覆盖。行尾按钮保留独立hover，状态标记和选中模型未改动。来源与职责见[选中行明暗主题](workspace_interaction.md#r020-选中行的明暗主题)。

先用旧样式复现：Night文字对比度普通／键盘态1.09，hover1.47。移除测试手工注入的选中色后，真实主题观察链日→夜→日九种状态通过；Night文字和箭头对比11.79，浅色13.05，节点身份、26px行高、非零滚动和文档保持。Explorer、共享交互默认、面包屑、标签及实际Graph／SCM共5个UI目标通过；Graph／SCM目标126项覆盖明暗、100%／125%、移入移出、行尾按钮及状态色。证据为`.cache/explorer_selection_before_focus_20260913.log`、`explorer_selection_after_20260913.log`、`selection_theme_ui_20260913.log`和`interaction_ui_1789299077489/checks.json`。本次build与完整check通过；同类SCM追加修复重建后，核心、最终资产与部署检查再次通过。曾从仓库根误运行需要enhancements工作目录的资产检查，提示缺少dist；在正确目录重跑通过，未改变检查规则。

隔离原生Typora1.14.10最终20项通过，真实github.css→night.css→github.css切换保留同一目录、正文、编辑状态和26px行高；Night对比11.79，浅色13.16。三张实际截图已核对，主题边界的亚像素宽差0.57px、切回完全恢复。首次采样遇到私有启动器尚未完成窗口尺寸调整，延后至就绪后重新验证；最终证据在`.cache/native_zoom_1_14_10/explorer_night_native_20260913_final/`。这是私有桌面的宿主API／DOM与实际渲染验证，不冒充用户物理鼠标操作。

本次通过既有事务备份安装，安装后检查OK，23项产品资产中仅workspace.css及其SHA256SUMS清单变化。安装脚本基线仍为已验收`7885b59`，R009已提交但尚未验收的系统剪贴板脚本未部署；当前源码／dist继续保留该阶段实现。ASAR、图标、原生偏好、工作区设置和主题摘要不变，用户窗口未重启，保存文档后正常重启Typora加载。发布与安装记录在`.cache/selection_theme_audit_20260913/`，日志为`.cache/selection_theme_install_20260913.log`与`selection_theme_install_check_20260913.log`。按每轮及时提交约定，本轮代码、测试、设计及对应构建一并提交，其他未完成项继续保留。

## 2026-09-13 全屏查看悬停入口保持不透明

R031：按钮原有阅读底色被公共action的半透明hover背景覆盖，导致文字或图片透底。在唯一的reading_media_entry.css中声明局部公共hover背景与前景，引用宿主阅读背景和正文文字色，图片与Mermaid同时生效。显隐、外侧右上覆盖位置、命中桥和正文零占位不变；设计见[不透明入口](reading_media_viewer.md#r031-悬停入口的不透明底色)。

旧样式在真实Chromium鼠标移入按钮后复现亮色alpha0.122、暗色0.314。回归使用生产主题观察链，覆盖媒体移入、按钮移入和离开；修复后图片／Mermaid、亮／暗四组显示时opacity与背景alpha均为1，文字与图标对比可读，正文DOM、段落／图片矩形和按钮身份不变。媒体查看器47项与公共交互56项通过，保留单击选中、双击及按钮打开、缩放拖动和Esc退出验证；build与完整check通过。前后日志为`.cache/media_entry_opaque_before_fullpage_20260913.log`、`media_entry_opaque_after_20260913.log`，公共回归、构建与检查日志为`.cache/media_opaque_shared_ui_20260913.log`、`media_opaque_build_20260913.log`、`media_opaque_check_20260913.log`。

隔离原生Typora1.14.10最终23项通过，github.css与night.css下图片／Mermaid四组均真实触发CSS :hover；显示完全不透明，离开隐藏且不拦截正文指针，无占位行和排版变化，正文、dirty状态及夹具文件保持。四张实际截图已视检，证据在`.cache/native_zoom_1_14_10/media_hover_native_20260913_final/`。首次远程调试参数被原生生产限制拒绝后，改用私有桌面窗口WM_MOUSEMOVE完成验证，未绕过限制或修改ASAR；此为私有窗口消息和实际渲染验证，不冒充用户物理鼠标操作。

既有事务安装及安装后检查OK，23项产品资产中仅workspace.css及其SHA256SUMS清单更新，包含上一轮明暗选中配色。已安装脚本仍为已验收7885b59，未部署R009待原生互通验收的阶段脚本。ASAR、图标、原生偏好、工作区设置和主题摘要保持，用户窗口未重启；保存后正常重启Typora加载。安装日志为`.cache/media_opaque_install_20260913.log`与`media_opaque_install_check_20260913.log`，发布、保护与提交核对保存在`.cache/media_opaque_audit_20260913/`。本轮代码、测试、设计和对应构建及时提交，其他未完成项保持原状态。

## 2026-09-13 底栏窗口缩放入口

R014新增右下角窗口缩放入口，使用固定官方Codicons的zoom-in／zoom-out图标；非零级显示，恢复100%隐藏。悬停或点击打开紧凑浮层，依次提供缩小、实际级别、放大、重置及原生设置入口。比例只读webFrame，操作复用已有命令，快捷键／菜单外改经原生提示节点、resize和focus同步；设置打开原生偏好，未新增比例配置。布局、明暗、hover与退出由公共层负责，来源及宿主适配见[窗口缩放设计](workspace_zoom.md#r014-底栏缩放入口)。

目标回归最终67项通过，保留原32项快捷键，并覆盖真实webFrame、xterm缓冲／草稿／正文、能力缺失、原生外改、小数级别、Esc／外点和卸载重绑。明暗×22／30px×125%／83%的8组矩形、图标及对比度检查与截图通过。初始几何断言将22px实际21.99375误作失败，按Chromium亚像素精度改为0.05px容差；键盘夹具补齐Enter的char事件。只读复核另发现焦点恢复会重开浮层、等待显示期间Esc不取消计时；新增3组650ms等待先复现旧代码，再由共同hover的恢复焦点保护及pending取消修复，指针或焦点仍在入口时也保持关闭。证据为`.cache/zoom_status_escape_before_20260913.log`、`zoom_status_escape_verified_20260913.log`，目标脚本输出包含escape_timing.json和截图位置。

共享交互、阅读链接悬停、公共默认交互3个UI目标，以及启动／底栏2目标通过；最终build和完整check通过，日志分别为`.cache/zoom_status_hover_regression_20260913.log`、`zoom_status_footer_startup_20260913.log`、`zoom_status_build_20260913.log`、`zoom_status_check_20260913.log`。共享Esc追加修复后的3目标结果随`zoom_status_escape_verified_20260913.log`记录。安装候选使用既有脚本基线加本轮源文件，依赖junction最初导致候选CSS扫描读到实际路径，候选构建启用preserveSymlinks后通过；生产构建规则未修改，未变化资产保留已安装字节。

原始Typora1.14.10私有实例最终70项通过，实际缩放、正负入口、重置、外部原生命令同步、明暗×22／30px及120%／144%定位与命中、同一面板保持、Esc／外点、设置入口、原生提示恢复和正文／磁盘保持均通过。初轮外点误选隐藏的原生输入框，改为可见正文继续验收；补测初次聚焦又被宿主选区收尾移走，在宿主稳定后明确激活入口完成复核。截图已视检；证据在`.cache/native_zoom_1_14_10/zoom_status_native_20260913_c_final/`。指针通过私有桌面Win32窗口消息，Esc通过renderer KeyboardEvent，补充键盘入口路径使用focus与button.click模拟，不冒充用户物理操作；原始ASAR未修改，用户窗口未操作。

既有事务安装及安装后检查OK，23项产品资产只更新workbench.js、workspace.css及清单。安装脚本为已验收7885b59加本轮R014及共享hover修复，保留前两轮明暗选中色与不透明媒体入口；源码HEAD的R009系统文件剪贴板阶段实现仍待原生互通验收，未部署。ASAR、应用图标、原生偏好、工作区设置及主题摘要保持，用户窗口未重启，保存文档后正常重启Typora加载。日志为`.cache/zoom_status_install_20260913.log`与`zoom_status_install_check_20260913.log`，资产和保护核对保存在`.cache/zoom_status_audit_20260913/`。本轮经检查的代码、图标、测试、设计及构建及时提交，不推送。

## 2026-09-13 更改标题提交、刷新与Graph入口

R027：更改标题只创建了分支与更多，补齐提交、刷新和打开完整Git Graph三个操作，顺序与用户截图一致；使用已分发的官方check、refresh和git-branch。提交与主按钮共用原有事务，刷新保留分页，Graph复用已有编辑标签入口。状态随控制器统一更新，读取/写入时禁用，仓库失败仍可刷新恢复。按钮不误触summary；复查发现折叠时空消息无法聚焦，现先显式展开并解除inert，再提示输入。来源、标题尺寸及Graph产品集成差异见[设计](git_scm_actions.md#2026-09-13-更改标题操作补齐)。

新增目标18组检查通过：临时仓库真实单次提交、消息与草稿保护、下拉预览取消、实际慢读/慢写防重复、错误恢复、Graph目标、折叠、Tab/Enter和销毁闭包。180/240/380px明暗六组验证22px标题及按钮、16px图标、不重叠与对比度。旧回归初次暴露精简panel夹具缺容器状态、已删除branch访问，以及把所有git-branch都当引用筛选按钮的判定；仅补齐夹具和语义定位。文件图标、Graph交互和SCM几何三个目标最终各自通过。日志为`.cache/scm_title_actions_20260913.log`、`scm_title_ui_regression_20260913.log`和最终`scm_title_geometry_regression_20260913.log`；完整check通过，见`scm_title_check_final_20260913.log`。

隔离原生Typora1.14.10最终80项通过，10次私有Win32指针点击经renderer观察为isTrusted；实际读取Git状态、忙碌禁用、折叠空消息聚焦、仅私有仓库的一次预期提交及重复打开Graph复用标签均通过。明暗×180/240/380px八张新截图及指标保存在`.cache/native_zoom_1_14_10/scm_heading_native_20260913_a/`，明暗180px截图已视检。宽度调节和Esc使用renderer KeyboardEvent，主题走原生接口；此为私有原生实例验证，不冒充用户物理操作。原始ASAR与正文/dirty状态、磁盘文档保持；测试实例已退出。

按既有事务安装并检查OK，23项产品资产只更新workbench.js、workspace.css及清单。候选基于已验收的底栏缩放版本叠加本轮三份源码，保留选中行明暗、媒体不透明和窗口缩放修复；R009已提交但未通过原生互通验收的阶段脚本继续不部署。保护检查确认ASAR、应用图标、原生偏好、工作区设置及主题摘要不变，用户窗口未重启；保存后正常重启Typora加载。安装日志为`.cache/scm_title_install_20260913.log`和`scm_title_install_check_20260913.log`，发布及安装核对见`.cache/scm_title_audit_20260913/`。本轮已验证代码、测试、设计与构建及时提交，不推送；其他未完成项保持原边界。

## 2026-09-14 Windows uninstall 双击入口

R025：新增 uninstall_windows.cmd / uninstall_windows.ps1，自动筛选当前用户的安装前完整备份，排除更新备份；多个候选才选择，空输入取消，非交互遇歧义失败。复用原恢复事务，保留文档、阅读记录和用户偏好，目标 Typora 未退出时拒绝执行。首页、安装指南、部署清单及增强说明同步；增强说明的旧 scripts/install_windows.ps1、scripts/restore_windows.ps1 路径已修正。Windows PowerShell 5.1 隔离 CMD 与恢复38项断言、部署检查均通过，证据和边界见[卸载入口设计](installation_design.md#r025-卸载入口)。未执行真实用户安装卸载。

## 2026-09-14 顶栏右键显示配置

H001.1：顶端功能区右键接管原生根菜单，使用公共菜单提供菜单栏、命令中心和导航控件的勾选开关；点击即保存到已有用户设置。菜单栏收进更多入口，命令中心隐藏时显示窗口标题并保留导航偏好。固定上游、组件范围及配置所有权见[设计](workspace_interaction.md#h0011-顶栏右键显示配置)。

`npm run check:ui -- test_workspace_titlebar.cjs test_workspace_titlebar_entries.cjs` 两项目标通过。隔离 Electron 覆盖实际右键输入、一次点击一次持久化、重新加载设置、保存失败、左键菜单、Esc与外点焦点、键盘入口、明暗主题及窄窗缩放；四张菜单截图和几何记录已生成，浅色与深色代表截图已视检。卸载后设置监听及菜单闭包不再执行。测试证据索引为 `.cache/titlebar_context_side_20260914/result.json`。

扩大检查时，`test_workspace_startup.cjs` 在打开源码文件后等待编辑器模型超时；同一测试仅在内存中换回 HEAD 顶栏实现也在相同步骤失败，尚未定位该集成问题。此项不据此宣称整体启动通过。本侧会话只修改顶栏源码、专项测试和设计记录，没有重建共享发布包、安装或重启用户 Typora，也未改动 Git 提交状态。

## 2026-09-14 安装过程日志

根据用户“install 看不出在做什么”的反馈，安装入口统一显示带时间戳的阶段、实际耗时和结果；Windows 的私有 Node 缓存、下载、校验与解压分别反馈，成功后显示 Backup、Log 和正常重启提示。每次安装独立保存 UTF-8 日志，日志目录或文件不可写时降级输出；预检拒绝、安装回滚完成与回滚未完成使用明确的不同结果。设计与用户说明见[安装日志](installation_design.md#r025-安装过程日志)和[查看安装进度与日志](installation.md#查看安装进度与日志)。

Windows PowerShell 5.1 安装／恢复事务和日志专项通过；Python 事务及日志回归在 Windows 临时目录通过，覆盖真实文件复制、故障回滚、阶段顺序、UTF-8 和函数输出隔离。下载使用本地微型 ZIP 替身，不访问网络；原安装事务使用私有复制的已验证官方 Node 缓存。日志独占锁、保存失败和回滚自身失败没有产生假成功。新增下载夹具首次遇到 .NET 归档路径分隔符和程序集加载问题，修正夹具后通过；Python 验证失败注入调整为只作用于安装目标，确认实际进入回滚路径。部署检查通过20个部署文件、23个工作台资产，证据在 `.cache/install_logging_side_20260914/`。

此次仅更新安装脚本、专项验证和说明，未安装或重启用户 Typora，未构建共享发布包、未修改 Git 提交状态。Linux/UCRT64 Bash 和 ARM64 原生入口未执行，不将 Windows 上的 Python 文件事务等同于原生平台验收。

## 2026-09-14 Git操作、文件功能栏与顶栏终端统一交付

本轮把 R027 的放弃更改、普通远端快捷操作和分支范围选择，与 R034 的比较页／普通文件顶端功能栏一起完成；后续明确要求的 R006.7 终端输入与 H001.1 顶栏右键配置也纳入同一安装候选。布局、图标、命令条件与配置语义参考固定 VS Code 来源，Typora 原生文件与编辑模型继续由现有服务管理；不声称支持整个 VS Code 扩展宿主。

获取、普通拉取／推送／同步复用同一命令入口，读取实际 remote、upstream 和 push 配置；唯一目标直接执行，多目标才选择一次，成功及失败均更新状态。历史引用采用顶端多选草稿，确认应用、取消保持原范围。放弃更改保留准确对象确认：区分已跟踪恢复和未跟踪回收，确认后一次执行、取消零写入。切库、迟到结果、重复点击及最终写入前草稿检查有独立回归。

比较页接通真实 Monaco 差异设置、导航、F7 与所选范围暂存／还原；普通 Markdown／源码功能栏位于所属组标签行，拆分、更多及可用 Git 文件动作复用文档与事务服务。顶栏右键直接切换菜单栏、命令中心、导航控件，保存成功后更新。终端容器隔离按键／组合输入冒泡，保留 xterm 原生输入处理。模态外点关闭吞掉该次完整指针手势并恢复原操作，普通菜单仍让外点目标接收操作。Night 原生主题强制的正文 hr 边距曾污染更多菜单；共享菜单现在明确重置分隔线盒与局部边距，正文分隔线保持原样。

完整业务检查通过。首次冻结界面回归 62 个目标中 60 个通过、2 个夹具缺失契约，修正夹具后分别通过；最终受影响的 22 个目标中 21 个直接通过，核心冒烟旧的“不恢复焦点”断言更新为本轮约定后在完整及安装候选均通过。最终菜单补丁的两个候选各通过共享控件 27 项、普通文件栏 36 项。首次失败及复跑日志均保留，不把分轮结果当成一次全量通过；证据在 `.cache/git_integrated_audit_20260914`、`.cache/modal_gesture_20260914` 和 `.cache/menu_separator_20260914`。

隔离原生 Typora 1.14.10 的综合 case f 共 220 项通过，核对新入口、明暗引用选择和放弃窗口、取消／外点／迟到焦点、两笔实际恢复事务及两个原生回收文件。回收站身份与内容 SHA256 一致，HEAD、暂存对象、未选文件及原 Markdown／dirty／磁盘保持。最终仅共享菜单 CSS 修正后，case g 的 21 项明暗视觉补测通过，两张实际菜单截图已视检；工作台与核心脚本和 case f 相同。原生证据分别为 `.cache/native_zoom_1_14_10/scm_integrated_native_20260914_f` 与 `scm_integrated_native_20260914_g`。右键、按钮及外点使用私有桌面的 Win32 输入；Esc 和原生终端边界采用 renderer 事件。Chromium xterm 输入链有独立回归，但物理搜狗版本与 Shift 切换尚未人工验证。

已按现有事务安装与检查入口安装通过验证的候选，安装后检查 OK，23 项资产与候选摘要一致。本轮更新 workbench.js、workspace.css、workspace_core.js 及清单；核心包同步更新共享退出服务。原 ASAR、应用图标、原生偏好、用户工作区设置和主题五项摘要不变。既有备份路径见 `.cache/git_integrated_install_20260914.log`，逐项安装证据为 `.cache/git_integrated_audit_20260914/install_verification.json`。用户窗口未关闭或重启，保存文档后正常重启 Typora 加载。本地源码及完整 dist 保留此前已提交的 R009 阶段代码，其原生互通验收仍开放，因此安装候选继续排除该阶段；未夹带其他会话尚未验收的资源管理器、自动保存／历史和安装日志改动。本次未推送远端。


## 2026-09-14 全部并行修改的集成基线

用户随后要求全部改动统一验证与安装，并在继续新增功能前先提交当前修改。本次交付范围扩大到当前 Git／编辑器／终端／顶栏、系统文件剪贴板、资源管理器分区、自动保存与本地历史，以及安装过程日志。上述早期部分候选的安装记录保留为历史事实；此前排除 R009、R037/R038 和安装日志的候选范围不再适用于最终交付。

全部源码构建与业务 check 通过。64 个 UI 目标的首次全量运行通过 62 个；启动测试的旧根标题菜单断言、历史测试与并行编辑夹具的交叠分别修正，冻结后两目标复跑通过。自动保存9组、原生保存适配7组、历史存储7组、文档事务16组和历史界面33组专项通过。Windows 安装／回滚、卸载38项、UTF-8阶段日志及 Python 安装测试通过。完整日志分别保留在 `.cache/all_changes_*_20260914.log` 和 `.cache/history_independent_final_20260914.log`；不把分轮复测写成一次全量全部通过。

完整构建在独立用户目录及私有桌面的原始 Typora 1.14.10 完成73项保存／历史检查，覆盖默认关闭、实际原生保存成功事件、延迟保存、两级版本选择、取消、恢复前保留未保存草稿，以及恢复后原生正文重载。实际 Git HEAD、暂存对象及其他夹具文件保持不变。证据为 `.cache/native_zoom_1_14_10/explorer_history_native_20260914_c`，包含构建摘要和6张截图；输入方式为 Win32 按钮与原生 CodeMirror 修改、renderer Esc。前两次夹具分别漏开隐藏 Explorer、过早断言原生重载正文，修正等待条件后通过，没有因此更改产品实现。

本轮系统文件剪贴板服务与平台测试通过；重新创建隔离 Windows 窗口站时返回 Win32 5，尚未重复完成当前构建的原生剪贴板互通。之前7项物理互通证据缺少源码摘要，不能替代本次结果。当前按用户明确的全部安装范围纳入该功能，同时保留此验收缺口；不触碰用户当前剪贴板。物理搜狗 Shift 和非 Windows 平台仍没有新实机证据。

当前先将已审查源码、测试、设计和完整构建提交为集成基线。全部候选的最终本机安装及用户新要求的 Git 动态执行反馈继续跟进；本记录不将旧的部分安装视为全部已安装，不推送远端。

## 2026-09-14 全量安装与Git动态进度收尾

先以本地提交 `ccf0f24` 保存上一批116个已审查文件，再单独加入 R027 动态进度和 R038 历史比较宽度修复。各Git写入口共用控制器的操作生命周期，Changes／Graph标题与中央工具栏显示覆盖式2px进度，底栏和仓库行同步更新忙碌提示。普通快捷命令仍一次点击执行，等待目标选择、失败、取消及切换仓库均清理所属活动；旧异步任务不能结束新操作。提交刷新期间输入的下一次说明保留。布局与配置依据固定VS Code源码，设计及采用值见[动态进度](git_scm_actions.md#2026-09-14-git快捷操作动态进度)。

本次业务 `check` 通过。64个UI目标首次通过60个，4个失败均为布局夹具缺少新进度模型；夹具接入真实模型后4个全部通过，没有放宽断言或给产品增加空实现。快捷操作最终9项、相关5个UI目标及历史界面34项通过。最后源码重新构建并通过安装包校验。证据为 `.cache/git_progress_final_check_20260914.log`、`git_progress_final_ui_20260914.log`、`git_progress_layout_retry_20260914.log`、`git_progress_commit_ui_20260914.log`、`git_progress_related_ui_20260914.log`、`git_progress_delivery_build_20260914.log` 和 `git_progress_delivery_deployment_20260914.log`；这些文件均位于 `.cache/`，首次失败与复跑分别保留。

最终同一构建在独立用户目录、私有桌面的原始Typora 1.14.10通过综合241项和保存／历史74项。包含明暗菜单、快捷操作、Git状态、真实临时bare远端fetch、取消／恢复／回收、文件顶栏与终端，以及原生保存、草稿保护、历史恢复和比较视图占满编辑组。证据为 `.cache/native_zoom_1_14_10/git_progress_native_20260914_b` 与 `history_geometry_native_20260914_a`，每组包含源码资产摘要，原生截图已视检。Windows当前报告减少动画偏好，原生进度按该偏好显示静态执行条；明确关闭减少动画的Electron检查验证滑块位置随时间变化和同步图标动画，不能把原生静态截图当成动画运动证据。私有桌面的Win32指针、原生保存API与renderer按键事件分别记录，不声称物理键盘或输入法全验收。

已通过现有安装事务完成全部候选安装，包含系统文件剪贴板、资源管理器分区、自动保存／历史和安装日志；早期部分候选的排除范围到此结束。安装后 `check_windows.ps1` 返回 `status: OK`，23项资产及清单与最终候选一致，原ASAR、应用图标、原生偏好、工作区设置及主题五项摘要不变。workbench.js摘要为 `881874241e71be3ff0dac8d9a0697d3cfb222d3679c1f8706be7aa608bf3b01f`，workspace.css为 `5c840d005fb4d18661ca775c648e94622fc4538b07de2e56c73c1b5c026a7163`。证据为 `.cache/all_changes_install_verification_20260914.json`，备份与阶段日志路径由 `.cache/all_changes_install_20260914.log` 记录。用户现有窗口未重启；保存文档后正常重启Typora加载本次版本。本轮仅本地提交，未推送。

R009当前构建的原生系统剪贴板互通仍受隔离窗口站Win32 5限制，物理搜狗Shift与非Windows平台仍无新实机证据；已安装不等于这些边界通过。其余本轮实现、自动化与原生候选证据已归档，后续开发从本次提交继续隔离。


## 2026-09-14 Shift候选提交与侧栏标题和移动进度

本轮从`61acabb`干净基线修复三项再次反馈。R006.7按用户澄清定位为Shift切英文时丢失待提交拼音：xterm 6记录了旧textarea起点，Windows输入法整值替换后从旧位置截掉提交文本。新增终端表面的公开DOM适配，只恢复明确提交文本对应的前缀，仍由xterm唯一发送；正常追加、空结果取消、连续组合及失焦销毁有独立回归，不修改输入法配置或第三方私有字段。该修复依据可复现事件形状独立实现，不能声称已合入VS Code上游。

R037三个资源管理器分区统一标题行、箭头、文字行盒、UI字体和工具栏显隐；原仓库名称节点与文件树更新保持。固定VS Code Modern采用值为28px行高、12px/600字体，实测三行箭头和文字相对侧栏分别为10px与28px。R027移除本产品将移动条改为整条静止线的减少动画覆盖；Changes、Graph及中央工具栏继续共用真实操作状态，2px滑块横向运动、不占行，长任务仍按固定上游规则节流。正式设计见[终端输入](terminal_operations.md#2026-09-14-shift候选拼音丢失复查)、[资源管理器分区](explorer_history.md#r037-资源管理器分区与菜单)和[移动进度](git_scm_actions.md#2026-09-14-横向移动进度条复核)。

验证：新输入回放、标题坐标和减少动画回归均先复现失败，再修正通过；完整`npm run build`及`npm run check`通过，9个相关UI目标一次全部通过。终端覆盖实际Chromium追加式组合和明确标注的Windows整值替换回放；含左右Shift、中文、相同文本、取消、两种DOM更新时序、同栈连续组合、失焦与卸载。Git覆盖明暗窄栏、两个动画偏好、三个入口实际transform变化和10秒后的持续运动。日志分别为`.cache/shift_headers_build_20260914.log`、`shift_headers_check_20260914.log`、`shift_headers_ui_20260914.log`，本轮没有宣称重跑全部65个UI目标。

最终原始Typora 1.14.10私有窗口77项通过，证据`.cache/native_zoom_1_14_10/shift_headers_final5_20260914`。分区点击、明暗布局和真实本地bare远端Fetch使用私有桌面Win32指针；系统减少动画开启时实际移动矩阵持续变化，完成后按300ms反馈收尾。终端在面板与编辑器间复用同一表面，renderer组合回放的实际onData为`中文`、`pin`、`pin`、`a`，原Markdown、dirty和Git暂存内容保持。此前原生夹具复跑修正了完成反馈等待、焦点协议数据计入文本和无视keydown取消而强发keypress的假设，期间候选未改动；没有把renderer回放称为搜狗物理按键验证。明暗标题及活动提示截图已视检。

已使用现有安装、备份和检查入口安装同一完整候选，检查`status: OK`，23项资产与候选一致，ASAR、应用图标、原生偏好、工作台设置和主题5项摘要保持。备份编号`20260914-095652-398-636dd991a6a24c608f9ad40223525b76`；逐项证据`.cache/shift_headers_install_verification_20260914.json`。本次workbench.js摘要`eb75c4d0a772534935615d16f102da6f5148c1e7daf396d3695c2c75e69017f2`，workspace.css摘要`ac8e3a2ccc66c143088cefcee76c1182ffa16abbd0d5011b99997729b29078cc`。用户窗口未重启，保存文档后正常重启加载。物理搜狗按键仍待用户实际输入确认；其他既有平台验收缺口未被本次缩进或进度修复关闭。本轮按用户明确授权提交并普通推送远端，不强推或改写历史。

## 2026-09-14 终端全局快捷键捕获归属

从已安装并推送的`1a427fa`继续核对用户关于Shift与搜狗快捷键的追问。未找到纯Shift被工作台取消的路径，也未收到重启后仍复现的确认；但新增回归证实window捕获早于终端冒泡隔离，中央Graph会抢走底部终端Ctrl+F/Escape，全局搜索会抢走终端Ctrl+Shift+F，若干全局入口遗漏229输入法标记。按[R006.7设计](terminal_operations.md#2026-09-14-全局捕获与终端焦点)修正这组独立缺陷，不把它们直接当作物理搜狗丢拼音的已证实根因。

共享`workspace_keyboard`只判定输入法事件和实际终端输入目标，不维护候选状态。终端、搜索、Graph、Explorer、面包屑、顶栏及工作区捕获入口复用IME/229判断；Graph按事件目标让行底部终端，终端查找不会再打开工作区搜索。快捷键抬键清理旧消费记录，但不取消输入法事件。普通全局命令保持，原有`terminal_composition`没有改动。

旧版Graph专项6项、捕获集成6项及抬键扩展2项先失败后通过。最终完整`npm run check`和12个相关UI目标一次全绿；新增集成目标39项、Graph输入归属13项均通过。日志`.cache/terminal_capture_check_20260914.log`与`terminal_capture_ui_20260914.log`。构建曾出现一次workspace.css写入UNKNOWN错误，保留失败日志后原命令重跑通过，没有绕过安装或文件校验。

原始Typora 1.14.10私有桌面采用同一最终观察器：旧版29/41、新版41/41通过，证据`.cache/native_zoom_1_14_10/terminal_capture_red3_20260914`与`terminal_capture_final2_20260914`。旧版12项失败覆盖终端、搜索、Graph、Explorer、SCM、面包屑及菜单捕获；新版Ctrl+F/Escape各到达一次xterm并生成正确控制字节，中文、pin、pin、a依次提交且焦点保持。纯Shift、keyup、取消及搬移正常，原Markdown、dirty和三套私有Git夹具的HEAD、暂存对象、差量与文件字节不变。首次新版观察器在xterm之后注册冒泡监听，错误地将合法消费后的零计数判为失败，已改为不拦截的捕获观察器并对旧、新两个候选重跑；未修改产品来适配夹具。截图已视检，输入采用renderer事件和实际xterm管线，不能替代搜狗物理键盘验证。

已通过现有安装事务交付，安装后检查`status: OK`，23项资产与本次原生候选一致，五项用户配置及宿主保护摘要保持。备份`20260914-104535-024-06619dd16f0c440ab04d966f89df8c4f`，证据`.cache/terminal_capture_install_verification_20260914.json`。workbench.js摘要`bc17f8a82deae889f615a9fe1fcae7526d692a6894c9179c0f90067787f3a8ba`；CSS未变。用户窗口未关闭或重启，保存后正常重启加载。源码、测试、设计与同一构建按用户授权一起提交并普通推送；物理搜狗及此前独立平台验收缺口继续保留。

## 2026-09-14 对照Markdown修复Shift直接提交

用户要求采用正常文档编辑的输入逻辑，本轮从`3d860be`干净基线读取原始Typora 1.14.10的正文Editor和CodeMirror输入链，确认浏览器拥有候选与确认文字，文档模型只处理实际输入结果。终端继续保持独立PTY协议，复用这一输入契约，不调用绑定正文节点的编辑、刷新或撤销函数。具体证据与范围见[R006.7设计](terminal_operations.md#2026-09-14-markdown输入契约与shift直接提交)。

真实Chromium左右Shift按下后直接`insertText(pin)`、抬键、后续`x`，没有composition事件，旧版textarea/contenteditable和终端textarea都有`pinx`，终端onData却只有`x`。本次修正xterm 6对纯Shift的错误已输入标记：现有输入适配器在父节点只隔离非组合纯Shift按下，不取消默认行为；input和keyup沿原路径完成，组合态、229、其他修饰组合及Shift＋字符／方向键照常进入xterm。没有私有字段补丁、自有候选缓冲或第二条PTY发送通道。

最终`npm run build`、完整`npm run check`通过；组合输入目标13组检查通过，另外捕获、终端面板、工作区快捷键、Escape焦点和主题5个UI目标全部通过。组合目标保留旧版左右Shift失败证据，新版逐一发送`pin`、`x`，并覆盖取消、普通追加／整值替换、连续提交、查找、修饰边界及销毁释放。日志为`.cache/terminal_md_input_build_20260914.log`、`terminal_md_input_check_20260914.log`、`terminal_md_input_ui_20260914.log`；正式组合测试证据目录名为`typora_terminal_composition_eptALp`（旧）和`typora_terminal_composition_AoqbfC`（新）。

原始Typora私有桌面同夹具旧版45/49、新版49/49：面板／编辑器和左右Shift四种组合均由旧版只收到`x`变为准确收到`pin`、`x`。普通textarea/contenteditable和真正原生Markdown同序列保持`pinx`；Markdown对照只编辑私有夹具草稿，再由原生撤销精确恢复原文与clean状态。三套私有Git夹具的HEAD、暂存对象、差量及文件字节保持。证据位于`.cache/native_zoom_1_14_10/terminal_md_input_red_20260914`和`terminal_md_input_final_20260914`，23项资产摘要一致，截图已视检。此处Shift为renderer回放，18次input由浏览器产生并记录为trusted；不等同物理搜狗按键已通过。

同一候选已通过现有事务安装，检查`status: OK`；23项资产一致，原ASAR、应用图标、原生偏好、工作区设置及主题五项摘要不变。备份`20260914-111429-262-b4babab7347944f1ba479b3632df10e6`，证据`.cache/terminal_md_input_install_verification_20260914.json`。workbench.js摘要为`7877b12c288e9800f9734242e1b64948d16e1307bea96b03310ef0919078abdd`，CSS未变。按已有授权提交与普通推送，用户窗口保持，保存后正常重启加载。搜狗物理输入和并发CDP提交与xterm0ms队列竞争的独立调查仍保留，不能把本次直接提交通过扩展为所有输入法时序已验收。

## 2026-09-14 Git可见刷新与共享快照

从`1015789`干净基线处理R027频繁刷新反馈。按用户最新要求，首次显示立即刷新，持续可见每30分钟检查，隐藏暂停；窗口聚焦、重复打开、同仓库切文件不额外全读。30分钟是本产品采用值，上游实际文件事件、防抖和差量规则见[刷新设计](git_scm_actions.md#2026-09-14-git刷新触发与无变化更新)。本轮没有增加自动fetch或完整文件系统监听，外部改动由定时检查或手动刷新发现。原生didSave、源码和可编辑diff只在保存完成后发布同一事件，保存合并，手动及Git命令完成及时更新；底栏和当前仓库行共用控制器快照。

无变化结果保留提交行、文件行、焦点和滚动，取消每8秒重读和重建。真实临时仓库旧探针同结果读取17条Git命令且三类行重建、文件失焦；新探针16条并保留全部节点及焦点。可变比较继续读取M→M内容，正常刷新不取消尚未完成的历史详情，替换读取器与切库使旧请求失效。最终复查修正了子目录正规化后其他仓库摘要被错误判过期，以及两个仓库HEAD／文件组相同时未应用各自树形和折叠布局的问题。

最终构建和完整`npm run check`通过，保存集成11组、调度假时钟8组通过。首轮16个UI目标15个通过，startup旧用例依赖已删除的底栏额外读取，误把合法Sync之后的刷新当作执行前等待；更新为共享读取期间的排队点击，严格保留切库零网络执行断言，并增加原库完成后仅执行一次的正例。最终5个受影响目标全部通过，覆盖Graph可见性／内容、快捷动作、SCM几何、底栏和完整启动释放。日志为`.cache/git_refresh_*_20260914.log`与`git_refresh_final_*_20260914.log`，首次失败及复跑分别保留；没有宣称重跑全部UI目标。

同一最终资产在原始Typora 1.14.10私有桌面23项通过，证据`.cache/native_zoom_1_14_10/git_refresh_native_final_20260914`。包含9秒空闲零读取、聚焦和重复打开零新增、可控时钟推进30分钟后仅一次读取、隐藏暂停与恢复补读、真实原生Markdown保存，以及两个真实本地bare远端fetch各只执行一次并刷新一次。明暗2px活动条实际transform随时间变化，截图已视检；保留私有正文、文件字节及dirty状态。30分钟边界使用时钟推进，未将此描述为持续30分钟人工观察；没有对用户真实仓库执行验收命令。

已通过既有安装事务交付并检查`status: OK`，23项资产与最终候选一致，五项宿主及用户配置保护摘要不变。备份`20260914-124144-578-6144163736bb40c784167b6cd403c0e9`，证据`.cache/git_refresh_install_verification_20260914.json`。workbench.js摘要`e92ca5dc73ba3d457b56877ced90c5d1df10d1cf62574afdfc731d779bded0ce`，CSS未变。源码、测试、设计与构建按用户授权一起提交并普通推送；用户窗口未重启，保存后正常重启加载。此前物理搜狗与独立平台验收缺口保持开放，不以本次Git交付关闭。

## 2026-09-19 资源管理器图标与内容层级

R046：文件树、打开编辑器和时间线统一工作台字体，分区标题与次要说明分层；辅助操作固定16px字形和独立命中区，保留已有悬停／焦点显隐、文件类型主题及命令。5个目标套件通过，原始Typora37项检查通过；300／220px、Github／Night四张截图已复核，标准build/check通过。已安装且check为OK，23项资产一致、宿主与配置5项保护摘要不变；用户窗口需保存后正常重启。

[需求与方案](explorer_history.md#r046-资源管理器图标与内容层级)、[本轮证据](../enhancements/tests/evidence/explorer_hierarchy_20260919.json)分别维护设计与结果。首轮字体失败和夹具取证问题保留，不用前轮110套件／压力结果代替本次验证；其他未完成需求保持原状态。开发skill同步更新并验证。

## 2026-09-19 可选ZIP原地更新

R047按用户澄清执行“立即安装，手动重启生效”：多窗口按宿主启动会话提醒一次，公告明确修复，稍后零安装；帮助菜单可重试。固定提交ZIP、摘要校验、独立worker与原安装事务共用，保留草稿、设置和备份。过程修复Windows5.1中文编码、下载失败误报成功及共享Node缓存并发覆盖，问题归档BUG-update-001／002／003。

9个目标套件、11次最终报告（含20／100／1000）与原始Typora40项检查通过，标准build/check及部署检查通过；共享缓存真实并发失败报告保留，修正后两个安装套件并行通过。本机安装check OK，27项资产一致、5项保护摘要不变，未重启用户窗口。开发skill和正式发布规则同步更新；设计、边界和可检索证据统一见[原地更新](workspace_update.md#本轮验证与交付)，不将网络替身当作线上发布成功。未推送，首次采用者需手动安装含更新模块的版本；既有未完成项保持。

## 2026-09-19 活动栏菜单勾选与留白复查

R044再次反馈，关联R018：前次验证图标几何时遗漏了勾选状态和完整文字起点。此次改为读取按钮visible状态的“勾选＋名称”菜单，隐藏／重开／恢复与既有持久化一致，方向键及Enter／Space单次执行；独立菜单隔离宿主8px伪元素、24px内边距与9pt行字号，采用26px勾选列、16px官方图形和24px行高。

6个目标套件、原始Typora46项及20次压力通过，明暗截图已视检，标准build/check通过。已安装2026.09.19.2且check OK，27资产一致；用户窗口未重启，未推送。需求、失败与证据见[设计复查](workspace_interaction.md#r044复现活动栏可见性勾选与菜单留白)和[机器可读证据](../enhancements/tests/evidence/menu_checks_20260919.json)。4项宿主／配置保护摘要保持，工作台设置的差异发生于安装前，保留当时状态；既有平台及跨机缺口不关闭。开发skill补充状态、父级字号、伪元素和文字起点验收，并通过校验。

## 2026-09-19 R048 分支检出快速选择器

用户指出底栏分支入口与VS Code不一致。本轮替换普通菜单，统一底栏/SCM仓库行/检出菜单；顶部可搜索本地、远端、标签，双行作者/短hash/主题，创建并切换、指定起点、分离HEAD；远端自动复用跟踪分支。沿用既有写锁、指纹、草稿保护与刷新，失败不强制覆盖文件。设计见[分支检出](git_branch_checkout.md)。

8个不同目标套件、10份所选报告通过（含20/100/1000压力档），真实Git服务10组场景与原始Typora56项通过；build/check通过。SCM菜单/历史多选/底栏布局三份回归为前一候选，随后仅改分支独立CSS；最终候选另通过分支UI、快捷入口、原生明暗及综合检查。首次失败、输入方式和平台边界在[证据](../enhancements/tests/evidence/branch_checkout_20260919.json)明确记录，明暗截图已视检。新语法预检避免原生注入错误耗尽超时。

2026.09.19.3已本机安装，27项资产匹配清单`8f2ab1e99979cf39de02656522fe8a0149237f551e41ca8c83cc08c4e020ccd0`；check OK，5项宿主/配置摘要不变。备份编号`20260919-201441-412-a283ad6d69cc497f93dcb1c27338c18f`。未重启用户窗口、未推送，保存后手动重启生效。开发skill已更新并验证；此前未完成项保持。
