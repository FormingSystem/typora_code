# 资源管理器、自动保存与本地历史

## R037 资源管理器分区与菜单

2026-09-14，用户提供十张连续截图，要求补齐资源管理器和本地历史流程。本次授权新增“打开的编辑器”和“时间线”，覆盖此前对应的布局冻结。文件夹继续使用已有按需目录树，文件操作继续调用文件服务；打开的编辑器从工作台叶子读取，不复制标签状态。关闭、关闭其他、关闭已保存及重新打开方式复用编辑器动作服务。

分区标题支持折叠，更多菜单和标题右键共享可见性设置；文件夹分区保持可见。打开的编辑器按组显示，默认最多展示9行，超出后内部滚动。文件菜单补充打开方式、剪贴板比较和时间线入口；文件夹菜单继续复用新建、剪贴板、系统定位、查找与终端。截图中的markmap、图片目录预览、聊天和文件引用查询依赖相应扩展或语言服务；本工程没有这些提供者，未增加这些入口。分区显隐属于工作台视图状态，其他设置使用下述上游键名。

时间线跟随当前文件，也可固定文件；Git历史与本地历史独立筛选、合并按时间排列。历史读取失败必须与空记录区分。切换目标、刷新及关闭时取消过期请求，迟到结果不能覆盖新目标。

### R037 分区标题对齐

2026-09-14再次反馈：打开的编辑器、文件夹根目录和时间线的箭头与文字没有对齐，根目录还继承了正文／文件树字体。原因是根目录保留旧标题的10px内边距和独立字重，另两行另设4px内边距与继承字体。三个同级分区统一由`workspace_explorer_sections`拥有标题几何；目录根名称节点保留供文件树刷新，只放入同一种折叠按钮，不重建目录树或复制状态。

核对固定VS Code `645f29cc3176500b4b5762ba887cf2a7f0ffdf2c`的`paneview.css`、Modern UI `modernUI.contribution.ts`、`padding.css`、`paneHeaders.css`和`fontRamp.css`：本轮采用28px分区行高、两侧4px外边距、左4px内边距、16px箭头及两侧2px间距、12px/600标题文字；使用工作台UI字体，不继承正文或等宽字体。固定版本的箭头、标题及工具栏属于同一行，三个分区不使用各自的位置补偿。标题长名称单行省略，原始目录身份不改变；时间线的文件名作为可缩略的次要文字。

工具栏遵循`paneview.css`的条件：分区展开且鼠标位于该分区内，或分区包含键盘焦点时显示；折叠时隐藏。工具栏使用已有公共交互的颜色、圆角及焦点规则，不再另写hover颜色。点击标题或按Enter／空格均只切换所属分区；刷新保留根名称节点和折叠状态。销毁时恢复原目录标题结构与处理器。验证需同时检查三行箭头和实际文字行盒、展开／折叠、键盘、长名、窄侧栏、主题与工具栏可见性；不能只比较外框。

本次定向验证：新增实际文字行盒与箭头坐标断言在旧版失败；修复后隐藏Electron中55项历史界面检查，以及资源管理器和文件搜索两个关联目标通过。覆盖三分区Enter／空格展开收起、Tab进入根目录工具栏、离开后隐藏、170px窄侧栏明暗状态、长目录名省略与时间线主标题优先、刷新保留节点和销毁恢复。上游缓存与红绿日志在`.cache/explorer_header_alignment_20260914/`，浏览器报告内容尺寸1182×802、`devicePixelRatio=1`，分别记录浏览器输入与宿主适配桩；原始Typora综合验收、安装与提交由主任务继续记录，不把这里的隐藏Electron验证写成已安装。

## R038 保存与历史恢复

统一设置入口采用VS Code键名和语义，存入现有工作台用户设置。桌面自动保存默认`off`，支持`afterDelay`、`onFocusChange`、`onWindowChange`；延迟默认1000毫秒。支持`files.autoSaveWorkspaceFilesOnly`和`files.autoSaveWhenNoErrors`，诊断错误来自现有Monaco模型。设置更改立即应用。自动保存不创建未命名文件，不切换标签、不弹保存位置选择框；源码继续使用文件服务的冲突检查和实际成功结果。保存失败保留草稿，并停止对同一失败内容反复重试，下一次编辑或显式保存可重试。当前配置落在工作台用户级设置，未移植VS Code的完整工作区和语言覆盖体系。

原生Markdown通过已经核对的`File.updateChangeCount`取得修改信号，成功写盘的`app.sendEvent/didSave`取得结果路径，覆盖另存为；不把保存前事件当作成功。运行期间由统一服务接管`File.option.enableAutoSave`，宿主草稿备份继续保留，原生自动保存菜单写入转交统一配置。自动写回调用`File.saveUseNode(false, true)`的静默路径；窗口失焦时只在同步调用入口临时允许当前宿主文档保存，返回后立即恢复真实前台状态。真实冲突处理、编码和错误结果仍属于Typora的保存实现，本工程不替换宿主写盘算法。

本地历史与自动保存各自负责一件事：自动保存触发写回，本地历史在成功保存后保存实际落盘字节。历史独立于Git、项目目录和当前打开的文件，放入应用用户数据目录`typora_code/History`。历史默认开启，每个文件256 KB、50条记录、相邻10秒保存可合并；大小、条数、合并窗口和glob排除规则可配置，不扫描VS Code或其他应用的私人备份。每条快照独立原子发布并验证摘要；同一文件的串行保存使用单调时间戳排序，避免同一毫秒的记录颠倒。

“查找要恢复的条目”先列出本应用历史中的文件，选择后列出保存时间；选版本打开与当前文件的比较，已删除文件可直接查看旧版本。选择版本本身不恢复文件。恢复动作先核对当前草稿及磁盘身份，并保留恢复前内容；与上游一样在真正恢复时明确提示。本工程选择先保存未保存草稿并留存快照，再恢复版本；这比上游直接放弃未保存草稿多了一步保护，保存冲突会中止恢复。恢复失败不能标为保存成功。关闭历史记录或排除当前文件时，需先允许保留恢复前版本才可覆盖。文件及目录重命名后，已有历史迁往新路径。

## 上游依据

固定源码：Microsoft VS Code `645f29cc3176500b4b5762ba887cf2a7f0ffdf2c`，1.137.0。核对文件：`files/browser/files.contribution.ts`（自动保存及Open Editors配置）、`files/browser/views/openEditorsView.ts`、`localHistory/browser/localHistoryCommands.ts`（两阶段选择及恢复）、`localHistory/browser/localHistoryTimeline.ts`、`services/workingCopy/common/workingCopyHistoryService.ts`与`workingCopyHistoryTracker.ts`（保存事件、历史合并与迁移）。完整上游路径均位于`src/vs/workbench/`。缓存保存在忽略目录`.cache/explorer_history_20260914/`。

官方说明：[自动保存](https://code.visualstudio.com/docs/editing/codebasics#_save-auto-save)、[本地历史](https://code.visualstudio.com/updates/v1_66#_local-history)。本工程模仿VS Code布局和功能设计，宿主保存适配使用Typora已经核对的入口。

## 验证与交付

2026-09-14通过`check:history`对应的自动保存调度、原生保存适配、真实临时文件历史、文件事务及隐藏Electron界面验证。覆盖四模式、连续输入、保存期间再次编辑、失败保留、源码诊断限制、原生保存后台入口与成功事件、18组编码/BOM/EOL、外部冲突、只读、删除后恢复、重命名、多历史实例、同毫秒排序与损坏检测。

界面使用真实工作台核心和Monaco，覆盖分区显隐/折叠、刷新后箭头、关闭已保存和取消关闭脏文件、Markdown重开菜单、Git/本地时间线、先选文件再选版本、恢复前保留草稿、主题颜色令牌及销毁。现有`test_workspace_explorer.cjs`和`test_workspace_files_search.cjs`回归通过。原生Markdown在此使用按已核对API构造的宿主适配桩；本轮没有操作用户的真实Typora窗口，因此不把这些测试写成原生实机验收。

完整入口与静态CSS独立构建通过，输出位于`.cache/explorer_history_20260914/build`。时间线筛选使用固定上游Codicons `filter`，同步保留原始SVG、来源清单与SHA256校验。

本次为侧会话新增需求，已有Git工作区差量属于主线程。仅增加本需求代码与窄范围接线，构建和测试使用独立输出；不安装、重启用户Typora或合并提交主线程改动。


### 2026-09-14 全量交付独立复查

自动保存按内容修订记录失败，编辑器失焦和窗口失焦不会重复尝试同一失败草稿；新编辑可再次触发。关闭文档后，尚未返回的保存不会再提示错误或重新排队。自动保存默认仍为 `off`，未命名文档和不符合诊断／工作区约束的文档不自动写入。

恢复确认由实际历史比较叶子拥有。关闭比较页或销毁历史模块会关闭所属确认；异步保留恢复前版本、保存草稿和最终历史写入返回时复查所属对象，关闭后的任务不能继续恢复或重新打开文件。恢复前同时拒绝目标草稿和正在保存／读取的目标；磁盘已经恢复后若编辑器又产生修改，保留该编辑内容，避免迟到重载。跨进程外部写入仍依赖文件身份和字节冲突检查，检查与文件系统替换之间不声称具备原子 CAS 保证。

新增回归在旧实现复现两种失焦模式重复失败写入，以及关闭历史比较后仍替换目标文件；修复后检查实际写入次数、正文、确认销毁和迟到打开／重载。Explorer 标题只创建一次分区显隐菜单，销毁时恢复原文件夹标题处理器。本次原生 Markdown 保存仍通过已核对 API 的隔离宿主夹具验证，不将其等同于真实 Typora 保存实机验证。


### 2026-09-14 原始宿主保存与恢复补验

完整候选在原始 Typora 1.14.10 的独立用户目录、私有桌面通过73项原生保存与历史检查。使用原生 CodeMirror 修改专属临时 Markdown、真实 saveUseNode/didSave，验证默认自动保存关闭、延迟自动保存、历史SHA256、先保存草稿再恢复、取消零写入、原生正文异步重载，以及明暗Explorer分区。证据 `.cache/native_zoom_1_14_10/explorer_history_native_20260914_c`；Win32指针、renderer Esc与物理键盘输入明确区分。此前“尚无原生保存验证”的阶段记录由本次证据补齐，其他平台边界继续保留。

## 2026-09-14 集成检查：历史比较宽度

原生截图复查发现历史比较没有占满编辑组。实际组的叶子使用横向 flex，而历史视图未声明剩余宽度，导致 Monaco 停留在初始内容宽度；这与自动保存或历史数据无关。历史视图采用已有源码文档的几何契约：`flex:1 1 0`、`width:100%`、`min-width:0` 和 `align-self:stretch`，比较正文继续由同一个 `git_diff_editor` 响应尺寸变化，不增加固定像素或补偿空白。

回归在真实工作台核心下比较叶子、历史视图和 Monaco 容器宽度，并继续覆盖取消恢复、保存草稿与销毁清理。新增断言在修复前失败；修复后34项历史界面检查通过，完整UI回归中的该目标也通过。证据分别为 `.cache/history_geometry_red_20260914.log`、`.cache/history_geometry_green_20260914.log` 与 `.cache/git_progress_final_ui_20260914.log`。本轮原生复核及安装结果另记在反馈记录。

最终原始Typora同一构建通过74项保存／历史检查，新增的实际叶子、视图和Monaco宽度一致断言通过，截图已复查；证据为 `.cache/native_zoom_1_14_10/history_geometry_native_20260914_a`。本次全量候选已安装且校验OK，运行中的用户窗口需正常重启加载；详见[本次交付](feedback_review.md#2026-09-14-全量安装与git动态进度收尾)。
