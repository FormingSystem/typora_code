# TyporaCode 开发交接

2026-09-23交付2026.09.23.14（R071.3）：正文位置/重排/大纲共用按排版失效的块几何，面包屑复用标题树，预览滚动不触发正文计算；多个预览与后台差异共用主题提取缓存，同值样式不重复写入。另修复导航异步预检期间的连续请求竞态。相同原始Typora组合场景串行对照：1000节正文100步滚动，元素几何读取403534→952、预览56909→408；100次宿主状态变更的已插桩回调累计3308.8→190.2ms（非独占CPU耗时）。构建、完整check及最终静态/部署补验、204套目录与最终6组UI通过，性能专项15项/3000次采样；原生最终性能17项、预览220项通过。相同候选两类隔离卸载重装及65卸载断言通过；本机38+24资产一致、5保护文件未变、check OK，实际卸载仅只读预检。用户窗口未重启，保存后正常重启加载。首次渲染/索引重建仍随规模增长，私有桌面节拍不是物理FPS；Win10/物理鼠标现场和任意第三方插件未全量覆盖，旧暂缓项保持。详见[本次证据](../enhancements/tests/evidence/reading_performance_20260923.json)。

2026-09-23交付2026.09.23.13（R069.8）：修复Shadow DOM预览内部Ctrl+滚轮被主窗口捕获的问题，沿完整事件路径识别所有者；浮动/搜索/分屏预览只调整各自阅读比例。按用户最新澄清，预览链接兼容普通单击、Ctrl+左键和右键跳转，拖选文字不误跳转，菜单随目标销毁。网页由宿主Chromium沙箱加载，等待/错误与回调清理归当前预览；默认浏览器异步启动失败捕获后局部提示，不透传全局。旧候选组合测试已复现失败；构建、完整check及最终静态/部署补验、202套目录和最终4组UI通过（链接65项、滚轮30项），原始Typora最终220项通过，包含真实网页脚本/拒绝内嵌、20轮历史/分屏及主窗口隔离。同候选两类隔离卸载重装及65卸载断言通过；本机38+24资产一致、5保护文件不变、check OK，实际卸载仅只读预检。用户窗口未重启，保存后正常重启加载。跨域load不等同站点成功显示，CSP/登录限制仍需默认浏览器；正文/菜单的原窗口缩放含义不变。物理鼠标硬件、Win10现场未本轮实测，未编辑保存提示继续暂缓；其他未完成项保持。详见[本次证据](../enhancements/tests/evidence/preview_isolation_20260923.json)。

2026-09-23交付2026.09.23.12（R034.2）：恢复Markdown渲染比较右侧30px红绿概览与视口滑块，支持点击变更、拖动和键盘定位，位置按真实渲染块高度映射；滚动只更新视口，布局变化合并刷新，取消和销毁停止交互。最终构建、完整check及静态/部署补验、202套目录、4组UI通过；Markdown专项37项、原始Typora62项通过，覆盖600段、20次切换和三档缩放。最终候选两类隔离卸载重装和65卸载断言通过；本机38+24资产匹配、5项保护文件未变、check OK，实际卸载仅只读预检。用户窗口未重启；Win10/真实SSH仍未现场覆盖。见[本次证据](../enhancements/tests/evidence/git_markdown_overview_20260923.json)。

2026-09-23交付2026.09.23.11（R034.1）：Markdown双版本比较默认并排排版，完整变更块左红右绿、对应行对齐；复用现有差异计算及工具栏，支持切回源码、差异定位与刷新，渲染态禁止隐藏源码选区写操作。构建、完整check及最终静态/部署补验、202套目录和5组UI通过；渲染专项24项、原始Typora55项通过，含600段功能压力、20次切换、主题/窄窗/缩放和真实Git刷新。相同候选两类隔离卸载重装、65卸载断言通过；本机38+24资产一致、5保护文件不变、check OK，实际卸载仅只读预检。用户窗口未重启，保存后正常重启加载。块级着色、历史图片替代展示及容量/平台边界见[设计](git_markdown_diff.md)和[当次证据](../enhancements/tests/evidence/git_markdown_diff_20260923.json)。

2026-09-23交付2026.09.23.10（R071.2）：Git状态改为带背压的流式NUL解析，SCM从单次XY快照投影，比较与排序分批让出线程；大量暂存路径通过标准输入传递。读取/写入预算分别5/30分钟，等待、取消和错误归Git区域；复用已有Git，确认缺失后提供Windows系统安装入口。完整check及最终发现/规模/静态/部署补验、200套目录和4组UI通过；真实100002项、20100054字节状态约459ms，主线程最大间隔15.85ms；原始Typora十万文件20轮实际变更刷新73项通过，耗时342–398ms。首次加载最大347ms及缩放短暂停顿仍记录。相同最终候选两类隔离卸载重装、65卸载断言通过；本机38+24资产一致、5保护文件不变、check OK，实际卸载仅只读预检。用户窗口未重启，保存后正常重启加载；Win10、真实SSH大仓库及新电脑winget安装未现场验收。详见[本次证据](../enhancements/tests/evidence/git_scale_20260923.json)。

2026-09-23交付2026.09.23.9（R069.7）：目录链接在当前预览列出直接子项，单击文件只读浏览、子目录继续进入；“返回目录”恢复列表位置和焦点，Alt前后保持本预览历史，打开源文件进入当前目标的普通标签。异步判型及列举复用本地/SSH文件提供者，大目录仅绘制可见行。构建、完整check、最终静态/部署、198套目录及4组UI通过；链接专项54项含1000子项、20轮往返与远端提供者端口，原始Typora203项含实际打开原文件通过。相同最终候选两类隔离卸载重装及65卸载断言通过；本机38+24资产一致、5项保护文件不变、check OK，实际卸载仅只读预检。用户窗口未重启，保存后正常重启加载；本轮未在Win10或真实SSH服务器现场验证目录预览。详见[本次证据](../enhancements/tests/evidence/preview_directory_20260923.json)。

2026-09-23交付2026.09.23.8（R069.6）：Markdown预览中的链接在本预览继续打开，Alt+左右恢复目标及阅读位置；新选中链接重置历史，各实例与正文隔离。读取失败保留原内容并可重试，迟到结果不能覆盖新选择。比例滑条和百分比并入打开源文件左侧，170px窄宽仍保留必要动作。构建、完整check、198套目录、最终4组UI（链接37项）及原始Typora172项通过，含20轮往返、分屏、三种主题和位置保持。相同最终候选两类隔离卸载重装、65项卸载回归通过；本机38+24资产一致、5项保护文件不变、check OK，实际卸载仅只读预检。用户窗口未重启，保存后正常重启加载；Win10未现场验证，网页隔离frame内部位置与导航不纳入本地Markdown历史。详见[本次证据](../enhancements/tests/evidence/preview_navigation_20260923.json)。

2026-09-23交付2026.09.23.7（R074）：功能区域共用明暗语义色，框架、内容、浮层与操作状态分层，正文及原生/社区设置保留原主题归属。主题变化先更新统一色表，再通知终端等消费者，图标不再反推终端背景。构建、完整check、198套目录校验及10组UI回归通过；颜色专项22项、原始Typora75项通过，1000次同值通知无调色板写入/终端更新、20次切换保留节点/焦点/滚动。隔离恢复和无兼容备份卸载重装、65项卸载回归通过；本机38+24资产一致、5项受保护文件不变、check OK，实际卸载仅只读预检。用户窗口未重启，保存后正常重启加载；Win10和全部第三方主题未现场覆盖。详见[本次证据](../enhancements/tests/evidence/workspace_colors_20260923.json)。

2026-09-23交付2026.09.23.6（R008.3）：修复底栏文件操作/字数/语言菜单的小数坐标反馈循环，共享浮层刷新器合并通知、隔离自身写入并取消关闭后的任务；悬停保留节点、焦点与滚动，原生阅读工具栏复用同一调度。旧候选300ms产生18次定位写入，新候选静止为0。构建、完整check及196套目录检查通过；8组UI回归、39项专项、原始Typora49项通过，覆盖明暗、多档缩放、终端遮挡和1000次通知压力。两类隔离卸载重装及65项卸载回归通过；本机38+24资产一致、5保护文件不变、check OK，实际卸载仅只读预检。未重启用户窗口，保存后正常重启加载；Win10及其他电脑未现场验证，未编辑保存提示继续暂缓。详见[本次证据](../enhancements/tests/evidence/popup_refresh_20260923.json)。

2026-09-23交付2026.09.23.5（R014.1）：终端Ctrl+滚轮修改共享字符字号，逻辑行锚点及xterm像素同步保持历史阅读位置；正文/标题安全区域走原生窗口缩放，代码、媒体、Mermaid与独立预览保持原手势。100%缩放入口常驻。构建、完整check、194套目录及5组UI通过；原始Typora完成62项检查，PowerShell220行输出、20轮往返及编辑区搬移保持进程/日志；正文字符偏移约0.54px。连续1000滚轮事件按帧合并，无逐事件配置写入。两类隔离卸载重装及65卸载断言通过；本机38+24资产一致、5保护文件不变、check OK，实际卸载仅只读预检。未重启用户窗口，保存后正常重启加载；Win10和物理鼠标硬件未现场验证，未编辑保存提示仍暂缓。详见[本次证据](../enhancements/tests/evidence/wheel_zoom_20260923.json)。

2026-09-23交付2026.09.23.4（R070.5/R070.6）：SSH按电脑与账号保存记录，支持别名、搜索、增删改及一键连接；默认终端跟随活动身份，另一个账号可单独新建终端，文件连接、拆分和重启共用认证。顶栏显示“SSH: 用户名”，悬停显示别名与真实地址。密码使用Windows凭据管理器，查看保险箱独立验证；重置只删除可查看记录，保留自动登录且不自动恢复旧查看内容。修正连接提前结束忙碌状态及抢先选择文件提供者，挂载等待共同目录事务。完整check、5组UI、真实Enter、100次并发记录保存/20次删除及20次保险箱往返通过；原始Typora主工作区101项、终端16项与20轮真实Shell命令通过。两类隔离卸载重装、65项卸载回归通过；本机38+24资产一致、5保护文件不变、check OK，实际卸载仅只读预检。未重启用户窗口，保存后正常重启加载。实机SSH仅一个Linux账号，多用户/端口隔离另由ssh -G和契约验证；未编辑保存提示仍暂缓。详见[本次证据](../enhancements/tests/evidence/ssh_accounts_20260923.json)。

2026-09-23交付2026.09.23.3（R072.3）：Typora偏好与社区原始设置在统一窗口右侧展示，不再另开后层弹窗；保留原生webview及SettingTab、字段与各自保存，切换/关闭恢复原始节点和样式。共同托管器跟随最大化与缩放，插件区域隔离通用表单CSS；修复宿主恢复正文焦点误关设置。完整check、4组UI及设置271断言（含真实鼠标）、原始Typora210项通过，覆盖20轮切换、明暗主题/缩放、实际社区保存、节点复用与前层命中，截图核对两类右侧页面。同候选两类卸载重装、65卸载断言通过；本机35+24资产一致、5保护文件不变、check OK，实际卸载仅只读预检。用户窗口未重启，保存后正常重启加载；未编辑保存提示继续暂缓，不宣称所有第三方插件及DPI兼容。详见[本次证据](../enhancements/tests/evidence/settings_owner_20260923.json)。

2026-09-23交付2026.09.23.2（R069.3—R069.5）：搜索、独立链接及分屏的本地预览共用缩放滑条/百分比/加减按钮，保留50%—150%、80%默认和原比例记忆。主侧栏竖向分隔条止于预览上方，不穿过加宽内容或拦截其拖动；按用户最新要求取消整列避让，正文与底栏保持正常宽度，预览只覆盖左下矩形。完整check、190套目录校验、5组UI（含真实指针44项）及原始Typora140项通过，覆盖170px窄预览、比例/滚轮同步、20轮显隐/分屏、缩放和实际命中；截图核对收起后全宽及边框不穿透。同候选两类卸载重装、65卸载回归通过，本机35+24资产一致、5保护文件不变、check OK；实际卸载仅只读预检，未重启用户窗口。保存后正常重启加载。保留旧夹具失败及分隔条拦截复现证据，网页不显示无效的本地文字缩放；未编辑保存提示继续按用户要求暂缓。详见[当次证据](../enhancements/tests/evidence/preview_scale_20260923.json)。

2026-09-23交付2026.09.23.1（R069.2）：链接预览右上关闭、顶部/右边/右上角调整高宽，侧栏收起时独立保留，打开预览不再主动展开功能栏；关闭释放占位且旧选区不会重新打开。搜索预览保持原有选择与打开流程，新增宽度/角落调整，宽度复用侧栏所有者。完整check、6组UI、真实指针37项及原始Typora107项通过，覆盖20轮显隐/分屏、三档缩放、原生搜索宽高和真实点击命中；修复截图揭示的侧栏背景遮挡。同候选两类卸载重装及65卸载回归通过，本机35+24资产一致、5保护文件不变、安装check OK，卸载仅只读预检。未重启用户窗口；保存后正常重启加载。初轮字符缩放断言失败记录保留，最终候选通过，不宣称全部系统/DPI覆盖。仅浏览保存提示仍按用户要求暂缓。详见[当次证据](../enhancements/tests/evidence/preview_dock_20260923.json)。

2026-09-23交付2026.09.22.14（R070.4）：SSH远端文件夹进入主资源管理器，文件操作、搜索、SCM/Graph和默认终端共享主机与项目；Markdown使用Typora原生即时编辑，图片按需加载，中文相对链接、锚点和前后导航沿用共同服务。保存先确认远端写入，冲突/断线保留草稿，Windows文件连接可选DPAPI密码记忆；终端仍独立OpenSSH认证。完整check、8组关联UI、真实SSH服务15项、原始Typora主工作区101项（含20轮编辑保存）及终端20轮命令通过。同候选两类卸载重装和65卸载回归通过，本机35+24资产一致、5保护文件保持、安装检查OK，实际卸载仅只读预检detach；用户窗口未重启，保存后正常重启加载。构建失败后旧资产测试未计入最终验收，补修内存规范化后原子发布及候选摘要核对。仅浏览保存提示按用户要求暂缓，16种格式224断言未复现；实时外部正文刷新、完整会话恢复、远程扩展宿主等仍保留明确边界。详见[本次证据](../enhancements/tests/evidence/remote_workspace_20260923.json)。

2026-09-22交付2026.09.22.13（R068.1）：原生Markdown围栏和只读Markdown预览增加悬停/键盘可达的一键复制；复制完整当前内容，含折叠部分和未保存修改，成功/失败有反馈。入口复用正文外覆盖层，不改变光标、折叠或正文高度。完整check、4组UI（100次复制及图片/图表回归）、原始Typora20轮及未保存/失败重试通过；修复验证中发现的未打包反馈图标和反馈文字被挤成竖排问题。同候选隔离两类卸载重装、65卸载断言通过，本机34+24资产一致、5保护文件保持、check OK，卸载仅只读预检。用户窗口未重启，保存后正常重启加载。测试记录剪贴板端口内容，不覆盖用户系统剪贴板；原生读取真实CodeMirror，不以端口测试宣称物理剪贴板回读。SSH完整远程工作区R070.4继续实施。详见[证据](../enhancements/tests/evidence/code_copy_20260922.json)。

2026-09-22交付2026.09.22.12（R070.4默认终端）：SSH连接身份由连接所有者提供，活动栏、菜单、新建及快捷键默认进入远端当前目录；已有本地PTY不被替换，显式本地配置可用，拆分/重启保持原会话身份，断线不回落本地，远程启动不等待本机Shell扫描。完整check、5组UI/ConPTY、原始Typora连接真实Linux虚拟机20轮命令与独立通道写入回读通过；同候选隔离两类卸载重装、65项卸载回归通过。本机34+24资产一致、5保护文件保持、安装check OK，卸载仅只读预检，窗口未重启。资源管理/搜索/SCM/原生Markdown的完整远程接入、安全密码记忆仍待实现；本次只完成默认终端归属修复，不代表完整Remote SSH。详见[证据](../enhancements/tests/evidence/remote_terminal_default_20260922.json)。

2026-09-22交付候选2026.09.22.11（R069.1/R072.2/R073/R073.1）：选中链接在独立左下区域只读预览，自动预览开关不影响搜索；左右/上下分屏复用当前主题、短文件名和打开源文件/刷新图标，网页以宿主Chromium隔离加载。设置改为唯一可最大化浮层，原生/社区配置独立；标签提供单行/换行，已打开编辑器改为edt active筛选列表。字符锚点保持重排视口，并修复宿主旧滚动动画覆盖恢复与关闭列表项造成焦点退出的问题。完整check、10组关联UI及最终5+3组复查、原始Typora82项（20轮分屏，Github/Night/本工程主题，边距和缩放）通过。同候选两类实际卸载重装、65卸载断言通过；本机34+24资产一致、5保护文件保持、安装check OK，卸载仅只读预检detach。用户窗口未重启，保存后正常重启加载。网页不绕过站点内嵌限制，私有桌面截图可能含旧帧，不将其作为逐像素等价证据。SSH统一远程工作区、搜索和SCM按用户最新反馈继续R070.4实施，不能将本轮称为所有历史需求已完成。[证据](../enhancements/tests/evidence/preview_settings_20260922.json)。

2026-09-22合并交付2026.09.22.10：保留远端8eae4e9/fb174ab的Win10 PowerShell历史修复，本地设置/SSH修改整理为其后续线性提交。齿轮只保留统一设置与扩展，Ctrl+,复用唯一设置页；原生和社区配置直达原所有者，移除三页重复导航并隔离Markdown标题样式。社区SettingTab从通用表单中移出，保留原始字段、节点及保存；独立安装官方2.10.23核对布局，真实Mapper/Copy插件92项和SSH原生119项通过。完整check、设置123断言及相关UI/ConPTY通过；最终候选两类卸载重装通过，卸载回归65项。本机34+24资产一致、5保护文件保持、安装检查OK，卸载只读预检detach；未重启用户窗口，需保存后正常重启加载。完整SSH工作区/SCM/恢复等仍待实现，不能称为全部VS Code SSH或设置像素等价。[证据](../enhancements/tests/evidence/settings_isolation_merge_20260922.json)。

2026-09-22本地未发布候选（原计划2026.09.22.9）：R072增加唯一设置编辑器，自有功能九类配置经统一注册表搜索、修改及恢复默认，保留原生偏好和插件SettingTab的所有权；R070.3远程Git改为独立只读标签，有界查询不再阻塞同连接文件操作。SSH配置集中管理连接参数，可见远程目录按配置定时刷新。完整check、5组关联UI、设置120断言、最终原生119项通过；慢Git期间100轮文件读写通过。同候选隔离两类卸载重装和65卸载断言通过，本机32+24资产一致、5保护项保持、检查OK，卸载仅只读预检detach；用户窗口未重启，保存后正常重启加载。本条为合并前候选记录；远端Win10已发布.9，本地设置和SSH改动改以.10重新构建验收。完整远程工作区/SCM/会话恢复等仍未完成，不将本轮称为全部VS Code SSH等价。[证据](../enhancements/tests/evidence/settings_ssh_20260922.json)。

2026-09-22发布回执：2026.09.22.9已由6cd3329普通快进推送至8eae4e9，远端main已核对一致。下方候选阶段记录保留当时事实，当前公开状态以本条为准；本机34+24资产一致、检查OK，保存后正常重启加载。[发布回执](git_history/20260922_release_9.json)。

2026-09-22候选2026.09.22.9（R006.10）：Win10 22H2 19045.7725 / Typora 1.14.10复现连续空回车导致ConPTY CSI S删除首行而不入历史，固定xterm 6.0.0的窄补丁将普通全屏SU接回自身历史滚动，保留容量、保存光标、擦除色、清屏、替代屏幕和局部区域语义。100次真实PowerShell回车及200行输出、滑块左中右命中、滚轮和上下键通过；20/100/1000协议行序、真实Chromium拖动及5组UI回归通过。完整check的42项已分段全部通过；首次短路径和长提示符回显夹具失败保留证据。最终同候选两类卸载重装、65卸载断言通过；本机安装check OK，34+24资产匹配，ASAR/偏好/设置字节保持，主题和启动页仅换行差异。用户窗口未重启，保存后正常重启加载。已合并另一机器11提交至6cd3329，保留其首次挂载测试与历史记录；正常推送状态以随后发布回执为准。Git Bash性能已按用户要求撤回，物理输入法/其他平台边界保留。[本轮证据](../enhancements/tests/evidence/terminal_scrollbar_win10_20260922.json)。

2026-09-22发行2026.09.22.8（R071.1）：按固定VS Code源码复核Git欢迎页，分离初次读取、空仓、读取失败和正常仓库状态；空仓隐藏提交框，读取失败提供重试，初始化进度固定在可见SCM标题。错误信息复用单一节点并随当前视图移交，切换分区设置不再露出无效控件。初始化与提交共用primary主题颜色；欢迎页按上游20px内边距、300px按钮上限及12px/16px按钮字形几何呈现。完整check、7组相关UI及最终目标/资产复查通过；原生Typora53项检查包括1万文件20轮刷新、空目录初始化失败重试和终端获焦。共享控件58断言、最终Git界面33断言通过。同候选隔离两类卸载重装、65项卸载断言通过；本机32+24资产一致、5保护文件不变、check OK、卸载只读预检detach。已安装，用户窗口未重启，保存后正常重启加载。原生缩放仍有68.6/69.2ms间隔，继续单独追踪；Git初始分支沿用本机Git配置，不声称所有VS Code设置已移植。[本次证据](../enhancements/tests/evidence/git_welcome_20260922.json)。

2026-09-22发行2026.09.22.7（R071）：Git表单接受后释放全局输入，按真实仓库互斥写入；刷新入口共用任务、项目配置异步读取。未跟踪合并改为路径集合，SCM变更、Graph详情与历史文件共用可视列表；空目录初始化、嵌套仓库/worktree和错误识别补齐。完整check、5组UI及最终50k三入口压力通过；真实Typora1万文件20轮刷新/阅读切换/终端获焦/三档缩放通过。旧合并50k约3586ms，新约5ms；真实采样大于50ms均位于缩放阶段（54–84ms），另列性能边界，不宣称全工作台无长帧。同候选隔离安装/两类实际卸载/重装、65卸载断言通过；本机32+24资产一致、5保护文件保持、check OK、卸载只读预检detach。已安装，保存后正常重启加载。测试技能与版本控制内测试约定已更新。[本次证据](../enhancements/tests/evidence/git_responsiveness_20260922.json)。本轮同步README中过期公开版本描述，早期“未推送”仅保留当时事实，当前发行以本条及远端Git为准。

2026-09-22候选2026.09.22.6：R070第二期提供远程目录项目终端和只读Git状态。点击即展示终端启动，认证留在SSH终端；特殊目录不展开本机变量，拆分/重启保留远端目标；Git查询固定远端目录，不混用本地仓库。真实原生86项检查、20次远程Shell命令和20次保存回读、终端面板/几何/主题回归通过；完整check及最终部署/目录/单元复查通过。相同候选隔离安装、两类实际卸载重装及65卸载断言通过；本机32工作台+24终端资产校验、5保护项不变、check OK、卸载只读预检detach。已安装，保存后正常重启加载，用户窗口未关闭。Git写操作通过项目终端；远程扩展宿主/调试/端口转发及其他平台未实现，不宣称全部VS Code Remote SSH等价。[验证证据](../enhancements/tests/evidence/remote_project_20260922.json)。

2026-09-22候选2026.09.22.5：R070首期增加左侧SSH远程资源管理器，远程目录/新建/UTF-8编辑、冲突保护保存、Markdown只读预览与单文件删除；认证交给OpenSSH，密码仅存内存，本地工作区与远端身份隔离。真实SSH100轮、原始Typora20轮保存回读及冲突/断线/关闭草稿保护、明暗缩放通过；完整check和5组关联UI通过。相同候选安装/两类卸载重装及65卸载断言通过，本机32工作台+24终端资产匹配、5保护项不变，check OK、卸载只读预检detach。已安装，用户窗口未重启；保存后正常重启加载。项目终端/Git仍待下一独立交付，不能称完整VS Code Remote SSH。提交后按用户授权推送，实际远端SHA以Git回执为准。[验证证据](../enhancements/tests/evidence/remote_ssh_20260922.json)。

2026-09-22本机候选2026.09.22.4：R069新增Markdown链接只读预览；搜索侧栏打开时选中链接显示目标，原生正文及阅读栏右键支持左右/上下分屏。本地目标可打开源文件编辑，网页联网后用无脚本沙箱呈现文档快照，登录及交互转系统浏览器。较晚链接选择不会被排队、运行完成或失败的搜索覆盖，显式新搜索可切回结果。最终构建、完整check、5组相关UI（含搜索时序回归）、原始Typora70项检查及20次分屏通过；同候选两类卸载重装和65卸载断言通过。本机29+24资产匹配，5保护项不变、check OK、卸载只读预检detach。已安装，用户窗口未重启，保存后正常重启加载；本轮仅本地提交，未推送。公网动态页面、物理鼠标、其他平台及私有桌面截图旧帧等边界见[交付证据](../enhancements/tests/evidence/link_preview_20260922.json)。Win10 PowerShell问题仍按用户要求暂停；提交前fetch核对origin/main与基线相同，未发现远端独有修改。

2026-09-22公开发布2026.09.22.3：origin/main从2548443快进到64127b5，包含检查失败直接重试、帮助菜单GitHub仓库入口及终端现场测试/暂停说明。GitHub固定提交7项关键文件逐字节核验通过；本机29工作台+24终端资产一致，安装check OK、卸载只读预检detach。沿用本次功能、原生及安装卸载证据，未重复执行压力测试或卸载用户环境。本回执随后一同发布，发行资产不变；下方“未推送”保留当时事实。用户窗口未重启，保存后正常重启加载。Win10 PowerShell问题未解决、按用户要求暂停本机排查；提交前复核未发现远端独有提交，后续两机并行仍需重新核对。[发布回执](git_history/20260922_release_3.json)。

2026-09-22本机候选2026.09.22.3：帮助菜单新增“Typora Code GitHub 仓库”，通过宿主浏览器适配打开固定项目地址，更新检查失败时仍可进入仓库获取ZIP。菜单复用现有布局/键盘/更多入口；安装指南区分下载完整包与首次纯离线所需运行时缓存。构建、最终完整check、顶栏UI与原始Typora119项断言通过，首轮夹具/文件保存失败及边界见[证据](../enhancements/tests/evidence/help_repository_20260922.json)。同候选两类卸载重装及65项卸载专测通过；本机29+24资产一致、5项保护文件不变，check OK、卸载只读预检detach。已安装，保存后正常重启加载，本轮仅本地提交、不推送。R006.10按用户要求暂停，由用户在Win10继续处理；两机并行开发需核对远端差量与发行清单，不能覆盖对方修改。

2026-09-22补充R006.10现场触发排查：用户报告按PowerShell上下键后才出现较宽历史滚动条。新补20轮首次/隐藏/重新挂载输出的前端对照，以及真实Windows PowerShell和PowerShell的命令历史召回前后采样；本机Win11两种Shell未按上下键已有14px滑块，滚轮位置189→145，召回后宽度不变。本次未复现Win10现场，旧输出是否恢复仍待确认，问题保持未完成。只补测试/设计/证据，产品及已安装2026.09.22.2不变，不发布、不重装；Git Bash子项继续停止处理。[证据](../enhancements/tests/evidence/powershell_history_trigger_20260922.json)。

2026-09-22终端反馈更正：用户确认Git Bash输入抖动为原生同类行为并明确不用处理，停止该子项。PowerShell仍在Windows 10 22H2（19045.7725）、Typora Code 2026.09.22.1复现无历史滚动条，不能归结为未加载候选；现场普通200行输出测试结果待确认。前轮Win11/参数夹具通过不能覆盖此缺口。重试按钮7215a16及2026.09.22.2已本机交付，本轮未执行推送。

2026-09-22本机候选2026.09.22.2：R047.3检查失败或超时结果新增“重试”，直接进入同一手动检查并立即显示进度；连续点击和旧节点不能重复发请求，沿用右上角关闭与公共操作区。构建、完整check、84项UI及原始Typora114项断言通过，各含20轮失败重试；键盘Tab/Enter可操作。相同候选安装/两类卸载/重装与最终65项卸载专测通过；专测首次文件替换失败及未明占用原因保留。本机29工作台+24终端资产一致、5保护项未变，check OK、卸载只读预检detach。用户需保存后正常重启，未推送。R006.10用户再次确认Win10无历史滚动条、Git Bash仅略有改善，终端需求继续未完成，不被本项覆盖。[验证证据](../enhancements/tests/evidence/update_retry_20260922.json)。

2026-09-22本机候选2026.09.22.1：R006.10接入配套ConPTY DLL、真实Windows后端信息及DA1响应，重复行格不再通知PTY，启动期间尺寸变化在就绪后补齐。构建、完整check、5组UI（54几何样本）、11种实际Shell配置的200行滚动/100次输入和原始Typora两种Shell的12项断言通过；滑块有真实尺寸。简单PS1仅用于隔离传输性能，默认Git Bash提示符松键后约5秒仍输出，尚未解决；Win10现场无滚动条仍待实机复核，本机为Win11。相同候选restore/detach卸载重装、65卸载断言通过；本机29工作台+24终端资产匹配，5保护项不变，check OK、卸载只读预检detach。已安装但用户窗口未重启，保存后正常重启加载；本次本地提交，不推送，不宣称需求全部完成。[验证证据](../enhancements/tests/evidence/terminal_conpty_20260922.json)。

2026-09-21发布2026.09.21.4：origin/main从9c0bb5d快进到9633d18，公开终端末行间距、完整路径搜索和代码块高度隔离三个修复；源码、设计、测试和对应构建随各修复提交。GitHub固定提交的公告、29项资产清单、工作台脚本、启动入口及卸载脚本共7项与Git字节一致；本机29资产匹配，安装检查OK、卸载只读预检detach。本条发布回执随后追加提交，发行资产不变；下方“未推送”保留历史事实，当前公开状态以本条为准。复用各修复本次验证，不把发布核验写成重跑原生压力测试；用户窗口未重启，保存后正常重启，跨平台/物理输入/卸载后真实宿主启动边界保留。[发布回执](git_history/20260921_release_4.json)。

2026-09-21本机候选2026.09.21.4：R068修复长代码展开/布局变化后短块保留原生高度缓存；按每块宽度与可见性合并刷新，离屏延后，不改变正文、光标和独立展开状态。旧版宽度变化后可见短块107.78→61.86px复现；新版原始Typora生成20轮、原文副本10轮共230次高度对照通过，3组UI及完整check通过。同候选安装/两种卸载/重装、65卸载断言通过；本机29资产一致、5保护项不变、check OK、卸载只读预检detach。用户窗口未重启，保存后正常重启加载；本地候选未推送。固定宽度旧版也可通过；私有桌面renderer输入、跨平台及卸载后实际宿主启动边界保留。[证据](../enhancements/tests/evidence/code_blocks_geometry_20260921.json)。

2026-09-21本机候选2026.09.21.3：R058修复显式路径检查延迟时清空已枚举搜索结果且没有等待提示的问题；目录候选先筛选，路径探测异步补充，同查询增量不反复中断评分，新查询/关闭拒绝迟到结果。完整check、3组UI、51001文件功能与5万候选性能、原始Typora30项检查及20轮查询通过；同候选安装/两种卸载/重装、65卸载断言通过。本机29资产一致、安装检查OK、卸载只读预检detach，用户窗口未重启，保存后正常重启加载；未推送。小工作区旧版完整路径测试也可通过，截图现场具体延迟源未证实，需用户重启复查；物理输入、其他平台及卸载后真实宿主启动边界保留。[证据](../enhancements/tests/evidence/quick_open_async_20260921.json)。

2026-09-21本机候选2026.09.21.2：R006.9修复终端末行贴底/裁切，父层留白移到FitAddon会扣除的xterm本身，面板/分屏/编辑器共用4px底部间距。旧版在80%缩放、状态条显示时复现约3px越界；新版54组几何、3组关联UI及原始Typora真实Shell140项检查通过，45组原生采样最小间距约4.21px。完整check、同候选安装/两种卸载/重装、65卸载断言通过；本机29资产匹配、安装检查OK、卸载预检detach。用户窗口未重启，正常重启加载；本次本地提交，未推送。历史容量及Shell输出不变，真实物理输入和跨平台边界保留。[证据](../enhancements/tests/evidence/terminal_geometry_20260921.json)。

2026-09-21终端滚动历史调查同步：用户明确反馈向上翻阅时旧日志消失。已核对scrollback代码默认1000行、可配置0至100000，xterm拥有历史缓冲；正文直接传入term.write，PTY流控与辅助进程stderr截取不等同日志历史裁剪。已补充设置入口、调大无法恢复旧内容及内存/查找/重排影响；未读取用户现场有效值、未做性能压测、未改配置或产品。仅说明同步，发行仍为2026.09.21.1，无需重建及安装/卸载；历史实际验证不重算。详见[终端说明](terminal_operations.md#r006-滚动历史容量与旧日志消失2026-09-21)。

2026-09-21发布2026.09.21.1：origin/main从56de16d普通快进到95f596d，包含R067导航历史、R025成对安装卸载验收及使用说明。固定提交公告、29项清单、工作台脚本、启动入口和卸载脚本共7项与Git字节一致；本机29资产匹配、安装检查OK、卸载只读预检detach。安装/两种卸载/重装隔离事务及65卸载断言已通过；未实际卸载用户环境。此回执随后追加发布，发行资产不变；下方历史未推送记录保留当时事实，当前公开状态以本条为准。用户窗口未由发布流程重启，跨平台及物理输入等既有边界保留。[发布回执](git_history/20260921_release_1.json)。

2026-09-21补验R025：2026.09.21.1同候选通过公开入口的安装→检查→restore卸载→重装→检查→detach卸载→重装→检查；正文、工作台配置及原始备份保留。Windows PowerShell5.1卸载专测65断言通过，目录165组与部署21文件/29资产检查通过。本机卸载仅只读预检detach，安装检查OK；未实际卸载用户环境。隔离宿主为文件事务夹具，不代表卸载后真实Typora启动验收。每轮安装交付必须成对验卸载的要求已写入仓库规则与开发skill。本次只改测试/规则/记录，无新发行、不重装、不推送。[证据](../enhancements/tests/evidence/install_uninstall_20260921.json)。

2026-09-21本机候选2026.09.21.1：R067将Markdown链接起终点、源码行列及编辑组接入同一导航历史，Alt＋左右键与顶栏按钮共用状态；关闭后可回溯重开，近邻源码移动合并，明确定位保留。链接沿用共同文件服务的草稿及编辑组保护，失效目标不污染返回位置。会话身份未变化时跳过重复磁盘替换，实际错误保留原记录。构建、完整check、7组关联UI及最终目标回归通过；原始Typora1.14.10正式候选49项检查通过，包括DOM点击、编码路径、20次跨编辑器往返、分栏及关闭重开。已安装29资产，5保护项与最初摘要一致、profile与最终安装前备份一致、只读检查OK；用户窗口未重启，保存后正常重启加载。本次本地提交，按用户之前要求暂不推送。源码选区采用固定VS Code默认跨编辑器/组及10行邻近规则；Markdown按原生块和阅读位置保存，不宣称渲染行等同源码行。详情见[导航设计](navigation_history.md)和[验证证据](../enhancements/tests/evidence/navigation_history_20260921.json)。

2026-09-20本机候选2026.09.20.24：R066统一共享弹窗标题栏的官方×关闭和底部操作右对齐；设置、Git、文件与更新沿用各自取消/关闭语义，保留输入初始焦点、键盘循环及单次清理。窄窗口按钮可换行，基础样式从Git/大纲收敛到共同控件。构建、完整check、78组UI和原始Typora正式候选111项断言通过；三种主题、两档缩放、设置草稿不落盘、检查取消/安装继续与20次反复关闭通过。29项安装资产匹配受测候选、6保护项未变、只读检查OK。用户运行窗口未重启，下次启动加载。本次按用户后续要求仅本地提交、暂不推送，等待用户检查效果；更新缓存反馈已撤回，未改复用逻辑。原生PrintWindow存在旧帧，最终主题及布局依据DOM实测；物理输入、跨平台和历史未决边界保持。详见[验证证据](../enhancements/tests/evidence/dialog_controls_20260920.json)。

2026-09-20发布2026.09.20.23：终端先展示与启动反馈修复5fcfeed已普通快进推送origin/main；固定SHA公告、资产清单、工作台脚本/样式与启动入口六项内容逐字节核对一致。本机29资产与原生受测候选一致，安装检查OK。本回执随后追加发布，发行资产不变；下方历史提交前状态保留当时事实。用户窗口未重启，需保存后正常重启加载；本次真实Shell样本及既有验收边界见[发布回执](git_history/20260920_release_23.json)。

2026-09-20交付2026.09.20.23：R006.8点击终端先创建会话并显示面板，目录准备、Shell探测、进程启动及等待首次输出使用阶段文字和共享活动进度。隐藏/重开复用会话，失败保留在终端内；重启期间迟到目录结果不覆盖新基点。最终构建、完整check、6组关联UI及原始Typora正式候选63项检查通过，含3轮真实PowerShell/ConPTY和20次立即终止；下一绘制帧布局约8.7–28ms，首次输出完成检查约0.6–2.2秒，仅为本机样本。已安装29资产与受测候选一致、6保护项未变、只读检查OK。用户窗口未重启，保存后正常重启全部Typora窗口加载。本条记录提交前状态，远端结果由后续发布回执补充；首次输出不等同提示符就绪，既有跨平台/物理输入等未决边界保留。[验证证据](../enhancements/tests/evidence/terminal_startup_20260920.json)。

2026-09-20发布2026.09.20.22：origin/main已从465de13普通快进到f76a580，包含91690ec更新全阶段进度、b8a7744拖窗调查及当前使用说明。固定SHA公告、资产清单、工作台脚本/样式、更新服务及启动入口六项与Git内容一致；本机29资产一致，只读检查OK。本回执随后追加发布，发行资产不变。下方历史“未推送”保留当时事实；本次不重建、不重装或重跑整套测试。用户窗口未重启，H001.2未复现及真实联网/UAC等既有验收边界保持。详见[发布回执](git_history/20260920_release_22.json)。

2026-09-20交付2026.09.20.22：R047.2为检查、下载、校验解压及安装接入共享进度绘制，Git沿用同一外观与动画；下载显示真实字节，有有效总量才显示百分比，其余阶段持续活动。取消立即反馈，关闭后可重新查看任务，失败/取消停止活动，成功提示手动重启。构建、完整check、更新服务、41项更新UI、Git快捷动作UI和独立真实ZIP安装事务通过；原始Typora正式候选34项断言含20次取消清理通过。首轮原生夹具过早打开浮层被首次文档切换关闭，保留失败并修正测试时序。已安装29资产与原生受测候选一致，6保护项未变，只读检查OK。用户窗口未重启，本轮未推送；真实联网吞吐/UAC及历史未决边界保持。[验证证据](../enhancements/tests/evidence/update_progress_20260920.json)。

2026-09-20 H001.2顶栏移动尺寸调查：用户后续确认重新打开后正常。当前2026.09.20.21独立宿主两种尺寸960×640、1130×720的系统移动及最大化/还原均保留宽高；顶栏使用原生app-region:drag，未发现工作台写固定宽高。非客户区消息未复现真实鼠标从最大化拖下，不能作为该路径已通过。根因未确定，状态为未复现/待复发取证；未改产品代码、未重新安装或重启用户窗口。[调查证据](../enhancements/tests/evidence/window_move_20260920.json)。

2026-09-20交付2026.09.20.21：R005.3取消同路径无条件拒绝，源码/Markdown正文及保存格式一致时直接复用目标标签和撤销；相同草稿不要求确认。差异、比较期间新输入与目标变化保留双方；时间线只作为保存版本记录，不代替草稿保护。构建、最终完整串行check、46项文档移交及26项控制器检查通过，真实频道20/100/1000和2000次取消通过；原始Typora80轮往返中40轮目标已打开同文件，四种已保存/草稿状态各20轮，均无重复标签或额外提示。两次完整检查曾遇到既有Windows临时文件替换EPERM，原因仍待归因，保留失败记录，不宣称已修复。本机29资产与受测候选一致、6保护项不变、只读检查OK；用户窗口未重启，保存后正常重启全部Typora窗口加载。本轮未推送，物理跨屏输入和既有边界保持。[验证证据](../enhancements/tests/evidence/drag_duplicate_20260920.json)。

2026-09-20交付2026.09.20.20：R005.2修复拖出附窗按目录恢复整组文件并覆盖原窗口会话；附窗只接收指定文档，普通启动和主动切目录继续恢复。移交失败改为不抢焦点的共享通知，成功无额外确认。压力发现连续移回Markdown被前一次宿主切换误拒绝，增加事务开始前的有界等待和取消/目标变化保护。最终构建完整check、3组UI（含38项文档移交断言）、开启恢复配置的原生源码/已保存Markdown/未保存Markdown各20轮，以及普通重开第二进程恢复通过。保留修复前失败证据；本机29资产与受测候选一致、6保护项不变、只读检查OK。用户窗口未重启，保存后正常重启全部Typora窗口加载。本轮未推送，物理跨屏输入与既有边界保持。[验证证据](../enhancements/tests/evidence/drag_direct_20260920.json)。

2026-09-20发布2026.09.20.19：origin/main已从90e1caf普通快进到ac1c903，包含72da736分屏修复及当前说明同步。固定SHA的公告、29项资产清单、核心/工作台bundle与静态head均与Git内容逐字节一致，本机29资产一致、只读检查OK。本回执随后追加发布，发行资产不变；下方历史“未推送”保留当时事实。用户窗口未由本流程重启，物理跨屏输入和设计列明的未实现配置及其他既有边界保持。详见[发布回执](git_history/20260920_release_19.json)。

2026-09-20交付2026.09.20.19：R005.1修复删除分栏取错权重、嵌套收回比例丢失及分隔线调整挤动其他栏；标签拖动只在可滚动边缘运行，活动标签重排和跨组插入不再重复开关编辑器。20/100/1000轮布局与跨renderer交接、2000次取消、原始Typora独立副本290断言和20次真实建窗合并通过；完整check与78组UI通过。空闲回调及重复生命周期工作已消除，总耗时无显著下降，不宣称整体提速。最终构建与受测资产一致，本机29资产一致、6保护项未变、检查OK。用户窗口未重启，保存后正常重启全部Typora窗口加载；本轮未推送。物理跨屏鼠标、跨进程撤销历史及尚未实现的正文边缘自动分屏/VS Code配置边界见[设计](drag_and_windows.md#r0051-压力测试与资源清理2026-09-20)和[证据](../enhancements/tests/evidence/drag_stress_20260920.json)，不覆盖其他历史未决。

2026-09-20发布2026.09.20.18：origin/main已从a1272d4普通快进到b14a6cb，包含face801更新检查反馈修复及使用说明同步。固定SHA的公告、29项资产清单、工作台bundle和启动入口与Git内容逐字节一致，本机29资产匹配且只读检查OK。本回执随后追加发布，发行资产不变；下方“未推送”保留当时事实，当前公开状态以此记录为准。用户窗口未重启，跨平台/UAC/启动长任务等既有边界保持。详见[发布回执](git_history/20260920_release_18.json)。

2026-09-20交付2026.09.20.18：R047.1手动检查更新立即显示状态，后台检查复用并转为可见，支持取消后重试；迟到请求不覆盖新提示。完整check、23项更新UI、21项共享Esc/焦点及原始Typora独立副本268项断言通过（其中10项更新入口）；网络为替身。本机安装29资产一致、6保护项不变、检查OK。用户窗口未重启，保存后正常重启全部Typora窗口加载；本轮未推送，既有未决边界保持。见[证据](../enhancements/tests/evidence/update_feedback_20260920.json)。

2026-09-20发布2026.09.20.17：origin/main已从8567d53普通快进到66f32d4，包含5个既有修复/交付提交及当前说明同步。远端OID、固定SHA公告/29项资产清单/head及卸载脚本与提交内容一致，本机只读安装检查OK。下方历史“未推送”保留当时事实；本次公开状态覆盖旧当前发布说明。用户窗口未重启，未执行真实卸载；既有兼容/性能/平台边界保持。详见[发布回执](git_history/20260920_release_17.json)。

2026-09-20交付2026.09.20.17：R025实际卸载失败来自schema 3安装前备份被排除，且宿主发行标识已升级。保留完整restore校验；无兼容备份时备份当前启动页后，只撤销唯一TyporaCode入口，保留标记外字节与个人数据；新增-check_only、日志、安装互斥及失败回滚。完整check、Windows安装/恢复及日志检查、65项真实CMD卸载断言通过，本机只读预检为detach。已安装29资产一致、6保护项不变、check OK；卸载脚本立即可用，用户环境未实际卸载、窗口未重启，本轮未推送。此路径保留主题、原生偏好和插件缓存；其他历史未决不覆盖。[证据](../enhancements/tests/evidence/uninstall_repair_20260920.json)。

2026-09-20交付2026.09.20.16：R057/R060补齐真实社区市场、安装后启用引导、独立设置入口与真实插件配置保存；已有工作台布局和设置所有者保持。R061统一Explorer、时间线与Git一级分区的整行留白、背景及动作区。R065按同一菜单的实际名称/快捷键测量共享列宽，正常视口完整显示，极窄视口换行。完整check、8组关联UI、原始Typora的261条共享界面与90条社区插件断言通过；真实社区目录37项读取及真实ZIP安装通过，两个上游插件验证启停与设置持久化。已安装29资产一致、6保护项不变，安装检查OK；用户窗口未重启，保存后正常重启全部Typora窗口加载。未推送，旧启动性能/跨平台/跨账户UAC未决保持。[证据](../enhancements/tests/evidence/shared_ui_plugins_20260920.json)。

2026-09-20交付2026.09.20.15：修复设置及同类共享菜单快捷键被挤入箭头列、横向滚动的问题。完整check、7组UI与原生237断言通过；已安装29资产一致，6保护项不变、检查OK。R061追加三主题/两档宽度和缩放的绝对边界回归，当前候选没有额外外层白边；未改侧栏几何，用户现场加载版本未知，保存后重启复核保持待验收。当前用户窗口未重启，本轮未推送；旧未决项保留。[证据](../enhancements/tests/evidence/menu_overflow_20260920.json)。


2026-09-20当前发布状态：2026.09.20.14已普通推送origin/main，发布候选5587d15包含R061至R064及当前说明同步；远端OID、固定提交公告/29项资产清单/静态head核对一致，本机只读检查OK。此处覆盖下方各轮“未推送”的当前状态，历史记录仍保留当时事实。用户运行窗口未由开发流程重启；跨账户UAC、跨平台和启动长任务仍待后续验证。详见[发布回执](git_history/20260920_release_14.json)。

2026-09-20交付2026.09.20.14：R064按远端SHA下载ZIP并记录成功安装身份，同序号新提交仍可更新；首次无Git安装按清单建立等价身份。下载/解压位于Typora用户数据temp，安装权限继续归R063。最终更新域4套、20/100/1000争抢和无Git/.git真实ZIP安装通过；本机普通权限安装，29资产一致、6保护摘要不变、check OK。用户未重启、未推送；网络使用固定提交替身，实际安全桌面/跨账户UAC仍未实测。[设计](workspace_update.md#r064-无git安装的提交身份与temp目录)与[证据](../enhancements/tests/evidence/update_commit_20260920.json)。

2026-09-20最新交付2026.09.20.13：R063按待写目标预检，未变化入口不覆盖，实际拒绝后解释并一次系统授权续装，保持原用户/备份/缓存身份；取消保留旧版本。完整check、权限压力、Windows事务与真实ZIP更新、更新UI和卸载回归通过。本机普通权限安装，29资产/head一致、6保护摘要不变、check OK；用户待正常重启，未推送。真实安全桌面和不同管理员凭据仍未实测。[设计](installation_design.md#r063-windows权限预检与按需授权)与[证据](../enhancements/tests/evidence/install_permissions_20260920.json)。

2026-09-20最新交付2026.09.20.12：R062提交浮层/菜单共用GitHub、Gitee、GitLab、Bitbucket网页入口，多目标一次选择，点击前复核远端并拒绝切库迟到。完整check、两组UI、解析20/100/1000与原始宿主33断言通过；29资产/head一致、5保护项不变、安装检查OK。用户待正常重启，未推送。[设计](git_commit_web.md)与[证据](../enhancements/tests/evidence/git_commit_web_20260920.json)。未知自托管平台及联网可访问性不在已通过范围；以下保留历史事实。

2026-09-20最新交付2026.09.20.11：R061统一Explorer／时间线／Git分区折叠槽位，树根取消额外留白并保留8px层级缩进。完整check、6关联UI和原始宿主138断言通过；29资产/head一致、5保护项不变、安装检查OK。用户待正常重启，未推送。[设计](explorer_history.md#r061-折叠槽位与树形缩进)与[证据](../enhancements/tests/evidence/sidebar_disclosure_20260920.json)。以下保留历史版本事实。

2026-09-20最新交付2026.09.20.10：R060扩展图标/侧栏与齿轮插件设置，入口复用同一插件服务和SettingTab；帮助菜单移除旧入口。完整check、7UI、20/100/1000及原始宿主通过，29资产/head一致、5保护项不变、安装检查OK；用户待正常重启，未推送。[设计](community_plugins.md#r060-扩展活动栏与设置入口)与[证据](../enhancements/tests/evidence/community_plugin_entries_20260920.json)。以下保留各历史版本事实。

2026-09-20最新交付2026.09.20.9：R059统一最近文件/目录菜单与Ctrl+R管理，确定失效记录在打开前持久化移除，权限/离线保留，清空与单项移除仅处理历史。目录切换/文件会话沿用原服务。完整check、7相关UI、20/100/1000及原生20次重读通过；29资产/head及5保护摘要一致，安装检查OK，用户待正常重启，未推送。见[设计](recent_open.md)和[证据](../enhancements/tests/evidence/recent_open_20260920.json)；下文保留当时版本事实。


2026-09-20 R058：文件快速打开采用固定VS Code查询/评分/排序/高亮，移除5万文件扫描截止，512候选按可见行绘制；[设计与边界](quick_open.md)。2026.09.20.8已本机安装，29资产/head一致、5保护项不变、check OK，用户需正常手动重启；当前测试见[证据](../enhancements/tests/evidence/quick_open_20260920.json)。以下历史记录保留当时版本。

2026-09-20当前启动方案：原生head直接接入、唯一DOM工厂同步挂载，完整UI首次ready后显示；去除加载提示层并防止核心根过早显示。静态模板备选已由此方案替代，见[最终启动设计](native_startup_design.md)。最终2026.09.20.7已安装，28资产及head一致、5保护摘要不变、检查OK；测试和未决边界见最新反馈；以下时间段保留当时事实。

2026-09-20 R057已实现：帮助菜单管理社区v2插件，唯一核心ABI、安装默认停用、异步后置加载、跨窗口配置同步、不可变包更新及共享设置窗口。候选2026.09.20.6，具体测试和安装状态见最新反馈记录；历史段落为当时状态。R056已完成单次显式挂载和标准重装模拟，前置静态模板及实际官方升级仍不能宣称完成。

2026-09-20最新安装2026.09.20.3：全局原生滚动条活动显示、空闲淡出，共享实现保留原布局和第三方显隐。检查及边界见[工作台交互](workspace_interaction.md#r0491-空闲滚动条淡出)，安装27资产/head一致，用户待手动重启。R056/R057仍按先现有UI、后社区适配推进；官方更新后用户手动重装增强，不再追踪独立启动器/自动修复。


2026-09-20 R056：[直接启动与宿主升级设计](native_startup_design.md)已记录当前依赖、唯一布局节点接管、配置所有者、错误恢复及升级后安装事务。用户已明确保留原Typora图标；社区上游安装/加载/Core更新及另一框架维护者说明已核对，未发现可直接复用的官方升级完成重新注入。先前入口选择不再是前置条件；本轮增加head依赖采样并复查基线，未修改运行资产或安装新版本。现有首次展示遮挡不算 R056 已实现，原生编辑内核与必需节点继续保留。

2026-09-20 R055：用户已授权同步信息并强制发布origin/main；发行资产仍为已验证安装的2026.09.20.2。本次按用户说明、架构/验收交接、发布回执分类提交；历史条目中的“未推送”保留为当时事实，origin/main已通过显式租约强推并复核公告、清单和静态head，当前发布结果统一由[发布契约与回执](workspace_update.md#r055-信息同步与历史发布)说明。远端旧历史已备份，消息重写不代表代码回退。

2026-09-20 R054：增加启动前置采样和侧栏真实生命周期测试，修复旧布局闪现、切换先收起再展开及重复show。静态head只在首次挂载阶段管理展示，失败/超时恢复原生；语法与布局解耦。2026.09.20.2已安装，完整基础检查、74 UI及三次原生启动/20次切换通过，27资产和head一致、5保护项不变。残余50–67ms启动长任务仍待归因，原始宿主没有V8 Inspector采样能力，不能把无异常切换外推为全功能无卡顿。详见[设计与复验入口](startup_stability.md)和[证据](../enhancements/tests/evidence/startup_stability_20260920.json)。用户窗口未重启、未推送；此前未决项保留。

2026-09-20 R053：SCM提交浮层渲染Markdown列表/强调/代码/链接，采用固定VS Code compact几何和分区；共同Shadow渲染隔离正文主题，构建明确区分独立Shadow样式与启动全局样式。2026.09.20.1本机安装通过，27资产一致、5保护项未变；完整check、7 UI及原生21项通过。原生主题采用setting.setCurTheme完成后应用，不能仅发命令即声称生效；私有桌面PNG有旧帧限制。详见[R053设计与证据](workspace_interaction.md#r053-提交浮层的-markdown-排版)，用户窗口待正常重启，未推送。

2026-09-19 R040.2：按目录文件会话恢复已实现并安装2026.09.19.9；沿用原生restoreWhenLaunch=2，空记录不回退最近文件，切换隔离继续保留。核心逆序遍历遇truthy会退出，收集需void；偏好页getExtraOption才包含恢复选项。设计、测试和交付见[工作区切换](workspace_switch.md#r0402-按工作区恢复上次打开的文件)，用户需手动重启。

2026-09-19 R040.1交付：打开文件夹、最近目录和关闭文件夹共用工作区切换，保护未保存Markdown/源码，清除旧编辑组、搜索、固定时间线、阅读历史与Git控制器；终端经提示后结束，同目录只刷新。修复前基线旧标签残留断言失败，新版5份分类报告、原始Typora 1.14.10的13项与20次切换、20/100/1000单元压力通过；build/check和72套UI回归通过。2026.09.19.7已安装，27资产与候选一致、5项保护摘要不变，check OK。用户窗口未重启、未推送；原生输入方式及平台/功能边界见[工作区切换设计](workspace_switch.md)和[证据](../enhancements/tests/evidence/workspace_switch_20260919.json)。历史未完成项保持。

2026-09-19 R051最新纠正：提交正文逐行`- 详情`，必须说明具体问题和修改效果，不能用文件名或“同步文档/补测试”填充。首轮仅格式通过，用户内容验收未通过；本轮逐条复核87条，重写68条清单式正文，保留19条已有说明。三分支已本地重写，87条文件树、标题、作者/时间及拓扑保持，完整bundle恢复、格式与对象校验通过，27项产品及安装资产未变。规则与开发skill已同步，格式检查后仍需内容审阅。原始、首轮和最终OID经[消息设计与映射](git_commit_messages.md)追溯。远端未推送，发布前需重新核对远端并取得强推授权；历史未完成验收项保持，本轮无须重新安装。

2026-09-19 R034边界复查：面包屑下沿恢复1px明暗主题分界线，共享绘制层保持22px行盒和正文起点。build/check、面包屑及文件/diff顶栏回归、原始Typora80项和明暗截图通过。2026.09.19.6已安装、27资产一致、5项保护摘要不变，check OK；用户窗口未重启，未推送。设计与证据见[导航分界线](editor_header.md#r034-面包屑与正文分界线)。

2026-09-19 R050最新交付：中央Git Graph三条文本列边界统一调整相邻可见列，拖动同向、提交编号经左侧边界缩放，说明列继续弹性填充。最终单元/功能、20/100/1000压力和原始Typora78项通过，明暗截图已视检；相关4项UI回归通过。2026.09.19.5已安装、27资产一致、5项保护摘要不变，check OK；用户窗口未重启、未推送。设计与验证边界见[列分隔线](../enhancements/git_graph_features.md#r050-提交图列分隔线)，证据见[本轮记录](../enhancements/tests/evidence/git_columns_20260919.json)。

2026-09-19 R049最新交付：滚动条基础样式统一到workspace_scrollbars.css，8px宽度、4px滑块圆角及原生三态，Monaco/xterm/阅读缩略图集中适配；不再由面板复制。10套件/12选用报告、三档压力和原始Typora62项通过；2026.09.19.4已安装，27资产一致、5项保护摘要不变，check OK。用户窗口未重启、未推送；设计与证据见[共享滚动条](workspace_interaction.md#r049-共享滚动条基础样式)。

2026-09-19 R048最新交付：底栏/SCM分支统一顶部检出选择器，搜索本地/远端/标签并展示提交详情；创建/指定起点/分离检出与自动跟踪复用接入既有写事务。2026.09.19.3已安装、27资产一致、5项保护摘要不变，check OK；原始Typora56项及20/100/1000生命周期压力通过，用户窗口未重启、未推送。方案与本轮失败/复跑证据见[分支检出](git_branch_checkout.md)。

2026-09-19 R044复查：活动栏右键菜单恢复实际可见性勾选，取消额外功能图标与原生留白；2026.09.19.2已安装、27资产校验一致，用户窗口待正常重启。6目标套件、原生46项及20次压力通过，明暗截图已视检；说明和证据见[工作台交互](workspace_interaction.md#r044复现活动栏可见性勾选与菜单留白)。未推送，历史未完成项保持。

2026-09-19 R047最新交付：可选ZIP更新已实现并本机安装，启动多窗口一次公告、立即安装／手动重启；帮助菜单可检查或查看进度。9个目标套件、三档压力及原始Typora40项通过，27资产安装校验一致。正式发布维护release.json，安装复用原事务；中文路径、下载错误、安装互斥和共享Node缓存并发均已回归。方案与证据见[原地更新](workspace_update.md#本轮验证与交付)。用户窗口未重启，本地改动未推送；首次采用者仍需先安装带更新能力的版本。

2026-09-19 R046最新交付：Explorer内容／标题／辅助图标层级已修复并安装，5个目标套件和原始Typora37项检查通过，build/check及安装检查OK。证据见[Explorer层级](explorer_history.md#r046-本轮验证与交付)。用户窗口未重启；未决平台与终端验收保持。

2026-09-19稳定性维护：R039—R045的设计、实现、分类用例和原生交付已集中记录在[稳定性验收](stability_validation_20260919.md)。开始新反馈先按[测试架构](stability_testing.md)登记影响和用例，再实施；[问题索引](stability_issues.md)按根因维护。114套件中110通过、4环境项明确未执行，23资产已安装且校验一致，用户窗口未重启。另一电脑的删除权限/占用与既有终端/跨平台缺口继续保留，不能用本轮本机结果关闭。


## 当前定稿边界

本仓库独立维护 Typora Code；源码、构建、验证与用户说明都在此处，不依赖原知识库。布局以 `59412a2` 的平直工作台为参考，随后依据用户明确要求增加单行顶栏、统一文件图标、clangd 大纲、可见标题同步、边距与可复制链接提示。2026-09-12 新要求重新授权文件菜单补充、资源管理器操作及底部终端面板；按当前需求逐项验证，不沿用此前对应的冻结限制。

2026-09-11 用户明确要求标签采用浏览器／VS Code 的原生拖出建窗和拖入合并，修复外部协议弹窗；此前授权的搜索与侧栏工具排序继续保留。这几项属于最新授权范围。功能、限制与复现方法分别见[搜索性能](search_performance.md)和[拖动与多窗口](drag_and_windows.md)。

顶栏为35px单行，保留原生七类菜单，新增终端菜单，保留居中搜索与阅读历史，窗口按钮使用原生节点。长菜单在顶栏下方滚动，支持 Shift+滚轮。活动栏连续排列并可排序，齿轮打开原生设置。文件图标统一调用 Seti 映射；文件夹按当前主题保留展开箭头。搜索单击在侧栏下方预览，双击或 Enter 打开；资源管理器按 F2 或右键重命名。

Markdown 使用原生渲染和标题大纲；源码使用可保存的 Monaco。C/C++ 大纲只有 clangd 一条解析路径，使用当前内存正文、工程编译数据库和用户解析配置；其他五种语言内置离线解析。没有 VS Code 扩展宿主或完整语言服务移植。

2026-09-13 R034重新授权文件打开后的顶部界面采用VS Code Modern标签：32px行、24px圆角底色、完整名称和悬停关闭；Markdown文件标签统一显示普通文件图标及名称，原生编辑与后台阅读均不加“预览”前缀；修改和固定继续使用文件服务／命令的真实状态。标签下的22px面包屑统一覆盖Markdown标题和源码函数，点击打开层级选择器；目录按需读取，差异两侧按各自模型定位。原生正文按文件身份与编辑模式归属，另一组获焦不清空标题链；选择标题会激活所属组并保留可编辑光标。大纲与导航共用符号提供者和配置，diff工具按钮仍归所属编辑组。来源与生命周期见[标题与函数导航](editor_header.md#r034-标题与函数面包屑)。

## 常驻部署

开始工作先读取[需求设计索引](requirements_design.md)和 Git 忽略的 `.cache/issue_tracking/requirements.md`，每条新需求必须登记编号并关联正式设计小节，实施前补充设计，澄清及实现调整同步更新；不得被后续消息覆盖。设计覆盖与实现进度独立核对，索引中的待设计项不能视为已经完成设计。全项目按职责统一命令、状态、配置与生命周期；各领域服务和界面独立实现，不建立一套囊括所有功能的通用大模块。命令、状态和资源所有者见[工作台架构](workspace_architecture.md)，当前操作见[文件操作](file_operations.md)及[终端配置](terminal_operations.md)。本轮已接通系统文件选择、Explorer 管理操作、保存全部／代码另存为／关闭目录及底部终端；尚未实现的完整 VS Code 生态能力与物理验收缺口继续保留在台账中。

唯一入口是 `resources/window.html` 的 head 静态 CSS 与 defer 脚本。`workspace_core.js` 等待宿主和样式就绪，初始化一次并发布 `ready`；`workbench.js` 等待该核心。文件切换不重建工作台。发布23个常驻资产，另有终端独立运行包；以构建生成的 `dist/SHA256SUMS` 为准。

部署使用 schema 4 事务：预检、完整备份、逐文件摘要验证、失败回滚。`profile.data` 为十六进制 UTF-8 JSON，只调整 `framelessWindow=true`，恢复只还原该字段原值或缺省。保留用户其他设置；不修改 ASAR，不部署第二个社区核心或旧加载器，不部署主进程菜单桥。Windows／UCRT64 使用同一 PowerShell 实现，Linux 使用 Python 实现；Windows 上的 Python 事务测试不代表 Linux 实机权限已验证。

新窗口由现有 `app.openFile(null, {mountFolder, anchor})` 创建，安全的文内片段锚点标识唯一内存频道，已有窗口仅在实际接收落点时握手。`workspace_files` 负责文档快照和基线保护，`workspace_detached_window` 负责双窗确认与生命周期；不保存临时正文，不写工程设置。接收成功也必须复查来源身份，不能按全局路径关闭另一个编辑组的同名标签。

## 保留的正确性与已知缺陷验证

- 保留宿主 `#title-text` 等节点的连接状态。此前删除节点曾中断 `changeCounter.reset`，造成路径变化但正文与标签不同步。
- 底栏 `.footer-item-right` 可嵌套；插入控件用直接子节点作为参照。大纲和缩略图共用 `reading_viewport_bounds`，扣除底栏实际覆盖区。
- 大纲优先完整进入视口的标题，保留12px边缘缓冲；无可读标题时回退正文所属章节。宿主自动／延迟回调共用该判定，同一标题不重写样式，不移动正文焦点。
- 正文边距为单侧0%–24%，保留用户历史值；正文宽度为100%−2×边距。不得把这个恢复扩展为整套外观设置。
- 链接按来源文档解析，目标存在且为普通文件后才切换；失败保留文档、草稿和标签。悬停1秒显示可复制提示，项目内生成地址以根目录为起点；展示仅解码一次，复制保留原链接。
- 搜索以会话身份取消旧扫描、Worker与DOM时间片。普通Git装饰不能阻塞首结果；“仅更改文件”和忽略规则不能为提速被跳过。未完成搜索不能授权替换。
- 标签通过 `tabs/draggable.ts` 原生 HTML DnD 跨 renderer，专属 MIME 只含随机令牌；不得重新改成不能跨窗的 pointer-only 标签拖动。活动栏继续使用 `pointer-drag.ts`。两者复用原图标，释放才提交，取消及卸载清理。
- 跨窗的源码草稿保留真实磁盘基线、格式与位置；未命名或虚拟标签不冒充普通文件。原生dirty Markdown在禁用自动保存时核对完整快照与宿主共享文档状态，目标确认且另一窗口确实持有该文档后释放来源；启用自动保存时前置拒绝。完整撤销历史不迁移。
- 目标确认后，来源标签的正常移除可能先于原生dragend发生。释放期间忽略由该移除引发的取消事件，不能中断自己的已确认移交；卸载、超时与状态复查仍有效。关闭空附窗前后的异步查询必须再次核对真实草稿及加载／保存状态。
- 文件保存、重命名、移动和关闭继续检查草稿、格式、路径及外部冲突。Git写操作仅在临时仓库验证；内部写锁不等于跨外部Git原子保护。旧仓库diff的按钮与迟到操作必须复核仓库身份。
- Graph同节点再次激活关闭详情；切换标签不重建已完成详情；stash辅助提交不独立成节点，引用颜色跟随轨道。工作区新文件直接打开，历史版本比较继续为明确diff动作。
- 提交行提供查看全部改动，历史文件行和历史diff右上角读取所选Git版本。Markdown在独立只读阅读容器渲染，相对链接和图片同样来自该提交；删除文件读取父侧存在的版本，不回退工作区。SCM文件操作拥有独立可见列，不能与长文件名重叠。
- Diff操作在所属编辑组标签行右侧，下面是紧凑版本行。Monaco按编辑组实际宽度在900 CSS px处分界自动切换行内／左右视图，侧栏拖动同样触发；手动行内选择与自动窄屏选项分别保存，切换保留选区和阅读位置。

## 2026-09-12 后续缺陷修复

本机 Shell 识别由 `terminal_profile_detection` 统一提供异步快照，菜单、默认配置、设置和新建／重启共用；查询失败和取消不伪造候选。底栏原生目录、字数详情与语言面板由 `workspace_footer_popups` 统一管理锚点、containment 和层叠，保留原节点及事件；底栏大纲入口已按后续要求取消。`workspace_footer_layout` 为各模块统一组、操作与文本几何，新增控件复用角色；原生工具栏限定于活动 Markdown 阅读区，View 菜单读取实际状态。需求设计分别见[终端](terminal_operations.md#shell-自动识别设计)与[底栏](statusbar_layout.md)，对应 R006.3.1、R012、R013。

R014：`workspace_zoom` 统一视图菜单、命令面板和 `workspace_shortcuts` 的原生窗口缩放；Ctrl+= / Ctrl+- 先于普通模态及终端退出分支处理，图表局部缩放优先。窗口比例、提示与持久化继续归 Typora 所有，见[缩放设计](workspace_zoom.md)。2026-09-12 用户明确取消插件适配，本轮仅修复快捷键。

## 本轮结果与验证边界

各提交对应的构建、完整套件、目标复跑、原生取样与安装记录集中在[反馈复查记录](feedback_review.md)。此前 `629fc6a`、`94b47b2`、`36aef2d` 和 `5622258` 的通过数字均属于各自构建，不在此重复标为当前通过。

在 `enhancements/` 执行：

```sh
npm run build
npm run check
npm run check:ui
```

`check:ui` 由 Node runner 持有输出管道并等待 GUI 测试退出，避免直接启动 GUI 后管道关闭引发 EPIPE；不得绕过 runner 以旧通过记录替代当前验证。它明确登记隔离隐藏 Electron 目标，使用临时工作区。可传具体测试文件；新 `.cjs` 目标必须登记，依赖真实 clangd 或终端的目标保持独立分类。真实 clangd 另运行 `npm run check:clangd`、`npm run check:clangd-ui`。

vendor源码变化同步 `source_manifest.json`，保留上游摘要并更新当前摘要；新增自有文件不伪造上游身份。build更新 `build_inputs.json` 与预构建资产，多人协作必须串行生成共享产物。测试失败先保留证据，再修复；原生验证使用私有桌面、未改ASAR和隔离文档，不切换用户桌面或取真实草稿做破坏性测试。

物理键盘 accelerator 冲突、Linux／UCRT64实机、管理员UAC人工交互仍需分别记录，合成按键和Windows夹具不能替代这些证据。真实安装需带事务备份及安装后只读检查；不强制关闭或刷新用户窗口。

## 研究资料使用边界

[VS Code设计取证](vscode_design_baseline.md)维护固定版本、源文件、数值及采用范围；[工作台矩阵](workbench_parity.md)、[Git矩阵](../enhancements/git_graph_features.md)、[图标映射](icon_mapping.md)、[代码大纲](source_outline.md)分别承担自己的事实。修改功能时同步真实受影响的入口和说明，避免在每个文件复制一份旧验收结果。

研究缓存只放忽略目录，不作为运行依赖。实现必须尊重当前宿主边界，不把VS Code官方说明或单个截图当成Typora API已可用的证据。

R022/R023/R024本轮：搜索重选复用预览的`reveal_match`，定位状态仍由所选匹配持有；共享hover把触发/焦点锚点与布局避让区分开，提供紧凑角色、指示角、悬停组与统一尺寸更新，Git只指定整个列表并填充内容。Graph设置沿用仓库原有存储；配置核对与未实现差异集中在[Graph配置](git_graph_configuration.md)。验收及安装状态见本轮反馈记录。

## 2026-09-13 日常文件与终端操作

终端列表宽度、组内分隔条及同窗列表拖放由共享几何和生命周期管理；列表行移动保留PTY与xterm对象，见[终端设计](terminal_operations.md#r0061-分隔条与列表拖放)。

`workspace_file_clipboard` 持有窗口内剪切意图，平台适配器只提供文件列表与版本，Explorer不再持有第二份剪贴板。外部复制在独立源边界读取，并沿用目标工作区的独占创建与回退；内部剪切经 `workspace_files.move_file` 保留草稿和模型。正常复制只读磁盘字节。测试替身仅替换系统端口，不替换文档事务，见[文件剪贴板设计与验收边界](file_operations.md#r009-系统文件剪贴板)。

## 2026-09-13 Markdown 字体颜色

R026在原生选区右键菜单增加“字体颜色…”。`markdown_color_menu` 管理入口及公共对话框；`markdown_color_native` 核对选区身份并使用 Typora 原生 replace/cursor/attr 撤销事务，表格的 `userText` 缓存与单元格一起更新。`markdown_text_color` 只处理规范色彩标签和源文本区间；`markdown_color_theme` 只设置阅读容器变量，不重写正文。

终端和字体颜色复用 `workspace_theme` 的实际背景合成与主题事件。升级宿主后需重新验证选区、局部修改、撤销及表格保存，不能仅依据 DOM 有颜色判断通过。操作、存储边界和适配算法见[字体颜色设计](markdown_text_color.md#r026)，交付证据见[反馈记录](feedback_review.md)。

## 2026-09-13 SCM Graph 操作

2026-09-14 R027刷新采用用户指定的首次打开立即读取、持续可见30分钟检查；隐藏暂停，聚焦和重复打开只处理到期或已记录的变更。原生Markdown与源码的成功保存共用完成事件，1000ms合并及5000ms完成间隔；Git命令和手动刷新及时执行。控制器统一快照，底栏与当前仓库行订阅，无变化不重建列表；可变版本比较与切库布局仍更新。上游实际事件规则、自动fetch区别及本轮交付证据分别见[刷新设计](git_scm_actions.md#2026-09-14-git刷新触发与无变化更新)和[反馈记录](feedback_review.md)，不得重新加入独立8秒轮询。

R027 将工具栏、仓库列表、文件／提交菜单与工作树管理接入既有 Git 操作服务。`git_scm_data` 唯一解析上游／基线和工作树；`git_scm_toolbar` 共享按钮／更多／快捷键状态；非当前仓库只读摘要先完成切换再启用写操作。切库、刷新及销毁使旧菜单、预览和异步读取失效。公开行为与剩余配置／扩展边界见[SCM 操作设计](git_scm_actions.md#r027)，本轮验证与交付状态见[反馈记录](feedback_review.md)。

## 2026-09-13 文件行点击与响应

R028按用户同日纠正：资源管理器文件夹整行单击展开／折叠，快速连点逐次响应，双击不重命名；目录改名保留 F2 与菜单，已选中文件双击改名保持。搜索文件组整行单击开关，双击仍打开。`workspace_explorer` 保持可见行与名称节点身份，动画帧合并刷新；展开状态先更新，慢读复用同一请求，迟到结果不改变用户最后一次开关。只有文件记录浏览器双击序列与手势开始前的选择；目录不经过双击过滤或展开状态回退，文件改名继续经过已有文档事务。`workspace_search` 持有分组开关，动作按钮、键盘和下方预览共用原状态；具体行为与回归见[文件行设计](file_operations.md#r028-文件行的单击双击与加载反馈)。


## 2026-09-13 顶栏搜索悬停

R029保留顶栏搜索的公共action角色，仅在 `workspace_titlebar.css` 中覆写中性悬停底色和原顶栏前景。不要将该入口改为none或增加单独指针监听，否则会丢失共同键盘焦点与清理规则。前景引用 `--tc-title-fg`；自定义属性直接写inherit会触发CSS宽泛关键字语义，不能把它当作颜色值传给下游。职责、固定参考和回归见[搜索入口设计](workspace_interaction.md#r029-顶栏搜索入口的悬停外观)。

## 2026-09-13 文档标签右键菜单

R030将标签菜单与快捷键收敛到 `workspace_editor_actions`，后台目标不切换活动文档。批量关闭限定目标组并保留固定项，逐项等待保存／取消；临时预览的保持打开与固定到组首分别管理。核心布局只提供分屏和叶子移动，正文与保存仍由文件服务持有，跨窗口仍走既有确认通道。

同窗源码副本共享模型、保存基线和格式，另存为更新全部副本路径；最后一个副本关闭才释放模型。Markdown 分屏读取原生内存，编辑事件刷新阅读副本；同一已加载文档重激活不再等待第二次 `file:open`，不同路径仍等待正确加载事件。原生保存等实际I/O完成，已命名文档放弃修改重新加载磁盘；未命名草稿仅保存或取消。操作与扩展差异见[标签菜单](editor_tab_menu.md)，当前验证与安装见[反馈记录](feedback_review.md#2026-09-13-文档标签右键菜单)。


## R031 图片与图表查看器

图片阅读层由 `reading_image_viewer` 绑定原生正文和历史阅读器；`reading_media_entry`统一管理图片与Mermaid外侧右上方的悬停浮层、裁剪和入口事件；不插入占位或包装、不修改正文间距，按钮随原图滚动。单击保留原生选中、双击图片打开；`reading_media_viewer`统一管理图片／图表副本、缩放拖动、主题和焦点。正文切换、来源重建及卸载均清理；Mermaid只选择真正的图表SVG，阅读按钮捕获事件避免首次聚焦重建造成失效。见[设计](reading_media_viewer.md)。

## 2026-09-13 提交节点文本布局

R022追加反馈按固定 VS Code 1.137.0的 IconLabel / SCM布局实现，见[提交标题与作者](workspace_interaction.md#r022-提交标题与作者连续排列)。`git_scm_history`在引用存在时也渲染作者；`git_graph.css`只对共同文本区域做末端省略，引用描述最大100px。没有增加状态存储、悬停计时器或 Git 业务路径。历史文件行和中央 Graph 独立列保持原职责。构建、完整检查、16组提交文本场景与5个相关UI目标及原生几何验证结果见对应反馈记录。


## 2026-09-13 Esc恢复与媒体悬停入口

R032由 `workspace_focus` 统一保存深层活动节点、输入框选择方向和原生正文选区；核心包与工作台包通过窗口级唯一退出栈管理临时界面。Esc按下／释放配对，只关闭最上层，子菜单逐级返回；外部点击、来源切换、销毁或失效目标不抢焦点。核心输入框、命令选择器、文件搜索、菜单、对话框和媒体查看器共用这一规则，普通编辑器和终端自己的Esc仍由原领域处理。见[退出设计](workspace_interaction.md#r032-esc退出与原操作恢复)。

R031以用户最后的“图像外侧右上角，悬停时覆盖显示”为准。共享媒体入口默认隐藏，图像／图表或按钮获得悬停、键盘焦点时显示。用户随后明确不接受预留工具行，当前已删除占位；悬停浮层不参与文档排版，详见下方R033与R031追加修复。图片单击选中和双击放大保持。完整构建／检查、56个UI目标复查与隔离Typora29项及安装状态见[本轮反馈](feedback_review.md#2026-09-13-esc恢复与媒体悬停入口)。


## 2026-09-13 弹窗外部关闭与媒体入口取消占位

R033扩展同一窗口级退出栈，统一外部pointerdown、宿主mousedown、焦点离开和Esc。一次指针／兼容鼠标手势只取消当前层；菜单族内部移动不关闭，遮罩与外部点击不确认也不抢回原焦点，Esc按原身份恢复。核心命令框与菜单、文件搜索、顶栏菜单、活动栏和工作台对话框复用同一取消入口；系统颜色选择器不会因窗口失焦取消所属设置。见[关闭设计](workspace_interaction.md#r033-弹窗外部关闭与焦点转移)。

R031删除流内工具行、原生图片附加class及普通图片包装。浮层仅按原图矩形定位，命中桥限于按钮宽度，图文DOM和几何不随挂载或悬停改变。原生Mermaid横向滚动壳的overflow-y:hidden只约束来源可见性；外侧按钮可越过未滚动的壳，真实阅读滚动区仍限制完整入口。已增加该宿主结构的回归，避免为裁剪问题再次添加占位。见[媒体设计](reading_media_viewer.md)。

完整检查、57个UI目标、最终媒体46项及Mermaid交互／核心目标复查、原生Typora1.14.10的31项验证及安装结果统一见[本轮反馈](feedback_review.md#2026-09-13-弹窗外部关闭与媒体入口取消占位)。原有18项R009系统文件剪贴板差量保持独立，尚需原生系统剪贴板验收；其他平台和完整工作区能力继续按台账，不由此次修复关闭。


## 2026-09-13 SCM覆盖分隔条

R035删除更改与提交图间7px占位轨道；SCM保留两条内容轨道，以绝对定位网格项让原分隔条对齐真实交界，包含22px标题最小高度的边界。4px命中区、300ms悬停及即时拖动／键盘反馈由现有样式管理，不新增观察器或比例状态。设计见[分隔条](workspace_interaction.md#r035-更改与提交图覆盖式分隔条)，完整检查、6个UI目标和隔离原生18项结果见[反馈记录](feedback_review.md#2026-09-13-scm分隔条覆盖显示)。

## 2026-09-13 标题函数导航与详情分支全名

R034 的 `workspace_breadcrumbs` 按编辑组组织22px导航；`workspace_document_symbols` 按模型、版本和解析环境共享快照，代码大纲与面包屑订阅同一结果，C/C++仍只使用clangd。原生Markdown共用当前标题判定；源码Markdown词法解析排除代码围栏，CRLF位置保留模型偏移。选择器复用公共关闭和焦点恢复；用户／工作区及允许的语言覆盖集中在 `workspace_breadcrumbs_settings`。默认值、快捷键、失败和销毁边界见[权威设计](editor_header.md#r034-标题与函数面包屑)。

R022 的列表与详情共用徽章形状、图标和配色，按上下文声明宽高规则：提交列表单行末端省略，详情卡显示完整分支名称，极长名称换行撑高，不沿用列表100px限宽。见[详情分支全名](workspace_interaction.md#r022-长分支徽章完整名称)。本轮构建、UI、原生验证与安装证据统一见[反馈记录](feedback_review.md#2026-09-13-标题函数导航与详情分支全名)；原18项R009未提交差量继续独立保护。

## 2026-09-13 文件剪贴板阶段实现入库

R009原先保留的差量已整理为可独立审查的阶段提交。Windows写入快照在持锁期间获得；Linux使用自身唯一写入版本并保留系统剪贴板内容；服务的旧刷新失败不能取消后来建立的剪切。对应源码、构建与本次验证记录共同入库，后续无需再为其他修复反复隔离这批未提交代码。

Windows隔离窗口站创建仍返回Win32 5，Linux原生互通仍待验证，因此R009平台验收未关闭，本次未安装。测试与下一步边界见[本次复核](feedback_review.md#2026-09-13-系统文件剪贴板阶段提交复核)，不得把阶段提交或端口替身通过等同于系统互通通过。

## 2026-09-13 明暗主题选中反馈

R020选中背景与前景由共同交互层成对提供，现有主题观察链驱动；Explorer与Graph／SCM引用同一配色，选中行hover保持优先级，行尾按钮仍有独立反馈。回归必须使用实际主题传播，不能在夹具手工注入待测选中色。当前安装为已验收`7885b59`脚本加本轮新workspace.css；源码HEAD中的R009系统剪贴板阶段实现仍待原生互通验收，未随样式部署。当前验证、事务安装和证据见[反馈记录](feedback_review.md#2026-09-13-选中行随明暗主题切换)。

## 2026-09-13 底栏窗口缩放控件

R014由workspace_zoom_status只读观察宿主实际窗口比例，按钮、菜单和快捷键共用workspace_zoom命令；底栏布局、可交互hover和退出焦点复用共同所有者。shared hover支持显式打开、上方优先定位和带焦点恢复保护的关闭，Esc不会因为恢复入口焦点再次弹出。图标及默认布局来源见[设计](workspace_zoom.md#r014-底栏缩放入口)，验证及安装见[反馈](feedback_review.md#2026-09-13-底栏窗口缩放入口)。

当前安装脚本基线为7885b59加本轮R014及共享hover修复，CSS包含既有明暗选中与媒体入口不透明修复。R009系统剪贴板阶段代码继续保留在源码及dist中，原生平台验收未关闭，因此不包含在本次安装候选中。用户窗口未重启，正常重启后加载新入口。

## 2026-09-13 更改标题操作入口

R027更改标题按提交、刷新、Git Graph、更多组织；UI仍由git_source_control拥有，读取/写入状态通过git_graph_panel统一通知，Git事务及Graph编辑入口复用原有服务。按钮不误触折叠；空消息提交显式展开并聚焦。图标使用已有官方Codicons，不增加扩展依赖。设计见[标题入口](git_scm_actions.md#2026-09-13-更改标题操作补齐)，本轮回归与安装见[反馈](feedback_review.md#2026-09-13-更改标题提交刷新与graph入口)。

本次已安装候选为前轮已验收窗口缩放版本加本轮三份SCM源码，只有workbench.js、workspace.css及清单变化。源码HEAD及其dist继续包含R009阶段实现，原生互通未验收，因此该阶段代码仍不在安装候选中。用户窗口保持，正常重启后加载新入口。

## 2026-09-14 集成Git与编辑器交付

R027 普通远端快捷操作、分支多选和准确范围放弃，R034 比较页及普通文件顶栏，R006.7 终端输入边界、H001.1 顶栏右键配置已统一构建、验证并本机安装。命令、状态和配置分别由 Git 控制器、文档服务及共享设置所有者管理；对话框完整外点手势、焦点恢复和菜单主题分隔线由公共控件管理。正式设计见 [SCM操作](git_scm_actions.md)、[文件顶部](editor_header.md)、[终端输入](terminal_operations.md#r0067-输入法与按键所有权)及[顶栏配置](workspace_interaction.md#h0011-顶栏右键显示配置)。

当前安装为此前已验收基线叠加本轮明确文件，更新三份运行资产及摘要；业务与受影响界面回归、原生综合220项及最终明暗菜单补测通过。安装检查和五项用户环境摘要保护通过，保存后正常重启 Typora 加载；没有强制关闭用户窗口。完整源码构建仍含已提交的 R009 系统剪贴板阶段，安装候选继续排除该尚未完成原生互通验收的阶段。物理搜狗 Shift 切换仍需按实际输入法版本验证；其他会话的资源管理器／自动保存历史、安装日志不纳入本次候选。细节、失败与复跑边界见[本轮反馈](feedback_review.md#2026-09-14-git操作文件功能栏与顶栏终端统一交付)。

后续功能开发采用独立 develop-typora-code 技能及本项目 AGENTS，先核对上游命令、目标解析、有效配置与回归，不再给普通快捷动作附加通用预览／执行确认。交付完成的源码、测试、设计和对应构建及时提交；不把安装、运行窗口加载和原生验证混为一种状态。


## 2026-09-14 全部改动集成基线

最新用户授权将当前所有并行功能统一纳入候选，替代上文部分安装的排除范围。Git快捷操作、文件功能栏、顶栏菜单、终端、文件剪贴板、Explorer分区、保存历史与安装日志已经收拢；全量检查及两项定向复跑、原生保存历史73项完成，结果与未完成平台边界见[本次反馈](feedback_review.md#2026-09-14-全部并行修改的集成基线)。先提交本批完整源码与构建，再单独处理 Git 动态执行状态；最终全量安装状态另记，不沿用早期候选摘要。

共享职责已按真实调用关系收敛：Git操作由控制器和事务服务所有；自动保存调度／原生保存适配／历史存储分层；文件与历史视图共用文档服务；菜单、焦点和主题交互由公共层维护。异步保存失败不重复重试同一草稿，关闭历史比较取消迟到恢复，Explorer标题菜单只由一个处理器响应。

## 2026-09-14 最终全量交付

集成基线已提交为 `ccf0f24`。随后新增 `git_operation_progress` 由Git控制器唯一管理实际活动，`git_progress_view`只呈现覆盖进度，底栏与仓库行订阅同一状态；所有写入口复用 `run_operation`，不自行维护另一份忙碌状态。关闭进度设置只影响显示，操作与刷新仍按真实生命周期结束。历史比较采用已有源码视图的flex尺寸契约，消除只占部分编辑组的空白。

最终全部资产已安装并检查OK，包含早期暂未安装的剪贴板、Explorer分区、保存历史及安装日志。完整业务回归、64个UI目标（60首次通过、4修正夹具后通过）、最终同一构建的原生综合241项与保存历史74项已记录；23项安装资产与候选一致，用户配置及原ASAR保持。保存后正常重启Typora加载，当前用户窗口没有被关闭。当前系统剪贴板原生互通、物理搜狗和非Windows的剩余验收边界及精确证据见[本次交付](feedback_review.md#2026-09-14-全量安装与git动态进度收尾)，不能引用早期部分候选代替最终安装状态。


## 2026-09-14 Shift拼音与分区反馈后续交付

在上述全量基线之上修复R006.7已提交拼音被旧textarea起点截断、R037三个分区标题缩进不齐、R027系统减少动画下活动条静止。终端表面持有公开DOM适配的生命周期，xterm仍是唯一发送方；分区几何与Git进度分别沿用自身共享呈现层，没有增加业务状态副本。

完整业务检查、9个相关UI目标与原始Typora私有窗口77项通过，同一23项资产已本机安装并检查OK。用户窗口保留，正常重启后加载；物理搜狗输入仍须实际键盘确认。源码、设计、测试和构建一起提交，并按本轮明确授权普通推送；证据、摘要、原生夹具复跑和剩余边界见[本轮反馈](feedback_review.md#2026-09-14-shift候选提交与侧栏标题和移动进度)。

## 2026-09-14 终端捕获归属后续交付

在`1a427fa`之上补齐全局输入法捕获与底部终端焦点归属，复用无状态`workspace_keyboard`；Graph不能因中央叶子仍活动而消费终端输入，全局搜索不能抢终端查找。keyup消费记录清理后让行IME/229，组合文本适配器与PTY发送路径不变。完整业务检查、12个相关UI目标、原始Typora同夹具41项通过，资产已安装检查OK；正常重启后加载。用户关于纯Shift丢拼音的物理搜狗验收仍开放，不能把本轮真实捕获缺陷等同该物理根因；证据和提交／安装范围见[本轮反馈](feedback_review.md#2026-09-14-终端全局快捷键捕获归属)。

## 2026-09-14 Markdown输入对照后续交付

在`3d860be`之上确认无composition事件的Shift直接提交被xterm已输入标记过滤：原生Markdown保留`pinx`，旧终端仅发送`x`。现有终端输入适配器对非组合纯Shift按下隔离到xterm的传播，保留浏览器默认、input、keyup、组合态及其他修饰键。完整业务检查、组合输入13组及另外5个UI目标通过；原始Typora同序列旧版45/49、新版49/49，真实Markdown对照和私有文件保护通过。已安装同一23项资产并检查OK，正常重启后加载。物理搜狗与并发输入队列竞态调查独立保留，具体证据、交付状态见[本轮反馈](feedback_review.md#2026-09-14-对照markdown修复shift直接提交)。
