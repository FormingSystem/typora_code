# 稳定性问题索引

按领域和根因检索，重复复现更新同一编号。需求设计见[测试架构](stability_testing.md)，本轮运行证据见[验收记录](stability_validation_20260919.md)。本表记录缺陷结论与边界；当前交付进度以本地台账为准，避免每份文档再建一套完成清单。

| 问题 | 需求 | 类型/严重度 | 复现与实际根因 | 方案与关联验证 |
| --- | --- | --- | --- | --- |
| BUG-plugins-001 管理和配置入口难发现 | R060 | 可用性/P2 | 初始管理页只在帮助菜单，齿轮仅原生偏好；迁入侧栏时宿主header/input规则又导致标题覆盖和隐藏控件外露 | 左侧扩展/快捷键/视图统一SidebarPanel，齿轮与插件行共用SettingTab，停用清理；标题语义与hidden在独立区域隔离。TC-community-plugins-ui/native和活动栏/偏好/快捷键/顶栏回归 |
| BUG-scrollbar-002 空闲滑块常驻 | R049.1 | 呈现/P2 | 共享CSS仅统一圆角和颜色，没有按交互状态收起原生滑块；不是单个Explorer样式缺陷 | 公共显隐服务按实际overflow所有者登记，500ms空闲后800ms淡出，保留原生输入与第三方所有者；TC-scrollbar-fade-unit/stress/ui/native，scrollbar_fade_20260920.json |
| BUG-files-001 最近目录遗漏 | R040 | 功能/P1 | 打开新目录只调用setMountFolder，未像宿主onRootChanged写入addRecentFolder | await宿主历史；取消/无效/迟到不记入。TC-quality-implementation、TC-system-native-stability |
| BUG-workspace-005 切目录残留旧工作区 | R040.1 | 状态生命周期/P1 | 只改挂载路径，旧标签仍有效；Git优先活动标签且未订阅工作区切换；搜索、固定时间线、终端等须一并失效 | 设计见workspace_switch.md；统一预检/保存取消/切换代次，逐领域清理与目标刷新；补齐旧Git订阅与迟到导航清理。TC-workspace-switch-ui/context/history-stress/native，证据workspace_switch_20260919.json |
| BUG-files-002 回收无效果 | R041 | 兼容/P1 | 用户在另一电脑确认删除无效果；本地调用renderer shell，未对齐原生主进程布尔返回契约。账户权限仍是未证实的跨机因素 | 文件/Git统一JSBridge回收，false/异常/仍存在均失败，禁止永久删除后备；尊重原生删除警告配置。TC-quality-001、Explorer与原生稳定性；另一电脑实际验收待补 |
| BUG-files-003 改后缀未更新编辑器 | R042 | 功能/P1 | 111→1111.md只迁移路径，源模型语言和视图未重分类 | 保持模型、撤销及dirty；clean自动原生Markdown，dirty保存成功后切换。TC-files-011、TC-system-native-stability |
| BUG-reading-001 章节被顶部遮挡 | R043 | 几何/P1 | 原生scrollAdjust显式margin以窗口顶端计算，未扣工作台顶部；共同视口原先仅处理底栏 | 统一读取顶部相交遮挡，适配显式宿主跳转；保留隐式编辑滚动。TC-workspace-020与原生两主题，关联大纲/面包屑/缩略图回归 |
| BUG-workspace-001 功能菜单图标错列 | R044 | 几何/P2 | fa负偏移；SVG没有槽；活动图标外壳克隆后带入未限定范围的24px规则；原生CSS覆盖anchor布局 | 菜单图标及外壳16px，独立文字列；24px仅限真实活动按钮。实际字体/SVG、间距/行盒验证：TC-workspace-004、TC-system-native-stability/stress |
| BUG-git-001 普通动作多次确认 | R041 | 功能/P2 | checkout等仍进入通用“预览→执行”表单 | 已知无参数动作直接进入共同事务，必要参数一次提交；风险文案执行前可见，重复/迟到/切库不执行。TC-git-016、Graph interaction、SCM actions |
| BUG-git-002 底栏状态信息丢失 | R045 | 功能/P2 | 暂存/未暂存都被概括为dirty星号；分离HEAD和同步数字表现不一致 | 从已有快照计算*、+、!，分离HEAD8位及官方git-commit，N↓ N↑。TC-quality-state-stress、TC-workspace-012；上游保护分支复合图标仍保留差异 |
| BUG-quality-001 需求到证据脱节 | R039 | 测试设施/P1 | 旧测试分散，缺少跨层分类和当次运行身份 | 稳定用例目录、分类运行器、失败/未执行状态及源/资产/日志摘要；六类代表性测试入口与文档闭环 |
| BUG-quality-002 跨PowerShell模块污染 | R039 | 测试设施/P1 | PS7→Node→PS5.1继承PS7模块路径，Get-FileHash未加载，三个安装夹具失败；直接PS启动无此问题 | 仅对子进程清理PSModulePath，让目标引擎重建标准路径；不改用户环境/权限。安装、卸载、日志系统用例重跑 |
| BUG-quality-003 过早判定表单完成 | R041 | 测试设施/P2 | 单次提交先禁用按钮再异步准备；旧测试将“按钮禁用且尚未写入”误认为完成，HEAD断言偶发失败 | 等待实际提交结果及事务结束，再核对HEAD与正文；不增加固定睡眠掩盖竞态。TC-git-010 |
| BUG-quality-004 原生夹具缺少依赖 | R039 | 测试设施/P2 | 新隔离复制器误排除window.html实际引用的appsrc，引发初始化超时 | 完整复制宿主依赖，独立Git根阻止发现开发仓库；保留首次失败、ASAR/资产摘要和截图 |
| BUG-quality-005 几何夹具未加载样式 | R044 | 测试设施/P2 | 原核心生命周期测试只装JS，新增几何断言未装候选CSS而失败 | 加载候选workspace_core.css和Git领域样式再量实际图标；原生两主题结果作为独立证据。TC-workspace-004 |
| BUG-files-004 Explorer内容与辅助操作主次 | R046 | 呈现/P2 | 文件列表继承正文等宽字体，标题又使用主题字体，分区图标未显式隔离字形与按钮盒模型 | 工作台13px/400内容、12px/600标题、11px说明，16px辅助字形；TC-files-009红绿、013文件图标及原始宿主两主题；方案见explorer_history.md的R046节 |
| BUG-quality-006 原生几何与截图不同步 | R046 | 测试设施/P2 | 面板未挂载即读取导致失败；仅量矩形会漏过首帧空白及测试CSS强改宽度造成的编辑区遮挡 | 等待面板可见命中与字体就绪，宽度经真实分隔条键盘入口，同时断言相邻编辑区；最终四张截图复核，前序通过不冒充完整视觉证据 |

| BUG-update-001 系统PowerShell解压脚本解析失败 | R047 | 兼容/P1 | 含中文的UTF-8无BOM脚本在PS7解析通过，但系统PS5.1按本地编码读取后报MissingCatchOrFinally；真实ZIP系统用例发现 | 辅助脚本保留UTF-8 BOM，构建保留字节；跨进程输出显式UTF-8，避免中文可执行路径被误解码；TC-update-install真实5.1执行及中文进程／用户路径回归 |
| BUG-update-002 下载写盘失败被报完成 | R047 | 正确性/P1 | WriteStream.end的回调未检查错误，目标已存在时可能先返回成功；随后error被已完成状态忽略 | 只在finish事件确认成功，error先失败；TC-update-protocol覆盖EEXIST且旧文件字节不变、断流／取消／超时 |

| BUG-update-003 共享运行时缓存并发覆盖 | R047 | 并发/P1 | 两个真实安装夹具使用独立用户目录，但prepare_typora_node无条件解压同一缓存；另一事务使用缓存Node时ExtractToFile报文件占用 | 缓存位置／版本／架构互斥；有效文件摘要匹配即复用；TC-update-install增加运行中的缓存Node，TC-delivery-002并发回归；首次失败报告保留 |

| BUG-workspace-002 活动栏勾选遗漏与空白 | R044、R018 | 功能／几何/P2 | 菜单未传button.visible；统一state-off引入8px伪元素，原生24px左padding与flex槽叠加；前次用例只测图标未测状态 | 共享可勾选项、单一26px勾选列、独立菜单自有几何；核心UI与原生明暗／切换／持久化回归 |

| BUG-git-003 分支入口仍是普通菜单 | R048、R027、R045 | 功能/P2 | 底栏和SCM各自枚举引用，缺少搜索/标签/创建来源，远端重复要求填写已知目标 | 共享检出选择器与完整引用服务；远端自动跟踪，事务/草稿保护；TC-git-checkout-service/ui/stress及原生56项；设计见git_branch_checkout.md |
| BUG-workspace-003 检出详情被共享列表居中 | R048 | 几何/P2 | 静态样式顺序让普通列表align-items覆盖检出双行规则，单量行高未发现文字偏移 | 显式检出双行作用域和文字起点断言；原生明暗截图复核；保留首次失败与后续证据 |
| BUG-quality-007 原生注入错误只表现为超时 | R048 | 测试设施/P2 | 夹具块内rows重复声明使脚本无法执行，旧Esc夹具只发送keydown | 准备前node --check；原生按共同退出栈发送完整按下/释放；失败报告保留，最终56项通过 |

来源日期均为2026-09-19；用户截图分别用于最近目录、面包屑和菜单问题，跨机删除与后缀场景来自用户文字。过程发现BUG-git与BUG-quality不从图片推断账户或主机信息。

| 问题 | 需求 | 分类 | 确认根因 | 处理与验证 |
| --- | --- | --- | --- | --- |
| BUG-workspace-004 滚动条样式分散 | R049 | 呈现/P2 | 原生直角与各面板独立宽度/颜色混用；标准thin/color会绕过WebKit绘制 | 共享workspace_scrollbars，原生/Monaco/xterm/缩略图集中适配；TC-workspace-scrollbars及三档压力、隔离宿主明暗截图 |
| BUG-quality-008 滚动夹具指针坐标不随缩放 | R049 | 测试设施/P2 | Electron滚轮delta方向写反，125%时滑块坐标未乘zoom；停留滑块的指针又污染下一主题默认颜色断言 | 使用真实输入约定并在三态检查间移开指针，保留首次失败与后续通过报告；不修改产品事件来迁就测试 |

本轮R050追加：

| 问题 | 需求 | 分类 | 确认根因 | 处理与验证 |
| --- | --- | --- | --- | --- |
| BUG-git-004 列边界反向且哈希无法调整 | R050 | 交互/P2 | 单列增宽挤压左侧弹性说明列，表头最右柄无法控制用户指向的作者/哈希边界 | 相邻可见列共用调整事务，说明最小宽与实际剩余空间分开；TC-git-columns/math/stress与原生明暗 |
| BUG-git-005 初始分隔条无障碍值为零 | R050 | 可访问性/P2 | 绑定时表头尚未进入列表；标签隐藏/恢复和侧栏宽度变化后数值未同步 | 在表头与行挂载后绑定，观察表头布局，隐藏时不发布零值；原生Night复现及最终重绘/销毁压力证据保留 |
| BUG-quality-009 Electron方向键名称误用 | R050 | 测试设施/P2 | 隐藏窗口夹具把DOM的ArrowLeft/ArrowRight作为Electron键码，真实方向输入没有命中处理器 | 发送Left/Right，DOM合成事件仍用ArrowLeft/ArrowRight；三次失败保留，不修改产品键盘规则来迁就夹具 |

本轮R034追加：

| 问题 | 需求 | 分类 | 确认根因 | 处理与验证 |
| --- | --- | --- | --- | --- |
| BUG-workspace-002 导航与正文边界缺失 | R034 | 呈现/P2 | 共享面包屑border:0遗漏固定上游浅色底边，原有测试仅核对行高和交互 | 统一1px主题绘制层，保留22px行盒及点击；TC-workspace-003与原生80项、明暗截图复验 |

## 未关闭的环境与产品边界

- BUG-files-002：已建立主进程回收契约并在本机原始Typora验证真实临时文件/目录；另一电脑的版本、权限、文件占用和回收站支持仍需该环境记录。失败保留文件，不能绕权限或降级永久删除。
- BUG-git-002：本轮处理已有底栏信息；VS Code保护分支/特殊状态复合图标尚未映射，不宣称所有Git状态外观完全等价。
- 原有终端物理输入法、跨平台剪贴板、ARM64/Linux原生桌面、UCRT64交付等缺口继续保留原需求号，不因本轮单元或隐藏窗口通过关闭。
- 新测试架构覆盖所有现有脚本的归属，不代表每一个历史需求都已有全部六类用例。新增需求按影响分析增加有价值的用例，缺口显式记录，避免为了填格子复制无效测试。

## 新问题记录模板

使用`BUG-领域-序号`并关联既有需求号；记录来源/最近复现日期、环境和候选摘要、最短复现、预期/实际、严重度、确认/假设根因、影响服务与状态所有者、权威方案章节、用例号、失败与修复后运行入口、安装和加载边界。未证实的根因写“待核对”，不能根据弹窗或截图推断权限。相同根因追加复现环境和证据，不按运行日期新建流水账；有独立原因才分号。

## R052：空占位、原生缓存与文档生命周期混用

2026-09-19。分类：状态所有权/宿主适配，影响Explorer、阅读、时间线和当前文档操作。core.empty误入列表导致New tab无法关完；缓存同路径重开跳过磁盘，删除后宿主生成隐藏dirty草稿。共同身份规则与显式原生释放修复，设计见[空编辑区](explorer_history.md#r052-空编辑区与最后一个文件)。连续操作分别复现旧实现缺陷，不能仅以单次删除成功关闭问题。回收后发生新输入继续保留，重置失败不清除非空内容。测试与交付结果统一关联当次证据，不另维护完成清单。

## R040.2：工作区文件会话缺失及遍历提前退出

2026-09-20关联回归记录：首次完整check在1000档会话磁盘替换出现一次Windows EPERM，独立重跑与后续两次完整check通过，未修改会话实现。保留`.cache/hover_check.log`与重跑日志，瞬时文件占用原因未确认；不能把复跑通过写成已修复该环境问题。

2026-09-19。分类：状态隔离/持久化/宿主契约。旧切换只清空不恢复；原生eachLeaves逆序且truthy终止，Array.push返回值使同组只处理一项，替身forEach掩盖该缺陷。修复显式void遍历、按标签顺序记录和按根原子保存；恢复读取原生偏好页getExtraOption（编辑器loadAll不含启动配置）。原生多文件往返与第二进程验证，失败报告保留在本地证据；设计见[工作区会话](workspace_switch.md#r0402-按工作区恢复上次打开的文件)，验证结果见对应正式证据。
## R053：提交正文语义与样式作用域

2026-09-19反馈，2026-09-20复核。原始减号和空行来自纯文本赋值；按固定VS Code Git hover逐换行分段后使用共同安全Markdown正文。首轮marked接口误用由专项UI发现；原生随后发现全CSS提取把Shadow样式放入head，导致原生缩进40px，夹具直接CSS字符串却通过。构建增加显式Shadow作用域并补正式构建首帧断言；问题依据及最终证据关联R053，不把夹具通过等同原生已通过。

## R054：启动与功能区切换时序

2026-09-20，权威方案见[启动稳定性](startup_stability.md)。按原因归档，不逐次运行新增问题。

| 问题 | 分类／原因 | 处理及验证边界 |
| --- | --- | --- |
| BUG-startup-001 原生布局闪现后替换 | 呈现/P2：静态CSS有了，但增强布局依赖的DOM尚未挂载；原测试只加载CSS并检查最终画面 | head控制一次性首次展示；就绪释放、失败/15秒超时恢复原生；真实宿主旧候选已复现，过程样本关联本轮报告 |
| BUG-sidebar-001 换面板先收起整个侧栏 | 生命周期/P2：Sidebar.switch每次调用hideSidebar再showSidebar，重复show还会再次onshow | 先解析目标，移交唯一面板所有者；重复show幂等，移除活动面板清理所有者；单元20/100/1000及真实四面板回归 |
| PERF-startup-001 界面注册与高亮串联 | 性能/P2：语法作为界面前置依赖；Git及浏览器注册同一任务合计约67ms | 高亮移到布局之后；两次注册间交还事件循环并复查取消；记录三阶段测量而非用总耗时推断阻塞 |
| PERF-startup-002 残余启动长任务 | 待归因：部分50ms以上任务发生在自定义注册前或数据首次加载时；并发测试增加波动 | 三次独立运行仍有50–67ms任务，其中一次browser-bind 52ms。专属宿主的inspector.Session返回“未初始化V8 Inspector”；保留错误和阶段时间线，不开生产调试或改ASAR，后续需可观测宿主/受控性能环境继续归因，不能宣称所有卡顿已消除 |

本轮夹具初始失败也保留：单元执行环境漏设CommonJS module；新HTML夹具漏UTF-8声明导致加载提示乱码。分别修正测试环境，不以改产品输出迁就测试。旧候选原生失败属于产品缺陷，和这两项测试设施问题分开记录。

## R057：社区兼容与生命周期

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-plugins-001 旧发行构造函数失败 | 社区ES5代码调用Parent.apply，编译后核心为ES类；公共ABI用Reflect.construct桥接同一类与原型 | 真实Codeblock Copy Button 1.2.0在原始宿主安装/启停，未恢复第二核心 |
| BUG-plugins-002 停用残留按钮 | 后处理器只注销处理函数，没有清除已生成按钮；注册器统一清理按钮和预览，异常构造/onload也清理 | 原始宿主20次启停、构造失败与设置/命令/底栏夹具 |
| BUG-plugins-003 高频原子替换EPERM | Windows文件临时占用原因未归因；1000轮出现替换失败。只对占用类错误作有界异步重试，保留旧配置 | 20/100/1000通过，持续拒绝注入证明原配置与运行状态保留；不能外推R040.2已修复 |
| TEST-plugins-001 旧夹具契约过期 | 独立启动夹具把ready提前兑现、漏复制新插件服务资产；更新到同一就绪契约和正式资产 | 启动/切库完整测试恢复通过；不降低异常断言 |
| TEST-update-001 旧发行夹具版本重复 | 给首版本减1与真实历史第二项碰撞，违反严格递减规则；改用真实历史尾部构造旧版本 | ZIP与真实安装事务通过；未放宽发布校验 |

R056继续修正BUG-startup-001：首帧控制旧范围只含原正文/侧栏，却遗漏先挂载的typ-workspace-root；新功能测试先复现根过早可见。统一补齐根和自有顶栏首次显示，去除加载提示层。原生rAF检查第一可见根与正文保持身份，20/100/1000幂等挂载及20次面板切换回归，不把物理屏幕录像或未知宿主兼容记为通过。

## R058：快速打开匹配与呈现

| 问题 | 根因与处理 | 验证 |
| --- | --- | --- |
| BUG-quick-open-001 无高亮、长路径漏匹配、排序偏差 | 自定义评分含负位置惩罚且UI未保留命中位置；换固定VS Code查询/评分/比较/范围 | 上游基准、双斜线、多词、引号、Unicode、长路径，旧候选高亮红测 |
| BUG-quick-open-002 大工程漏文件 | 扫描在50000项停止，与匹配结果上限混用；完整枚举，512匹配上限独立 | 第51001项文件可找到 |
| PERF-quick-open-001 512行构建停顿 | 候选增加后DOM全建测得64ms间隔；改可见行绘制，同机复测19ms | 键盘可访问第512项，5万候选输入10ms |
| BUG-quick-open-003 弹层比例偏小 | 62vw在正常窄窗口中提前缩小；改含边框600px，按实际窗口约束最大宽 | 原生802px视口下600px，行22px |
| TEST-quick-open-001 Escape夹具不完整 | 只发keydown，公共关闭栈在keyup接受；补完整按键周期 | 原生20轮查询/退出，不修改产品关闭语义 |

前轮PERF-startup-002、真实官方升级及其他平台边界保持，不能以本次搜索通过覆盖。

## R059：最近文件与目录生命周期

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-recent-001 已删除项目反复报错 | 自有菜单直接打开，遗漏宿主失效移除；菜单/管理列表/原生目录入口共用历史服务，缺失与权限/离线分开 | 临时路径单元、实际菜单与主进程持久化；不对展示中的全部离线路径做阻塞扫描 |
| TEST-recent-001 夹具历史和偏好失真 | UI替身保存正斜线路径却使用精确字符串移除；原生副本未开启restoreWhenLaunch=2却期待恢复文件 | 替身使用真实Windows路径形式；独立副本显式设置并验证原生偏好，保留首次失败 |
| BUILD-recent-001 Windows临时文件映射占用 | 首次最终构建workbench.js写入被系统拒绝，导致不完整清单；未安装该次资产 | 重新完整构建后检查摘要再验收；占用进程未归因，不称产品逻辑已修复 |

## R061：侧栏折叠与缩进

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-sidebar-geometry-001 分区与树根左侧留白不一致 | Explorer叠加margin/padding/icon margin，树renderer硬编码额外8px；统一interaction几何token，renderer仅写深度 | 旧候选红测；6关联UI和原生明暗、窄栏、缩放、折叠及SCM对照 |
| TEST-sidebar-geometry-001 原生截图交接超时 | zoom小数直接进入阶段名，违反私有桌面允许的字母数字下划线契约；改整数百分比 | 保留首次失败，最终138断言与8张Explorer截图；未改放宽捕获边界 |

## R062：提交托管网页

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-scm-web-001 浮层缺入口且菜单绑定GitHub | 菜单私有拼接只支持一个平台；移入共同provider解析与打开动作，浮层/菜单复用 | 四公共平台解析、多目标/取消、切库/远端变化；原生真实runner在浏览器边界截获 |
| TEST-scm-web-001 旧GitHub测试未迁移 | 移除旧函数后首次测试仍导出原接口；测试改共同解析及真实菜单 | 最终完整check、两项UI通过，首次失败保留 |
| TEST-hover-capture-002 原生截图未包含浮层 | 私有桌面PrintWindow抽查未捕获DOM已显示的浮层；不将该图作为像素证据 | 原生33断言及Electron capturePage明暗图通过，不等同物理屏幕验证 |

## R063：安装权限与实际写入范围

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-install-permissions-001 访问拒绝无授权续装 | 安装器无权限预检/UAC交接；新增共同权限服务，原事务重入且显式保留原用户路径 | 实际临时ACL；系统授权端口替身，不代表跨账户安全桌面实测 |
| BUG-install-permissions-002 不变文件被误当待写 | 初版探针对正在使用且摘要相同的Node写打开，真实更新失败；与写入规则一致跳过，并取消未变化启动页覆盖 | 真实ZIP+运行Node更新通过；只读且未变的宿主入口仍可普通安装 |
| TEST-install-permissions-001 只读夹具前置状态不符 | 前序恢复已还原原生页面，却期待重复更新；先完成初装再设只读 | 不放宽产品权限检查，最终安装与回滚回归通过 |
