# 稳定性问题索引

按领域和根因检索，重复复现更新同一编号。需求设计见[测试架构](stability_testing.md)，本轮运行证据见[验收记录](stability_validation_20260919.md)。本表记录缺陷结论与边界；当前交付进度以本地台账为准，避免每份文档再建一套完成清单。

| 问题 | 需求 | 类型/严重度 | 复现与实际根因 | 方案与关联验证 |
| --- | --- | --- | --- | --- |
| BUG-preview-web-001 正常网站在预览中空白 | R069.11 | 浏览器承载/P2 | iframe受站点禁止内嵌及opaque来源、表单/存储限制；下载网络配置不能改变网页承载方式 | 使用原始宿主已开放的隔离Chromium guest；真实GitHub、拒绝iframe回环页、跨来源历史、严格TLS及20轮清理，见web_preview_browser_20260924.json |
| BUG-reading-link-001 正文链接编辑手势被导航接管 | R067.2 | 交互归属/P2 | 原生正文普通点击可编辑，但非活动主Markdown分栏排除链接编辑切换，根代理对全部链接执行打开 | 主Markdown分栏普通左键进入来源编辑，根代理只保留明确修饰键导航；独立预览沿原会话浏览，见body_link_editing_20260924.json |
| BUG-git-source-001 比较正文与来源行失联 | R034.4 | 状态所有权/P2 | 提交图文件没有来源选择投影，变更列表只记最后单击文件；差异视图缺少仓库/版本/路径来源契约，虚拟行首次定位还未建立滚动高度 | 差异持有结构化来源，活动视图事件与行创建共用投影；独立定位命令先建立虚拟范围再聚焦，不重开正文；20/100/1000文件、筛选外提交/改名、取消与原始宿主验收见git_diff_source_20260924.json |
| BUG-remote-003 Git历史和终端跨服务对象 | R070.7 | 身份隔离/P1 | 仓库全局历史混入物化缓存及本地目录，终端配置绕过工作区切换直接启动其他身份 | Git捕获提供者并分域保存/显示，终端统一先切工作区；真实SSH仓库与本地切换、两个注册账号及20/100/1000身份转换验证 |
| BUG-remote-004 选择器误关闭和手动发现零结果 | R070.7 | 交互/P2 | 原生后台焦点恢复触发模态focus-out关闭；主动找子仓库仍用默认0层扫描 | 模态显式关闭、主动至少扫描一层；原生复现失败后复验通过，见ssh_workspace_scope_20260923.json |
| BUG-git-markdown-001 渲染差异缺少概览跳转条 | R034.2 | 呈现/导航/P2 | 新阅读容器只接普通滚动，隐藏Monaco时其红绿概览一并隐藏；前轮验收只测工具栏跳转 | 基于渲染块高度增加局部概览与视口，覆盖点击、拖动、键盘、缩放、模式切换及刷新清理；源码保留Monaco原概览 |
| TEST-git-markdown-001 比较页刷新入口验证失配 | R034.1 | 测试/P2 | 原生夹具使用仓库刷新等待独立比较更新，不能证明比较页自身刷新行为 | 改为实际刷新差异命令，校验同标签内容更新、只读和源文件不变；原始宿主最终通过，见git_markdown_diff_20260923.json |
| BUG-preview-006 链接不可跳转且快捷键串入正文 | R069.6 | 导航/P2 | 只读渲染主动移除所有href；主历史在window捕获Alt方向键，预览没有自己的导航所有者 | 受控链接事件与每预览独立历史，主导航排除预览；新选择清历史，失败保留旧阅读器，172项原生及37项链接UI，见preview_navigation_20260923.json |
| BUG-preview-007 窄工具栏辅助动作挤压关闭 | R069.6 | 呈现/P2 | 缩放并入顶栏后，公共按钮规则覆盖窄宽隐藏规则；仅测滑条无法证明关闭完整 | 提高局部角色优先级，验收含关闭的全部必要动作在170px同一行可见；最终原生与UI均通过 |
| BUG-workspace-colors-001 功能区域与正文缺少层次 | R074 | 呈现/P2 | 自有面板多处直接继承Typora正文背景，不同领域缺少统一功能背景角色；图标还从终端背景推断主题 | 集中语义色和主题事件顺序，宿主明暗独立判断；22项颜色专项、75项原生窗口及10组相关UI回归，见workspace_colors_20260923.json |
| TEST-workspace-colors-001 原生面板采样未证明可见 | R074 | 测试/P2 | 首版夹具仅查询已连接DOM，原生隐藏面板/透明根会造成误判；隐藏后立即点击也可能撞上动画 | 改为实际功能入口并校验视口命中和实际绘制背景，关闭原生首次提示；保留失败记录，最终75项通过 |
| BUG-remote-002 连接完成反馈早于目录切换 | R070.5 | 状态生命周期/P2 | 原生回归在连接成功和首个context事件后立即打开远端选择器，仍撞上未完成的目录切换；旧入口提前选择文件提供者且通过无完成回执的命令启动挂载 | 复用并等待共同set_folder事务，提交时才选择提供者，完成后解除连接忙碌；主工作区101项及普通切库UI回归通过，见ssh_accounts_20260923.json |
| BUG-reading-003 短代码残留过大高度 | R068 | 几何/P2 | 原生CodeMirror在布局宽度变化后保留换行高度缓存；仅刷新被展开围栏，其他实例未登记宽度/可见性失效 | 每实例观察宽度和可见性，离屏延后、帧内合并；旧版可见短块107.78→61.86px复现；关联代码交互/缩略图及原生混排，详见code_blocks_geometry_20260921.json |
| BUG-terminal-geometry-001 终端末行裁切 | R006.9 | 呈现/P2 | 父层padding未被FitAddon扣除，border-box与缩放组合中末行超出约3px | 所有表面将padding集中到xterm自身，底部4px；54组UI几何及原始宿主面板/分屏/编辑器回归 |
| BUG-reading-001 链接与源码导航缺项 | R067 | 导航/P2 | 历史仅适配Markdown打开，源码未接入；带锚点原生路径可先打开后补定位，缺少光标/活动编辑器观察 | 统一导航端口、链接起终点事务与近邻合并；导航历史设计与本轮证据 |
| BUG-workspace-009 相同会话反复替换文件 | R067 / R040.2 | IO/P2 | 连续1000次相同会话写入期间，两轮完整检查出现Windows rename EPERM；具体外部占用者未确定 | 相同字节跳过创建/替换，实际变化仍原子写；不忽略权限/占用错误，验证1000次仅一次替换 |
| BUG-workspace-006 分屏拖动空转与比例错位 | R005.1 | 性能/布局/P2 | 无滚动仍持续rAF；删除后才求索引误删末权重；活动排序重复开关编辑器；拖动重分配无关分栏 | 按可滚动条件调度、删除前取权重、相邻两栏夹紧、活动叶避免重复toggle；20/100/1000模型和频道压力，原生20次分栏及20次建窗合并，drag_stress_20260920.json |
| BUG-workspace-008 同路径无条件拒绝合并 | R005.3 | 可用性/P2 | 接收只检查路径重复，没有比较实际正文和保存格式 | 一致时复用现有目标及撤销，差异/比较期间变化保留双方；源码/Markdown同内容及原生重复目标回归，drag_duplicate_20260920.json |
| BUG-workspace-007 拖放附窗恢复整组文件 | R005.2 | 状态生命周期/P1 | 拖出附窗恢复原目录全部标签，接收竞争后出现移动标签模态框 | R005.2：启动意图隔离恢复与会话写入；事务开始前等待宿主上一轮切换，取消/目标变化停止；失败使用共享非模态通知。原生开启恢复配置覆盖源码及Markdown，drag_direct_20260920.json |
| BUG-update-004 手动检查没有即时反馈 | R047.1 | 可用性/P2 | 只在网络完成后显示结果，忙碌时手动命令直接返回 | 请求级状态、即时对话框、后台转可见、取消和迟到隔离；TC-update-ui及原始宿主检查，update_feedback_20260920.json |
| BUG-delivery-001 历史安装无法卸载 | R025 | 交付/P1 | 卸载仅接受schema 4，最初schema 3备份全部排除；宿主发行标识变化使放宽schema也不能安全恢复。此前仅新schema理想夹具未覆盖历史升级 | 完整恢复保持严格；默认无候选时只移除当前唯一接入，备份/原子替换/回滚、只读预检、日志及安装互斥；65项CMD回归与实际只读诊断，uninstall_repair_20260920.json |
| BUG-plugins-002 真实插件对象配置未保存 | R057/R060 | 数据持久化/P1 | Mapper 1.2.1原地修改对象再set，上游Store因同引用跳过通知；替身设置测试没有覆盖此调用 | 仅社区PluginSettings ABI隔离默认对象、同引用set复制后通知；实际插件编辑磁盘保存、停用/重启实例、卸载保留配置通过，shared_ui_plugins_20260920.json |
| BUG-menu-002 实际菜单名称仍被截断 | R065 | 呈现/P2 | 四列在每行独立分配，其他行长快捷键挤占名称；测试插入额外长标签撑宽菜单掩盖问题 | 公共工厂统一测量最长名称及快捷键；实际三项菜单Range逐段验证，窄视口换行，三主题原生检查通过 |
| BUG-menu-003 普通命令预留勾选空位 | R065.1 | 呈现/P2 | 顶栏/共享/核心菜单无条件创建状态槽，共享宽度也固定累加 | 同一构造器按布尔checked能力分配槽，普通命令无留白，混合项快捷键列保持；本次证据见菜单状态槽验收 |
| BUG-sidebar-002 分区整行背景不统一 | R061 | 呈现/P2 | 仅统一箭头槽，Explorer标题按钮和Git整行仍分别持有背景与边距 | header/title/actions公共角色接管一级分区；真实指针悬停验证整行，连接状态下采集5类标题几何 |
| BUG-plugins-001 管理和配置入口难发现 | R060 | 可用性/P2 | 初始管理页只在帮助菜单，齿轮仅原生偏好；迁入侧栏时宿主header/input规则又导致标题覆盖和隐藏控件外露 | 左侧扩展/快捷键/视图统一SidebarPanel，齿轮与插件行共用SettingTab，停用清理；标题语义与hidden在独立区域隔离。TC-community-plugins-ui/native和活动栏/偏好/快捷键/顶栏回归 |
| BUG-scrollbar-002 空闲滑块常驻 | R049.1 | 呈现/P2 | 共享CSS仅统一圆角和颜色，没有按交互状态收起原生滑块；不是单个Explorer样式缺陷 | 公共显隐服务按实际overflow所有者登记，500ms空闲后800ms淡出，保留原生输入与第三方所有者；TC-scrollbar-fade-unit/stress/ui/native，scrollbar_fade_20260920.json |
| BUG-files-001 最近目录遗漏 | R040 | 功能/P1 | 打开新目录只调用setMountFolder，未像宿主onRootChanged写入addRecentFolder | await宿主历史；取消/无效/迟到不记入。TC-quality-implementation、TC-system-native-stability |
| BUG-workspace-005 切目录残留旧工作区 | R040.1 | 状态生命周期/P1 | 只改挂载路径，旧标签仍有效；Git优先活动标签且未订阅工作区切换；搜索、固定时间线、终端等须一并失效 | 设计见workspace_switch.md；统一预检/保存取消/切换代次，逐领域清理与目标刷新；补齐旧Git订阅与迟到导航清理。TC-workspace-switch-ui/context/history-stress/native，证据workspace_switch_20260919.json |
| BUG-files-002 回收无效果 | R041 | 兼容/P1 | 用户在另一电脑确认删除无效果；本地调用renderer shell，未对齐原生主进程布尔返回契约。账户权限仍是未证实的跨机因素 | 文件/Git统一JSBridge回收，false/异常/仍存在均失败，禁止永久删除后备；尊重原生删除警告配置。TC-quality-001、Explorer与原生稳定性；另一电脑实际验收待补 |
| BUG-files-003 改后缀未更新编辑器 | R042 | 功能/P1 | 111→1111.md只迁移路径，源模型语言和视图未重分类 | 保持模型、撤销及dirty；clean自动原生Markdown，dirty保存成功后切换。TC-files-011、TC-system-native-stability |
| BUG-reading-001 章节被顶部遮挡 | R043 | 几何/P1 | 原生scrollAdjust显式margin以窗口顶端计算，未扣工作台顶部；共同视口原先仅处理底栏 | 统一读取顶部相交遮挡，适配显式宿主跳转；保留隐式编辑滚动。TC-workspace-020与原生两主题，关联大纲/面包屑/缩略图回归 |
| BUG-workspace-001 功能菜单图标错列 | R044 | 几何/P2 | fa负偏移；SVG没有槽；活动图标外壳克隆后带入未限定范围的24px规则；原生CSS覆盖anchor布局 | 菜单图标及外壳16px，独立文字列；24px仅限真实活动按钮。实际字体/SVG、间距/行盒验证：TC-workspace-004、TC-system-native-stability/stress |
| BUG-git-001 普通动作多次确认 | R041 | 功能/P2 | checkout等仍进入通用“预览→执行”表单 | 已知无参数动作直接进入共同事务，必要参数一次提交；风险文案执行前可见，重复/迟到/切库不执行。TC-git-016、Graph interaction、SCM actions |
| BUG-git-002 底栏状态信息丢失 | R045 | 功能/P2 | 暂存/未暂存都被概括为dirty星号；分离HEAD和同步数字表现不一致 | 从已有快照计算*、+、!，分离HEAD8位及官方git-commit，N↓ N↑。TC-quality-state-stress、TC-workspace-012；上游保护分支复合图标仍保留差异 |
| BUG-quality-001 需求到证据脱节 | R039 | 测试设施/P1 | 旧测试分散，缺少跨层分类和当次运行身份 | 稳定用例目录、分类运行器、失败/未执行状态及源/资产/日志摘要；六类代表性测试入口与文档闭环 |
| BUG-quality-002 跨PowerShell模块污染 | R039 | 测试设施/P1 | PS7→Node→PS5.1继承PS7模块路径，Get-FileHash未加载，三个安装夹具失败；直接PS启动无此问题 | 仅对子进程清理PSModulePath，让目标引擎重建标准路径；不改用户环境/权限。安装、卸载、日志系统用例重跑 |
| BUG-quality-003 过早判定表单完成 | R041 | 测试设施/P2 | 单次提交先禁用按钮再异步准备；旧测试将“按钮禁用且尚未写入”误认为完成，HEAD断言偶发失败 | 等待实际提交结果及事务结束，再核对HEAD与正文；不增加固定睡眠掩盖竞态。TC-git-010 |
| BUG-quality-004 原生夹具缺少依赖 | R039 | 测试设施/P2 | 新隔离复制器误排除window.html实际引用的appsrc，引发初始化超时 | 完整复制宿主依赖，独立Git根阻止发现开发仓库；保留首次失败、ASAR/资产摘要和截图 |
| BUG-quality-005 几何夹具未加载样式 | R044 | 测试设施/P2 | 原核心生命周期测试只装JS，新增几何断言未装候选CSS而失败 | 加载候选workspace_core.css和Git领域样式再量实际图标；原生两主题结果作为独立证据。TC-workspace-004 |
| BUG-files-004 Explorer内容与辅助操作主次 | R046 | 呈现/P2 | 文件列表继承正文等宽字体，标题又使用主题字体，分区图标未显式隔离字形与按钮盒模型 | 工作台13px/400内容、12px/600标题、11px说明，16px辅助字形；TC-files-009红绿、013文件图标及原始宿主两主题；方案见explorer_history.md的R046节 |
| BUG-quality-006 原生几何与截图不同步 | R046 | 测试设施/P2 | 面板未挂载即读取导致失败；仅量矩形会漏过首帧空白及测试CSS强改宽度造成的编辑区遮挡 | 等待面板可见命中与字体就绪，宽度经真实分隔条键盘入口，同时断言相邻编辑区；最终四张截图复核，前序通过不冒充完整视觉证据 |

| BUG-update-001 系统PowerShell解压脚本解析失败 | R047 | 兼容/P1 | 含中文的UTF-8无BOM脚本在PS7解析通过，但系统PS5.1按本地编码读取后报MissingCatchOrFinally；真实ZIP系统用例发现 | 辅助脚本保留UTF-8 BOM，构建保留字节；跨进程输出显式UTF-8，避免中文可执行路径被误解码；TC-update-install真实5.1执行及中文进程／用户路径回归 |
| BUG-update-002 下载写盘失败被报完成 | R047 | 正确性/P1 | WriteStream.end的回调未检查错误，目标已存在时可能先返回成功；随后error被已完成状态忽略 | 只在finish事件确认成功，error先失败；TC-update-protocol覆盖EEXIST且旧文件字节不变、断流／取消／超时 |

| BUG-update-003 共享运行时缓存并发覆盖 | R047 | 并发/P1 | 两个真实安装夹具使用独立用户目录，但prepare_typora_node无条件解压同一缓存；另一事务使用缓存Node时ExtractToFile报文件占用 | 缓存位置／版本／架构互斥；有效文件摘要匹配即复用；TC-update-install增加运行中的缓存Node，TC-delivery-002并发回归；首次失败报告保留 |

| BUG-workspace-002 活动栏勾选遗漏与空白 | R044、R018 | 功能／几何/P2 | 菜单未传button.visible；统一state-off引入8px伪元素，原生24px左padding与flex槽叠加；前次用例只测图标未测状态 | 共享可勾选项、单一26px勾选列、独立菜单自有几何；核心UI与原生明暗／切换／持久化回归 |

| BUG-git-003 分支入口仍是普通菜单 | R048、R027、R045 | 功能/P2 | 底栏和SCM各自枚举引用，缺少搜索/标签/创建来源，远端重复要求填写已知目标 | 共享检出选择器与完整引用服务；远端自动跟踪，事务/草稿保护；TC-git-checkout-service/ui/stress及原生56项；设计见git_branch_checkout.md |
| BUG-workspace-003 检出详情被共享列表居中 | R048 | 几何/P2 | 静态样式顺序让普通列表align-items覆盖检出双行规则，单量行高未发现文字偏移 | 显式检出双行作用域和文字起点断言；原生明暗截图复核；保留首次失败与后续证据 |
| BUG-quality-007 原生注入错误只表现为超时 | R048 | 测试设施/P2 | 夹具块内rows重复声明使脚本无法执行，旧Esc夹具只发送keydown | 准备前node --check；原生按共同退出栈发送完整按下/释放；失败报告保留，最终56项通过 |

来源日期均为2026-09-19；用户截图分别用于最近目录、面包屑和菜单问题，跨机删除与后缀场景来自用户文字。过程发现BUG-git与BUG-quality不从图片推断账户或主机信息。

| 问题 | 需求 | 分类 | 确认根因 | 处理与验证 |
| --- | --- | --- | --- | --- |
| BUG-workspace-004 滚动条样式分散 | R049 | 呈现/P2 | 原生直角与各面板独立宽度/颜色混用；标准thin/color会绕过WebKit绘制 | 共享workspace_scrollbars，原生/Monaco/xterm/缩略图集中适配；TC-workspace-scrollbars及三档压力、隔离宿主明暗截图 |
| BUG-quality-008 滚动夹具指针坐标不随缩放 | R049 | 测试设施/P2 | Electron滚轮delta方向写反，125%时滑块坐标未乘zoom；停留滑块的指针又污染下一主题默认颜色断言 | 使用真实输入约定并在三态检查间移开指针，保留首次失败与后续通过报告；不修改产品事件来迁就测试 |

本轮R050追加：

| 问题 | 需求 | 分类 | 确认根因 | 处理与验证 |
| --- | --- | --- | --- | --- |
| BUG-git-004 列边界反向且哈希无法调整 | R050 | 交互/P2 | 单列增宽挤压左侧弹性说明列，表头最右柄无法控制用户指向的作者/哈希边界 | 相邻可见列共用调整事务，说明最小宽与实际剩余空间分开；TC-git-columns/math/stress与原生明暗 |
| BUG-git-005 初始分隔条无障碍值为零 | R050 | 可访问性/P2 | 绑定时表头尚未进入列表；标签隐藏/恢复和侧栏宽度变化后数值未同步 | 在表头与行挂载后绑定，观察表头布局，隐藏时不发布零值；原生Night复现及最终重绘/销毁压力证据保留 |
| BUG-quality-009 Electron方向键名称误用 | R050 | 测试设施/P2 | 隐藏窗口夹具把DOM的ArrowLeft/ArrowRight作为Electron键码，真实方向输入没有命中处理器 | 发送Left/Right，DOM合成事件仍用ArrowLeft/ArrowRight；三次失败保留，不修改产品键盘规则来迁就夹具 |

本轮R034追加：

| 问题 | 需求 | 分类 | 确认根因 | 处理与验证 |
| --- | --- | --- | --- | --- |
| BUG-workspace-002 导航与正文边界缺失 | R034 | 呈现/P2 | 共享面包屑border:0遗漏固定上游浅色底边，原有测试仅核对行高和交互 | 统一1px主题绘制层，保留22px行盒及点击；TC-workspace-003与原生80项、明暗截图复验 |

## 未关闭的环境与产品边界

- BUG-files-002：已建立主进程回收契约并在本机原始Typora验证真实临时文件/目录；另一电脑的版本、权限、文件占用和回收站支持仍需该环境记录。失败保留文件，不能绕权限或降级永久删除。
- BUG-git-002：本轮处理已有底栏信息；VS Code保护分支/特殊状态复合图标尚未映射，不宣称所有Git状态外观完全等价。
- 原有终端物理输入法、跨平台剪贴板、ARM64/Linux原生桌面、UCRT64交付等缺口继续保留原需求号，不因本轮单元或隐藏窗口通过关闭。
- 新测试架构覆盖所有现有脚本的归属，不代表每一个历史需求都已有全部六类用例。新增需求按影响分析增加有价值的用例，缺口显式记录，避免为了填格子复制无效测试。

## 新问题记录模板

使用`BUG-领域-序号`并关联既有需求号；记录来源/最近复现日期、环境和候选摘要、最短复现、预期/实际、严重度、确认/假设根因、影响服务与状态所有者、权威方案章节、用例号、失败与修复后运行入口、安装和加载边界。未证实的根因写“待核对”，不能根据弹窗或截图推断权限。相同根因追加复现环境和证据，不按运行日期新建流水账；有独立原因才分号。

## R052：空占位、原生缓存与文档生命周期混用

2026-09-19。分类：状态所有权/宿主适配，影响Explorer、阅读、时间线和当前文档操作。core.empty误入列表导致New tab无法关完；缓存同路径重开跳过磁盘，删除后宿主生成隐藏dirty草稿。共同身份规则与显式原生释放修复，设计见[空编辑区](explorer_history.md#r052-空编辑区与最后一个文件)。连续操作分别复现旧实现缺陷，不能仅以单次删除成功关闭问题。回收后发生新输入继续保留，重置失败不清除非空内容。测试与交付结果统一关联当次证据，不另维护完成清单。

## R040.2：工作区文件会话缺失及遍历提前退出

2026-09-20关联回归记录：首次完整check在1000档会话磁盘替换出现一次Windows EPERM，独立重跑与后续两次完整check通过，未修改会话实现。保留`.cache/hover_check.log`与重跑日志，瞬时文件占用原因未确认；不能把复跑通过写成已修复该环境问题。

同日R005.3回归再次出现两处EPERM：文本另存后的保存与会话记录rename，均在独立临时目录，根因仍未确认；文本用例独立复跑通过。保留`.cache/issue_tracking/duplicate_check.log`和`duplicate_check_verified.log`，后续完整串行结果见`drag_duplicate_20260920.json`。本轮不改写入保护或新增盲目重试，不能将同文件拖放修复外推为该问题已解决。

2026-09-19。分类：状态隔离/持久化/宿主契约。旧切换只清空不恢复；原生eachLeaves逆序且truthy终止，Array.push返回值使同组只处理一项，替身forEach掩盖该缺陷。修复显式void遍历、按标签顺序记录和按根原子保存；恢复读取原生偏好页getExtraOption（编辑器loadAll不含启动配置）。原生多文件往返与第二进程验证，失败报告保留在本地证据；设计见[工作区会话](workspace_switch.md#r0402-按工作区恢复上次打开的文件)，验证结果见对应正式证据。
## R053：提交正文语义与样式作用域

2026-09-19反馈，2026-09-20复核。原始减号和空行来自纯文本赋值；按固定VS Code Git hover逐换行分段后使用共同安全Markdown正文。首轮marked接口误用由专项UI发现；原生随后发现全CSS提取把Shadow样式放入head，导致原生缩进40px，夹具直接CSS字符串却通过。构建增加显式Shadow作用域并补正式构建首帧断言；问题依据及最终证据关联R053，不把夹具通过等同原生已通过。

## R054：启动与功能区切换时序

2026-09-20，权威方案见[启动稳定性](startup_stability.md)。按原因归档，不逐次运行新增问题。

| 问题 | 分类／原因 | 处理及验证边界 |
| --- | --- | --- |
| BUG-startup-001 原生布局闪现后替换 | 呈现/P2：静态CSS有了，但增强布局依赖的DOM尚未挂载；原测试只加载CSS并检查最终画面 | head控制一次性首次展示；就绪释放、失败/15秒超时恢复原生；真实宿主旧候选已复现，过程样本关联本轮报告 |
| BUG-sidebar-001 换面板先收起整个侧栏 | 生命周期/P2：Sidebar.switch每次调用hideSidebar再showSidebar，重复show还会再次onshow | 先解析目标，移交唯一面板所有者；重复show幂等，移除活动面板清理所有者；单元20/100/1000及真实四面板回归 |
| PERF-startup-001 界面注册与高亮串联 | 性能/P2：语法作为界面前置依赖；Git及浏览器注册同一任务合计约67ms | 高亮移到布局之后；两次注册间交还事件循环并复查取消；记录三阶段测量而非用总耗时推断阻塞 |
| PERF-startup-002 残余启动长任务 | 待归因：部分50ms以上任务发生在自定义注册前或数据首次加载时；并发测试增加波动 | 三次独立运行仍有50–67ms任务，其中一次browser-bind 52ms。专属宿主的inspector.Session返回“未初始化V8 Inspector”；保留错误和阶段时间线，不开生产调试或改ASAR，后续需可观测宿主/受控性能环境继续归因，不能宣称所有卡顿已消除 |

本轮夹具初始失败也保留：单元执行环境漏设CommonJS module；新HTML夹具漏UTF-8声明导致加载提示乱码。分别修正测试环境，不以改产品输出迁就测试。旧候选原生失败属于产品缺陷，和这两项测试设施问题分开记录。

## R057：社区兼容与生命周期

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-plugins-001 旧发行构造函数失败 | 社区ES5代码调用Parent.apply，编译后核心为ES类；公共ABI用Reflect.construct桥接同一类与原型 | 真实Codeblock Copy Button 1.2.0在原始宿主安装/启停，未恢复第二核心 |
| BUG-plugins-002 停用残留按钮 | 后处理器只注销处理函数，没有清除已生成按钮；注册器统一清理按钮和预览，异常构造/onload也清理 | 原始宿主20次启停、构造失败与设置/命令/底栏夹具 |
| BUG-plugins-003 高频原子替换EPERM | Windows文件临时占用原因未归因；1000轮出现替换失败。只对占用类错误作有界异步重试，保留旧配置 | 20/100/1000通过，持续拒绝注入证明原配置与运行状态保留；不能外推R040.2已修复 |
| TEST-plugins-001 旧夹具契约过期 | 独立启动夹具把ready提前兑现、漏复制新插件服务资产；更新到同一就绪契约和正式资产 | 启动/切库完整测试恢复通过；不降低异常断言 |
| TEST-update-001 旧发行夹具版本重复 | 给首版本减1与真实历史第二项碰撞，违反严格递减规则；改用真实历史尾部构造旧版本 | ZIP与真实安装事务通过；未放宽发布校验 |

R056继续修正BUG-startup-001：首帧控制旧范围只含原正文/侧栏，却遗漏先挂载的typ-workspace-root；新功能测试先复现根过早可见。统一补齐根和自有顶栏首次显示，去除加载提示层。原生rAF检查第一可见根与正文保持身份，20/100/1000幂等挂载及20次面板切换回归，不把物理屏幕录像或未知宿主兼容记为通过。

## R058：快速打开匹配与呈现

| 问题 | 根因与处理 | 验证 |
| --- | --- | --- |
| BUG-quick-open-001 无高亮、长路径漏匹配、排序偏差 | 自定义评分含负位置惩罚且UI未保留命中位置；换固定VS Code查询/评分/比较/范围 | 上游基准、双斜线、多词、引号、Unicode、长路径，旧候选高亮红测 |
| BUG-quick-open-002 大工程漏文件 | 扫描在50000项停止，与匹配结果上限混用；完整枚举，512匹配上限独立 | 第51001项文件可找到 |
| PERF-quick-open-001 512行构建停顿 | 候选增加后DOM全建测得64ms间隔；改可见行绘制，同机复测19ms | 键盘可访问第512项，5万候选输入10ms |
| BUG-quick-open-003 弹层比例偏小 | 62vw在正常窄窗口中提前缩小；改含边框600px，按实际窗口约束最大宽 | 原生802px视口下600px，行22px |
| TEST-quick-open-001 Escape夹具不完整 | 只发keydown，公共关闭栈在keyup接受；补完整按键周期 | 原生20轮查询/退出，不修改产品关闭语义 |

前轮PERF-startup-002、真实官方升级及其他平台边界保持，不能以本次搜索通过覆盖。

## R059：最近文件与目录生命周期

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-recent-001 已删除项目反复报错 | 自有菜单直接打开，遗漏宿主失效移除；菜单/管理列表/原生目录入口共用历史服务，缺失与权限/离线分开 | 临时路径单元、实际菜单与主进程持久化；不对展示中的全部离线路径做阻塞扫描 |
| TEST-recent-001 夹具历史和偏好失真 | UI替身保存正斜线路径却使用精确字符串移除；原生副本未开启restoreWhenLaunch=2却期待恢复文件 | 替身使用真实Windows路径形式；独立副本显式设置并验证原生偏好，保留首次失败 |
| BUILD-recent-001 Windows临时文件映射占用 | 首次最终构建workbench.js写入被系统拒绝，导致不完整清单；未安装该次资产 | 重新完整构建后检查摘要再验收；占用进程未归因，不称产品逻辑已修复 |

## R061：侧栏折叠与缩进

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-sidebar-geometry-001 分区与树根左侧留白不一致 | Explorer叠加margin/padding/icon margin，树renderer硬编码额外8px；统一interaction几何token，renderer仅写深度 | 旧候选红测；6关联UI和原生明暗、窄栏、缩放、折叠及SCM对照 |
| TEST-sidebar-geometry-001 原生截图交接超时 | zoom小数直接进入阶段名，违反私有桌面允许的字母数字下划线契约；改整数百分比 | 保留首次失败，最终138断言与8张Explorer截图；未改放宽捕获边界 |

## R062：提交托管网页

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-scm-web-001 浮层缺入口且菜单绑定GitHub | 菜单私有拼接只支持一个平台；移入共同provider解析与打开动作，浮层/菜单复用 | 四公共平台解析、多目标/取消、切库/远端变化；原生真实runner在浏览器边界截获 |
| TEST-scm-web-001 旧GitHub测试未迁移 | 移除旧函数后首次测试仍导出原接口；测试改共同解析及真实菜单 | 最终完整check、两项UI通过，首次失败保留 |
| TEST-hover-capture-002 原生截图未包含浮层 | 私有桌面PrintWindow抽查未捕获DOM已显示的浮层；不将该图作为像素证据 | 原生33断言及Electron capturePage明暗图通过，不等同物理屏幕验证 |

## R063：安装权限与实际写入范围

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-install-permissions-001 访问拒绝无授权续装 | 安装器无权限预检/UAC交接；新增共同权限服务，原事务重入且显式保留原用户路径 | 实际临时ACL；系统授权端口替身，不代表跨账户安全桌面实测 |
| BUG-install-permissions-002 不变文件被误当待写 | 初版探针对正在使用且摘要相同的Node写打开，真实更新失败；与写入规则一致跳过，并取消未变化启动页覆盖 | 真实ZIP+运行Node更新通过；只读且未变的宿主入口仍可普通安装 |
| TEST-install-permissions-001 只读夹具前置状态不符 | 前序恢复已还原原生页面，却期待重复更新；先完成初装再设只读 | 不放宽产品权限检查，最终安装与回滚回归通过 |

## R064：ZIP更新身份与缓存归属

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-update-004 同序号新提交无法更新 | 原检查只有sequence门槛；改为SHA回执绑定已安装清单，首次ZIP建立资产等价身份；重复/过期计划拒绝，失败不推进 | 无Git/.git的真实ZIP事务，固定SHA网络响应替身；不等同公开远端发布实测 |
| BUG-update-005 更新临时包不在Typora temp | 状态目录直接放在用户数据根；共享路径服务统一到用户数据temp，永久SHA回执独立保存 | temp缺失创建，普通权限下载/安装；受保护宿主入口仍沿用R063，不能承诺免除系统ACL |
| TEST-update-build-001 重建中断后的混合资产 | Windows拒绝写入有映射占用的workbench.js，清单未完成；随后安装夹具正确拒绝服务摘要不一致 | 保留失败报告，等待检查结束后重建完整候选并重新执行更新域；不把失败包安装到用户环境，占用来源未归因 |


## R065：共享菜单列数与宽度

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-menu-width-001 设置菜单出现横向滚动 | 可选快捷键/自定义class生成四单元，标准菜单三列；共同工厂始终四单元并按内容计宽 | 旧版原生红测，新版名称与快捷键行盒、窄窗口、三主题、缩放通过 |
| TEST-capture-ack-001 原生截图回执争抢 | Windows读回执时写入失败，旧逻辑已标阶段完成；只在成功写后标记，IO失败沿轮询重试 | 首次失败证据保留；最终237断言通过 |
| VERIFY-sidebar-002 重复白边反馈 | 旧测试仅量组件内部；新增活动栏到面板/树根绝对边界及用户正文主题 | 当前候选未复现额外外层间距，截图窗口版本未知；待用户重启后复核，未把推测记为根因 |

### BUG-workspace-quick-open-002：路径探测阻塞筛选

2026-09-21，R058，功能/并发：完整相对路径查询在stat延迟时清空所有已枚举结果，进行中状态又未显示。真实临时文件加延迟端口回归在旧版超时，新版通过。路径探测与普通候选筛选分离，同查询增量评分串行完成，补充等待/错误反馈和关闭/新查询拒绝迟到。用户截图触发延迟的具体环境尚未确认，小工作区原始Typora旧版完整路径查询也可通过；不可把受控延迟复现当作用户现场全因已证实。关联既有Quick Open功能、性能和原生用例，交付状态见反馈记录。


## R006.10：Windows终端后端与输入积压（2026-09-22）

| 问题 | 根因与处理 | 验证边界 |
| --- | --- | --- |
| BUG-terminal-conpty-001 后端兼容信息缺失 | 原实现禁用配套DLL，xterm未获windowsPty；接入匹配DLL、真实系统build及DA1，去掉重复行格通知 | Windows 11后端测试保留；Win10连续提示符缺陷已单独复现并修复，见下文R006.10记录 |
| PERF-terminal-prompt-001 Git Bash默认提示符积压 | 100次输入单次转发，Git仓库默认提示符松键约4.9秒仍有输出；简单PS1隔离后无积压 | 用户2026-09-22确认原生Git Bash同样出现并撤销修复要求；停止处理，保留配置，不记为已修复 |
| TEST-terminal-wheel-001 滚轮事件错误 | Chromium仅传deltaY时wheelDeltaY为0；补wheelTicksY及原生DOM完整数据 | 首次假失败保留，最终实际视口移动判定 |
| TEST-terminal-assets-001 原生夹具使用旧后端 | 准备工具原先仅复制已安装运行文件；现覆盖当前发行清单并逐项校验 | 缺DLL的失败样本不是候选功能结论；最终前后端摘要同时登记 |

### BUG-update-retry-001：检查失败只提示重试但无操作

2026-09-22，R047.3：错误结果复用仅含关闭的message弹窗，用户需要重新进入帮助菜单。增加公共重试按钮，校验当前弹窗身份后关闭并调用同一手动检查，不新增网络或安装通路。20轮失败/重试、键盘进入、旧节点重复点击和取消/迟到隔离均纳入当次验证；后台失败保持静默。

2026-09-22 R006.10现场边界更新：Windows 10 22H2（19045.7725）、2026.09.22.1仍需按上下键后才出现较宽滚动条，旧输出是否保留未确认；本机Win11新增20轮首次输出和两种PowerShell召回命令前后测试未复现。用户明确转到Win10继续修复，当前本机排查暂停；不以2026.09.22.3发布宣布该问题修复。Git Bash抖动用户已确认原生同类表现并撤回处理。


## R069 链接预览生命周期（2026-09-22）
新增选区与右键预览，复用搜索只读视图与编辑组。网络请求按预览实例取消，迟到网页不得覆盖新文件，HTML不能取得宿主权限或可编辑属性；20次分屏关闭检查归属清理。真实宿主夹具最初直接赋活动叶子而未执行标签切换，导致原生正文与目标不一致；改用真实toggleTab入口后通过，不将该夹具失败记为宿主缺陷已修复。网页脚本/登录交互及其他平台未验收；结果见[证据](../enhancements/tests/evidence/link_preview_20260922.json)。

R069收尾补充：搜索排队、运行结束和错误清理可能覆盖较晚的链接选择；现在结果清理保留新预览所有权，自动首项选择仅作用于搜索预览。用延迟目录枚举和Git范围失败夹具覆盖三种时序，并验证显式新搜索仍切回结果。首次新增断言在加载期间访问尚不存在的正文节点，补齐等待条件后再验；不能把夹具异常计为产品修复。

## R071 模块刷新阻塞（2026-09-22）
- BUG-git-modal-001：操作执行及完成仍保留全屏表单遮罩，其他模块被模态消费。
- PERF-git-changes-001：未跟踪文件逐项some去重构成二次成本，SCM为所有变更生成DOM。
- BUG-git-discovery-001：父目录控制器按包含关系复用，无法据此保证嵌套仓库身份；空目录缺初始化闭环。
- TEST-quality-lifecycle-001：只有预制仓库和最终结果，没有空→初始化→变化状态及跨模块操作响应断言。
状态实施中，根因与交付分别关联workspace_responsiveness.md及本次证据。

R071本次验证：上述Git模态、二次合并、全量DOM及仓库生命周期缺口已修复，完整check、5组UI、1万真实文件20轮与安装卸载通过；证据见`enhancements/tests/evidence/git_responsiveness_20260922.json`。

- **PERF-workspace-zoom-071**：真实宿主缩放0.8/1/1.25阶段仍有54–84ms事件循环间隔，刷新阶段未记录超过50ms间隔。范围是缩放/原生编辑器/xterm重新布局，不据此判为正常或归咎Git；已单独记录，需后续分阶段trace继续定位，不混入本次Git阻塞修复的通过结论。


2026-09-22 R071.1复核：空仓欢迎页复用无更改透明度、初始化反馈归属隐藏区域，以及分区布局覆盖空仓状态，归入BUG-git-modal-001的状态呈现后续修复。SCM统一loading/empty/error/ready所有者；主题主按钮角色补齐默认颜色，初始化与提交共用。验证见本轮交付记录，缩放长帧问题保持独立未完成。

## R070.3 / R072 远程操作隔离与配置归属（2026-09-22）

- BUG-ssh-modal-001：远程Git模态遮罩阻断其他模块，改为固定远程身份的独立只读标签，重复入口复用、关闭丢弃迟到结果。
- PERF-ssh-serial-001：慢Git占用文件协议主循环；仅Git使用一个有界执行槽，JSON输出加锁，文件请求继续串行；子进程stdin明确DEVNULL，避免继承协议管道。
- ARCH-settings-owner-072：自有设置分散入口缺统一检索；集中注册字段和作用域，读写仍经同一领域所有者，切目录后的旧表单拒绝写入。
- 当前上述修复通过本轮验证；R070.4完整远程工作区/资源提供者、远程SCM和会话能力继续待实现。验证见settings_ssh_20260922.json，既有缩放长帧保持独立；Win10历史问题按下方远端验证记录更新。

## 2026-09-22 R006.10 Win10连续空回车丢失历史

真实PowerShell提示符重绘使用CSI S，固定xterm的SU路径直接删除首行；普通换行的200行测试无法覆盖。补丁限定ConPTY普通全屏缓冲，使用同一历史滚动所有者；证据同时保留原版失败、20/100/1000行序测试与Win10原始宿主复现。上游尚未合并的提案不作为正式修复来源。分类证据见[本轮记录](../enhancements/tests/evidence/terminal_scrollbar_win10_20260922.json)。

## R072.1 设置入口重复与插件样式越界（2026-09-22）

BUG-settings-navigation-072：齿轮多入口与三个空页面重复，收敛为唯一设置和分类直达。BUG-community-form-072：通用对话框100%输入宽与按钮规则覆盖SettingTab，改用独立原始设置容器并排除自有交互层；实际插件表格保存/重开及主题通过。证据settings_isolation_merge_20260922.json；完整VS Code设置和SSH能力差距继续独立跟踪。

## R069.1/R072.2/R073 阅读重排与共同入口（2026-09-22）

- BUG-reading-reflow-073：固定段落顶部不能保持长段落内的阅读位置；原生旧滚动动画还会覆盖恢复结果。共用当前字符锚点，显式重排经同一所有者，停止content旧动画且不跳到旧终点。原生边距/窗口缩放与20轮预览缩放通过。
- BUG-quick-picker-close-073：关闭活动项激活相邻叶子时误触发focus-out关闭列表，关闭事务内保留选择器；实际核心回归通过。
- BUG-settings-modal-072：普通设置标签被侧栏挤占；改为独立可最大化浮层，几何优先级纠正后原生82项及设置130断言通过。
- ARCH-link-preview-069：链接预览和搜索共用状态造成互相覆盖，拆为各自数据与生命周期，复用同一只读渲染服务；关闭自动链接预览不影响搜索。

交付与环境限制见[本轮证据](../enhancements/tests/evidence/preview_settings_20260922.json)。

## R070.4 默认终端误用本地身份（2026-09-22）

BUG-ssh-terminal-context-070：项目终端独立传配置，普通新建/活动栏仍调用本地根且会误复用已有本地PTY。SSH所有者注册当前身份快照，终端共同创建入口解析默认目标；每个会话固定自己的启动配置，显式本地入口和本地拆分不被远程默认值覆盖。连接中、失败和断线不能创建本地PTY。原始Typora连接真实Linux虚拟机通过20轮命令、独立通道写入回读、拆分/重启和断线；统一资源管理、搜索、SCM、原生Markdown与安全凭据记忆继续待实现，不将此项修复当成完整远程工作区。

## R070.4 主工作区接入（2026-09-23）

- ARCH-ssh-resource-070：独立SSH浏览器绕开主文件/搜索/Git服务，Markdown只能源码加预览。远端提供者统一业务IO与Git执行，原生宿主仅使用按需物化文件，保存先确认远端；主工作区实际渲染、文件操作、搜索、初始化/提交、导航、断线与凭据由真实SSH和原始Typora验收。
- BUG-ssh-fs-wrapper-070：资源代理在调用时重新读取被其他适配器包装过的方法，搜索夹具暴露递归；创建代理函数时捕获原始方法，不修改Node全局fs，补充包装后调用回归。
- BUG-git-empty-status-070：从仓库切到无仓库目录后，底栏未处理empty状态而一直检测中；复用确定不可用状态，本地切目录UI和远端初始化均复查。
- BUG-quality-build-070：Windows占用使刚生成的bundle再次原地截断失败，连续PowerShell命令仍准备旧资产；该阶段图片结果不作为新源码证据。构建改为内存换行规范化后临时文件替换，失败停止后续验收；确认返回码和私有副本摘要后重新运行原生验证，保留失败日志。图片最终通过按需Blob显示且不改原Markdown链接；不把旧资产失败当作新实现根因。
- BUG-native-dirty-pending-070：用户反馈只浏览出现保存提示，历史16种格式组合224断言未复现。2026-09-23最新反馈重新启用调查（R040.3），覆盖此前暂缓约定；已修复恢复抢占及快速切换丢失，独立误报仍未复现，不清除原生脏状态。本轮证据见[会话优先级验证](../enhancements/tests/evidence/session_priority_20260923.json)。

本次证据见[远程主工作区验证](../enhancements/tests/evidence/remote_workspace_20260923.json)；未完成能力以远程设计的当前边界为准，历史缩放长帧等其他未决事项不变。


## R069.2 链接预览占位无法关闭（2026-09-23）

原实现把预览放进侧栏并每次调用show，固定40%高度，无关闭入口，功能栏收起与预览耦合。改为独立面板及共享尺寸交互，读取侧栏状态而不改变它；统一分配左列与底部占位。实机检查发现原生侧栏背景会遮挡body中的预览，已通过收缩侧栏本体高度修正，并添加工具栏三处真实命中检查。横向调整复用侧栏宽度所有者，避免resize恢复旧宽和误改预览高度。

原生初轮已有字符缩放断言失败，后续添加偏移采样，最终候选原生107项通过；保留初轮日志，不将一次通过当作所有缩放环境均覆盖。分类用例和当次交付证据见[预览调整验证](../enhancements/tests/evidence/preview_dock_20260923.json)。未编辑却保存提示仍按用户要求暂缓。


## R069.3—R069.5 缩放入口与预览边界（2026-09-23）

- BUG-preview-scale-controls：独立预览复用了阅读器，却漏接搜索的比例控件。共享呈现控件观察阅读器比例，三处使用同一实现；170px、滚轮、键盘和两种文本渲染回归。
- BUG-preview-sash-scope：主侧栏本体缩短后，其固定分隔条仍覆盖全高。引入真实分隔条后复现预览右边拖动被拦截；按所属区域裁剪底边，实际命中/边缘拖动通过。
- UX-preview-blank-column：整列避让导致预览上方大面积空白；最新授权取消整列偏移，正文正常排版，预览仅覆盖自身矩形。
- TEST-preview-owner：旧搜索夹具未加载独立链接控制器，等待不存在节点失败；补接真实所有者并修正并存/独立关闭断言，不再假设新搜索会关闭链接预览。

本次5组UI、原始宿主140项及安装/卸载证据见[验证记录](../enhancements/tests/evidence/preview_scale_20260923.json)。


## R014.1 滚轮缩放定位与常驻入口（2026-09-23）

- UX-zoom-default-entry：原实现遵循非默认比例才显示的规则；按新授权改为100%常驻，保留共同几何与操作。
- BUG-terminal-font-anchor：字号重排删除物理折行，且xterm相对滚动仍使用旧像素偏移；逻辑行marker/字符偏移与绘制后公共绝对滚动同步共同修复。历史、底部、隐藏挂载、备用屏及平滑设置分别回归。
- TEST-wheel-native：原始宿主合成点击在缩放后鼠标不在浮层内，浮层会按生命周期离开；夹具在重置前重新打开，另用真实Chromium输入验证连续控制。两种事件证据不能混称物理硬件测试。

本次证据见[滚轮缩放验证](../enhancements/tests/evidence/wheel_zoom_20260923.json)，已有未解决平台问题和保存提示暂缓状态不变。

## R008.3 展开菜单重复定位（2026-09-23）

- BUG-popup-layout-feedback：小数包含块下，实际矩形减去CSS序列化坐标形成舍入反馈；自身style观察又调度下一帧。旧候选300ms记录18次定位写入，最终静止写入为0。采用零坐标原点、几何差异判断及共享布局事务。
- BUG-hover-refresh-state：共享悬停自然尺寸测量可能收缩滚动范围；重新布局保留滚动，内容更新不重建或丢失焦点。缩放、链接和提交卡片共用该修复。
- TEST-popup-native-events：原始宿主关闭图标使用mousedown；修正夹具错误的click。首次启动/缩放期间存在真实布局变化，等宿主准备完成后单独计量静止窗口，不把过渡变化当作空闲循环。全部失败样本保留。

当次8组UI回归、39项专项、原始宿主49项、安装卸载结果见[证据](../enhancements/tests/evidence/popup_refresh_20260923.json)。其他电脑、Win10和物理鼠标环境未到场，不能由本机通过替代。

## R069.7 目录预览验证（2026-09-23）

- BUG-preview-008：目录链接被交给普通文本读取而无法浏览。目标先经共享文件提供者异步判型，目录只读一层并使用可视列表；文件预览保留返回目录上下文，历史与正文隔离。
- TEST-preview-directory：隐藏Electron窗口可能不产生绘制帧，虚拟列表测试改用离屏渲染；固定语言的排序假设改为验证实际最后索引。原生列表保留获焦离屏行，选择文件的断言须按文件语义匹配。普通Markdown标签可能是工作台渲染视图，不能只用File.bundle判断打开失败；实际验证活动标签、正文内容和非链接预览视图。首次失败日志保留，不作为最终验收。

本次用例、实际结果及平台边界见[目录预览证据](../enhancements/tests/evidence/preview_directory_20260923.json)。

## R071.2 Git大清单与执行背压（2026-09-23）

- PERF-git-snapshot-copies：整段execFile、重复status/diff、全量JSON副本和同步排序共同造成压力；改为流式背压、单次状态快照与协作投影，界面保持虚拟行。
- BUG-git-pathspec-length：多文件路径不能直接塞入Windows命令行；add/reset/restore共用NUL stdin，仍是一笔Git操作。
- TEST-git-byte-contract：新TextDecoder默认去除BOM，首次完整回归在混合换行选区暂存中检出摘要变化；最终保留BOM并回归原选区用例。不得为通过测试跳过字节保真。
- TEST-git-ui-snapshot：原UI测试按两份status和全部按钮计数；改为验证单份status、非等待时取消按钮隐藏及慢读取实际取消。分批任务需等待生命周期完成再检查DOM。

本轮完整证据及平台边界见[大仓库Git验证](../enhancements/tests/evidence/git_scale_20260923.json)。

## R069.8 预览事件边界（2026-09-23）

- BUG-preview-shadow-wheel：document捕获阶段从composedPath首节点取得影子树的#write，closest查不到外部所有者，先执行主窗口缩放。沿完整传播路径排除独立预览；旧版组合测试失败，修复后真实Chromium滚轮只改变命中实例。
- BUG-preview-link-modifier：旧预览拒绝Ctrl链接点击。按用户最新澄清同时支持普通单击、Ctrl/Command单击和右键菜单；普通拖选不跳转，菜单随目标/阅读器销毁。
- BUG-preview-browser-rejection：显式默认浏览器入口未等待异步启动，拒绝可能成为全局未处理错误。当前所有者等待并捕获，过期错误不更新新目标；网页frame独立清理回调和等待计时。
- TEST-preview-shadow-integration：仅对预览外层派发wheel无法覆盖影子树捕获次序；新增真实内部composed事件并同时绑定主缩放监听。真实网页拒绝内嵌与可观察error事件分别记录，跨域load不等同成功显示。
- TEST-preview-native-inline：原生夹具中的HTML关闭script字面量会截断运行器内联脚本；拆分字面量后重跑。初次无checks回执的运行不算通过。

本轮结果与平台限制见[预览隔离证据](../enhancements/tests/evidence/preview_isolation_20260923.json)。

## R071.3 阅读滚动与刷新串扰（2026-09-23）

- BUG-reading-geometry-scan：普通滚动每次扫描所有块/标题和离屏字符节点；按根节点共享排版几何，失效才测量，滚动复用坐标。
- BUG-reading-scroll-owner：预览scroll触发主文面包屑与阅读服务；入口先判实际滚动所有者，标题树不在每帧重建。
- BUG-preview-theme-work：每个预览/后台比较在无关body状态变化时重新序列化全部样式；共享样式缓存和主题通知，消除同值写入及无变化概览刷新。
- BUG-navigation-preflight-race：旧发行版生命周期测试复现异步预检期间两个导航同时进入；事务在首次await前占有，退出统一清理，现有取消用例回归通过。
- TEST-breadcrumb-theme：旧暗色测试只设变量但实际背景仍为浅色，主题服务将其纠正；夹具补齐实际背景，保留原颜色断言。

运行样本和边界见[性能证据](../enhancements/tests/evidence/reading_performance_20260923.json)。原生鼠标硬件与Win10现场未覆盖；用户暂缓的未编辑保存提示仍保持暂停。

## R070.8/R069.9 资源树与预览导航（2026-09-23）

- BUG-picker-flat-tree：旧选择器在平面目录上画箭头，单击只有选择。改为复用Explorer的节点缓存、按需展开、键盘和可见行，选择与确认归各入口所有。
- BUG-picker-static-cascade：原生静态样式中通用breadcrumb的固定flex覆盖地址栏剩余宽度；通用对话框按钮颜色覆盖远端选中。明确地址轨道布局和选中状态的所有者，专项测试直接加载完整发行CSS，复验实际宽度与非悬停颜色。
- BUG-picker-stale-selection：输入路径/展开定位期间切目录后，迟到失败或选择可能更新新视图；按目录代次拒绝。保存目标与选择草稿分开，取消覆盖后重试不将文件名当成目录。
- BUG-preview-history-actions：预览只有Alt历史，工具栏缺少入口；图标与同一实例历史及忙碌状态同步，新链接清空旧记录，不能调用正文历史补位。
- TEST-picker-overwrite-retry：新增覆盖取消用例最初在取消同一事件回合点击仍忙碌的保存按钮；改为等待真实忙碌状态解除后操作，保留失败日志并重跑。
- OBS-preview-reflow-timing：并行运行完整检查/安装时，原生预览一次在既有边距字符锚点断言失败；未改实现或放宽阈值，串行空闲重跑最终候选222项通过。单次现象不足以判定根因，失败样本保留，不作为负载下位置稳定的通过证据。

实现、系统回归与交付边界见[本次证据](../enhancements/tests/evidence/preview_tree_20260923.json)。

## R034.3/R067.1 阅读主题与编辑器导航归类（2026-09-23）

- BUG-preview-inherited-theme：Shadow根挂在功能区，其继承字体不是原生正文；原主题提取还遗漏根变量作用域、禁用和media状态。共享适配继承正文计算值并保留有效主题条件，搜索/链接、Git历史和渲染比较统一接入，滚动不重新提取CSS。
- BUG-revision-fixed-skin：历史阅读器另写固定标题与表格样式，覆盖选中主题；保留布局与业务标记，移除独立皮肤和重复主题观察器。
- BUG-history-resource-classification：导航仅登记Markdown/源码，忽略作为主编辑器的只读版本和比较页；资源身份包含版本与编辑组，通过唯一导航服务恢复模式、选区、滚动和已关闭页。浮动预览会话仍独立。
- BUG-diff-restore-first-change：首次原生关闭重开回归检出Monaco异步首次差异定位覆盖恢复位置；恢复等待实际差异计算并抑制默认首次跳转，不能用固定延时或只验收现存标签掩盖。

分类与新增接入契约见[导航设计](navigation_history.md#视图分类与新增接入契约)，主题边界见[R034.3](git_markdown_diff.md#r0343-当前主题继承2026-09-23)。当次失败样本和通过结果随交付证据保留。

## R069.10 临时预览交互归属（2026-09-23）

- BUG-preview-outside-lifecycle：外部选区和工具操作缺少统一收起入口，链接预览持续占用空间；dock管理固定/关闭，选择控制器按交互所有者通知，取消旧选区延迟。
- TEST-preview-offscreen-focus：隔离Electron offscreen窗口的focus()更新activeElement但不发focusin；焦点专项显式派发事件，另以Chromium真实鼠标输入验证外部关闭，不将合成焦点写成物理键盘验证。

验收与平台边界见[本次证据](../enhancements/tests/evidence/preview_pin_20260923.json)。

- BUG-preview-root-variant：.18提取:root后遗漏html.dark等根变体变量，使暗色预览标题沿用默认色；保留变体条件并仅复制变量，使用已有lookup真实CSS用例回归。

- TEST-existing-search-interaction：扩大关联回归时，搜索夹具等待release_search_read/release_search_git与可见结果组合超时；交互夹具仍访问已移除的.git-scm-group节点。在未修改fc3cbbd源码和对应正式CSS中复现相同失败，本轮保留为既有测试维护项，不能计入通过；最终本轮专项、lookup、选文搜索、交互默认与阅读性能另行验收。


## R040.3 启动恢复优先级与标签加载（2026-09-23）

- BUG-session-restore-040：历史文件逐个前台打开再切回首屏，旧单元20条触发21次打开；原始宿主实际30文件退出重开约14.5秒后抢占首屏。改为后台身份登记和一次可取消激活，真实重启32+152项通过。
- BUG-native-tab-latest-040：宿主加载忙时第二次标签请求被丢失；最新请求等宿主可用，旧事件及位置回调按视图归属取消，原生20轮快速双切通过。
- BUG-native-dirty-pending-070：取消此前暂缓；本次无编辑场景没有产生脏状态，保存弹窗独立原因尚未复现，继续保留。详见[证据](../enhancements/tests/evidence/session_priority_20260923.json)。


## R040.4 后台源码标题（2026-09-23）

BUG-session-source-label-040：源码标题只在onOpen从内部URI修正为文件名，按需恢复暴露未激活标题。将身份元数据同步与正文打开分离，插入后同步标签；旧候选原生断言失败，新候选70+5项通过。用户本机另反馈R040.3错乱和保存弹窗未再出现，保留其他环境边界。见[证据](../enhancements/tests/evidence/session_label_20260923.json)。

## 2026-09-24 R070.9 地址编辑与目录搜索

- BUG-picker-mousedown-enter：选择树mousedown提前进入目录，使真实单击与程序click行为不同；移除重复进入，只由click展开/选择，双击与Enter进入。原始Typora真实SSH55项及完整指针序列回归通过。
- BUG-picker-address-scope：路径探测期间仍可确认旧目录、正则先映射Windows缓存被拒绝；提交立即禁用确认，远端POSIX stat优先，搜索取消与迟到回调绑定地址请求及工作区所有者。最终原生/目标UI通过，早期失败保留在本轮证据。
- TEST-existing-search-interaction部分收敛：搜索等待夹具改为延迟实际spawn进程的close，替代已失效的execFile拦截，files_search已通过；旧SCM节点夹具问题未在本轮关闭。

当次证据见[地址栏验证](../enhancements/tests/evidence/address_picker_20260924.json)，平台边界不以旧结果补齐。

## 2026-09-24 R034.5–R034.8 呈现与选择职责

- BUG-text-wrap-owner：换行由各实例持有，默认关闭，菜单与设置未共享；统一版本化呈现服务，首次/旧契约采用新默认，当前及未来契约保留。
- BUG-diff-layout-anchor：渲染和源码各记像素，切换后回到旧位置；跨布局改用源行/列/语义块比例，相同布局导航仍保留精确像素，输入优先。
- BUG-markdown-range：块尾空行误算进标题范围；删除尾部空行，并细化连续同类块、表格单元格与列表项，概览/下一处同步目标。
- BUG-git-expanded-selection：父提交aria-expanded被误用为选中样式，展开区域padding与普通/虚拟缩进不一致导致背景长短不一；展开/焦点/当前文件来源分离，整行背景仅内容缩进。
- TEST-presentation-migration：旧布局测试测容器边界而非文件内容起点、旧交互测试要求展开底色，按新契约分别检查；列宽夹具的相对仓库路径被服务身份验证拒绝，改用绝对临时路径，未放松产品验证。

本次验证状态和平台边界见[候选证据](../enhancements/tests/evidence/git_presentation_20260924.json)。

2026-09-24补充R020.1/R034.9与BUG-diff-layout-anchor：

- BUG-selection-expanded-source：展开提交、键盘焦点和活动diff被混作选中；共享列表模型分离三者，同一来源刷新不覆盖手动选择，真实宿主提交/文件转移通过。
- BUG-diff-layout-anchor补充：Monaco迟到同值差异导致已显示DOM重建，映射短暂失效；按模型版本与差异结果合并，保留待恢复锚点，100次同值通知与最终原生164项通过。
- BUG-markdown-diff-decoration：整格高饱和背景盖过主题与具体改词；改为内容浅色、行内强调及修改边线，配色/原生回归通过，复杂结构仍有块级边界。证据见[本次记录](../enhancements/tests/evidence/shared_selection_20260924.json)。

2026-09-24 R065.2：BUG-menu-per-item-columns——R065.1按每行能力撤销标记空间，混合菜单文字起点不一致；用户明确全部按VS Code排版。统一菜单层2em标记/文字起点和快捷键列，普通项保留menuitem语义，状态变化不改几何。覆盖顶栏八类、共享与核心菜单，旧紧凑间距断言按新授权替换。

2026-09-24 R047.7：NET-request-trust-proxy——浏览器与Node代理/信任链来源不同，原更新请求未提供企业配置。共享请求级Agent显式携带代理和附加CA，保留默认根、主机名验证、域名白名单及重定向/取消。真实回环TLS、HTTP/HTTPS CONNECT、原生宿主与worker通过；企业现场仍待验证。UI夹具缺虚拟搜索Worker构建插件与CONNECT夹具半关闭清理归测试接线问题，最终复验通过。见[证据](../enhancements/tests/evidence/network_settings_20260924.json)。
