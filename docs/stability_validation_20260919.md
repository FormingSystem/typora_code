# 2026-09-19 稳定性修复与验收

本轮从干净的`9164530f11f7a779ff11643618f7e94c969c231e`工作树开始，先登记R039—R045、影响分析和领域设计，再修改产品。需求与测试定义入库，原始运行日志和截图留在忽略的证据目录；开发skill同步加入现行流程并通过校验。机器可读结论及逐套件关联见[本轮证据索引](../enhancements/tests/evidence/stability_20260919.json)，后续维护入口见[测试架构](stability_testing.md)。

## 修复及行为边界

| 需求 | 本轮结果 | 保留的边界 |
| --- | --- | --- |
| R039 | 114个已有/新增套件统一编号、分类及需求/设计/实现关联，统一报告保留版本/资产/源/日志摘要、失败与未执行；问题按领域和根因聚合；skill已更新 | 套件数不等于内部断言数；不能把某一层通过代替全部环境 |
| R040 | 成功打开文件夹后写宿主最近目录；每次打开菜单读取最新历史；失败/取消/迟到处理有测试 | 原生历史读回通过；未另建本地历史副本 |
| R041 | 文件/Git统一调用主进程回收，拒绝/异常/假成功均可见；普通已知目标动作直接执行，参数表单一次提交；删除尊重原生警告配置 | 本机真实临时文件及目录回收通过；另一电脑的权限/占用/版本因素仍需该机复现，未通过绕权或永久删除兜底 |
| R042 | `111`→`1111.md`的语言和默认视图自动更新；同一dirty模型及撤销状态保留 | 未保存草稿继续可编辑并立即获得Markdown语言模式，保存成功后进入原生Markdown；改名不静默保存 |
| R043 | 共同阅读视口处理真实顶部遮挡，原生显式章节跳转适配偏移；编辑时隐式滚动保持 | 原生Github/Night标题完整可见，关联大纲/面包屑/缩略图/终端布局回归 |
| R044 | 核心菜单图标外壳及实际字形共用16px槽与独立文字列；Git活动图标24px规则限定于活动按钮 | 原生两主题截图已视检，隐藏Electron补充菜单/焦点/生命周期；不把外框对齐当作字形对齐 |
| R045 | 现有入口逐域核对及回归；底栏分支区分`*`/`+`/`!`、分离HEAD、同步数量；同类菜单/主题范围一起修正 | 上游保护分支等复合图标和未授权的完整扩展生态没有伪装成已实现；既有未完成终端/平台项仍保留 |

## 本次验证

最终候选`dist/SHA256SUMS`的SHA-256为`d626e958950b797f5fa0584d1d1a6a2fe855bf7e07630378678fd1353034f93f`。构建及`npm run check`通过，目录检查114项，`git diff --check`通过。全部结论基于本轮执行，没有沿用旧通过数量。

| 层级 | 实现验证 | 压力验证 |
| --- | --- | --- |
| 单元 | 14套件通过 | 1套件通过 |
| 功能 | 85套件通过 | 3套件通过 |
| 系统 | 6套件通过，4套件未执行 | 1套件通过 |

合计110个不同目录条目通过、4项未执行。完整功能轮次为86通过/1失败；失败是新增核心菜单几何断言没有加载CSS，修正夹具后该套件独立复跑通过。通用事务等待、框架配置补充及元数据归类后也各自复跑；不把存在失败的原报告改写为全绿。逐项采用哪个运行结果见证据索引。

20、100、1000三档各运行5个压力套件：纯状态随机属性（固定种子20260919）、临时文件/目录端口循环、原生菜单生命周期，以及两个既有固定负载搜索/快速打开测试。后两项报告iterations为空、保留实际工作负载，不冒称执行了指定千轮。原生压力在专属宿主和用户目录完成，菜单节点数量没有增长，结束状态及原始文档字节保持；不等于所有模块已做长时间浸泡测试。

原始Typora 1.14.10独立宿主副本中21项实现断言通过，包括真实最近历史、clean/dirty改后缀、保存后的默认视图、实际文件/目录回收和两主题标题/菜单几何；固定原始ASAR摘要未变。输入方式是renderer事件与真实宿主API，独立桌面没有切换到用户桌面，不能表述成物理鼠标或跨账户验证。启动采样DPR=1.25、窗口zoom=1；驱动随后调整独立窗口取图，截图和每次几何值保存在原生证据中。

四个明确未执行项：真实Windows文件剪贴板互通、旧原生注入型阅读测试、需独立Shell环境的终端交互、Bash安装包装器。前两者不能借默认运行覆盖用户剪贴板/安装；本轮原生验收走新的隔离入口。另一个Python安装事务在临时目录通过，但不冒称Linux原生桌面通过。clangd服务与隐藏界面已使用本机clangd实际运行。

## 过程失败与复跑

[问题索引](stability_issues.md)保留宿主样式覆盖、普通Git多次确认、PS7→Node→PS5.1模块路径污染、提交准备阶段被误判为完成、原生复制遗漏appsrc、核心夹具缺CSS等根因。原先的footer/interaction夹具缺少现有服务字段也在本轮补齐。构建初次核心来源摘要不匹配后重新核对来源清单；原生标题验证最初将“可见”错误限定为距顶10px，依据实际宿主行为改为检查标题完整位于可见区域，保留原失败。

构建/检查日志位于`.cache/issue_tracking/build_verified_20260919.log`、`check_verified_20260919.log`；独立运行和历史失败均在`.cache/issue_tracking/runs/`，汇总索引保存报告摘要。测试期间候选变化的早期报告明确为incomplete，没有用于独自验收最终候选。源和证据定义可从当前提交复现，原始大日志没有混入产品运行包。

## 安装与加载

通过现有Windows事务入口安装，安装检查返回`OK`；23项资产全部与上述候选一致。宿主ASAR、宿主图标、用户偏好、工作台设置和项目主题五项保护摘要保持；没有强制关闭或重启用户窗口。证据为`.cache/issue_tracking/install_verification_20260919.json`及同目录安装/检查日志，备份编号`20260919-182851-690-58161368bd78471a807910beb1e51839`。

用户保存文档后正常重启Typora才会加载本次版本。资产安装成功不表示当前旧窗口已加载，也不替代另一台电脑的实际权限验收。代码、测试、设计及对应构建一并提交；本轮不推送远端。
