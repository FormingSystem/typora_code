# R047 工作台原地更新

## 需求与交互

2026-09-19用户要求：从本仓库ZIP原地更新，启动时以全部窗口为整体只提醒一次，展示版本公告和已修复问题，可稍后更新。同日澄清选择立即安装、手动重启生效，替代最初提出的退出后安装方案。

工作台就绪后异步检查，不阻塞编辑。以Windows宿主主进程PID与实际启动时间作为会话身份，用用户目录中的排他创建完成跨renderer去重；关闭首窗再开另一窗不会重复提醒，真正退出再启动可再次提醒。网络失败、离线和限流只写日志；用户可从“帮助→检查Typora Code更新”重试。同一窗口内检查、弹窗和执行各自串行，公告为纯文本。稍后／Esc关闭不下载、不安装；点击更新后立即下载、校验、安装，独立后台进程在来源窗口关闭后继续。下载与校验阶段可取消，进入安装事务后只能关闭进度窗，不能中途杀进程。成功提示保存文档后手动重启，绝不强行退出、切换文件或清空草稿。

## 发布身份与公告

固定官方来源`FormingSystem/typora_code`的`main`分支。先通过GitHub提交API取得40位提交SHA，再从同一SHA读取`enhancements/release.json`与发行摘要，下载同一提交的ZIP；不会显示一个版本的公告却安装后来移动的分支。依据[GitHub ZIP API](https://docs.github.com/en/rest/repos/contents#download-a-repository-archive-zip)和[提交API](https://docs.github.com/en/rest/commits/commits)。

`release.json`是版本控制内的发布契约，包含递增整数sequence、显示version、日期及面向用户的修复条目；构建复制到安装资产。2026-09-20 R064替代仅按sequence判断的约定：提交SHA作为更新身份，sequence继续保护未推送候选不被旧远端降级。每次对外发布功能／修复必须增加版本并写公告、构建验证后一起提交；同序号不同SHA也可更新，公告显示目标提交说明。保留历史公告，跨版本更新展示全部适用条目。GitHub仓库ZIP包含预构建资产，用户无需Git、npm或开发环境。

## 状态、职责与实现

UI模块复用workspace_dialog、焦点栈及公共按钮，只负责公告／进度。Node更新服务拥有版本解析、HTTPS检查、会话去重和独立worker；状态与日志放用户数据目录的`temp/typora_code_updates`，不放用户工作区。worker复制自身及解压适配后运行，避免更新覆盖正在执行的辅助脚本；只接受固定HTTPS域名、限制重定向／超时／体积，ZIP拒绝绝对路径、上级路径、重复大小写目标、链接与解压膨胀。元数据、dist清单及所有资产的摘要必须一致，公告不执行HTML。信任边界为固定官方GitHub提交的TLS来源与其清单，不将自带SHA清单声称为独立数字签名。

安装复用下载包内现有Windows事务安装器，传入实际宿主与用户数据位置，保留原生ASAR、设置和文档，已有备份与错误回滚继续适用。安装器增加跨进程互斥，手工安装与自动更新不能交错写入。2026-09-19并发验收发现独立用户目录仍共享Node缓存：prepare_typora_node无条件重新解压，可能覆盖另一安装事务正在使用的验证程序。缓存服务增加按缓存位置／版本／架构的独立互斥，有效摘要直接复用，异常退出后重新校验；状态属于缓存服务，不由UI另加锁。立即安装更新磁盘资产，已有窗口保留当前内存版本；不进行renderer热替换。安装器按既有文件事务写入，并非整套目录原子切换；进度期间不主动新建窗口，完成后再手动重启。含中文注释的PowerShell辅助脚本以UTF-8 BOM交付，兼容系统自带5.1。下载、校验、安装失败和回滚失败分别记录；按R063在实际写权限不足时解释并请求一次系统UAC，取消保留原版本；不以成功提示掩盖权限或文件占用。当前自动安装覆盖Windows x64／ARM64的既有安装入口；其他平台明确显示未支持自动安装，不猜测提权或运行时路径。

发布的首次升级需用户先安装一个含更新模块的版本；旧版本没有这一代码，不能凭远端提交获得提示。尚未推送的候选也不能被其他用户发现。

## 影响与验证

影响启动生命周期、帮助菜单、构建资产、安装互斥、备份恢复和跨窗口状态；文件／Git／终端业务不新增状态。目标测试覆盖版本和公告校验、20／100／1000并发争抢、重复启动、重启新会话、网络失败／限流／取消、固定提交、恶意ZIP、摘要不一致、安装失败与进程崩溃，UI覆盖公告纯文本、稍后、重复点击、忙碌和销毁。Windows隔离目录执行真实ZIP解压与现有安装／恢复，原生宿主验证启动和菜单；不更新真实用户仓库或推送远端用于验收。测试目录关联R047，日志和候选摘要按统一证据架构记录。

## 本轮验证与交付

2026-09-19：9个目标套件、11次最终报告全部通过，包含20／100／1000争抢档；标准build/check通过，缓存修正后补验真实并行安装、回滚与部署检查。原始Typora1.14.10独立副本40项检查通过，其中3项直接验证更新启动和手动入口，其余覆盖既有文件／导航／侧栏。中文可执行路径、非默认中文用户目录、运行中的私有／共享缓存Node、独立worker及危险ZIP均有系统用例。网络及发布内容使用替身，没有把未来远端在线发布声称为已验收。

安装后check为OK，27项发行资产与候选一致，5项宿主／配置保护摘要不变；用户窗口未关闭或重启。开发skill和发布规则已同步。完整需求、源码身份、用例、失败报告、资产与日志摘要见[本轮证据](../enhancements/tests/evidence/workspace_update_20260919.json)。首次PowerShell编码、写盘错误误报及共享缓存占用按BUG-update-001／002／003归档，修复后分别回归；旧报告保留。

本轮仅本地提交和安装，未推送。首次采用者须先安装含更新模块的版本；后续正式公告和资产推送后才可触发在线提示。ARM64实机、其他平台自动安装与既有终端／跨机权限缺口保持开放。

## R055 信息同步与历史发布

2026-09-20用户授权全量同步更新信息、按改动类型提交并强制推送。范围为当前工程和已有远端main；用户说明维护当前行为，领域设计维护实现约束，反馈/机器证据保留当时结果，交接说明当前状态。本地台账继续忽略，不上传私有运行目录或个人技能目录。历史记录中的“未推送”表示当时状态，由新的发布记录覆盖当前状态，不篡改旧证据。

分类以可独立审阅的结果为准：使用说明与功能范围、架构及验收交接、远端发布回执分别提交；既有实现提交保留其必要设计、测试及构建，不为凑类型拆散已验证依赖。正文逐条说明行为变化，不能只列文件。此次不改变发行资产或增加虚假版本号，沿用已安装验证的2026.09.20.2。

发布前fetch并核对远端OID，检查历史映射包含旧远端且文件树一致，验证完整bundle备份、提交消息、发布清单和安装一致性。仅对明确的main使用带旧OID的force-with-lease；租约失败停止覆盖并重新核对，不改用无条件force。发布后ls-remote复核提交并读取固定提交的公告；网络或权限失败如实保留待发布状态，不把本地安装当作远端发布。

### 2026-09-20 发布回执

已按用户授权，将origin/main从9164530强制更新到eee459a（包含功能候选9dd1db8及分类说明提交）。显式force-with-lease成功，ls-remote核对一致；固定提交的公告、27项资产清单及静态head与Git内容逐字节一致，远端当前发布版本为2026.09.20.2。随后仅追加本回执，不改变发行资产。备份摘要、完整OID和核对范围见[机器可读回执](git_history/20260920_publication.json)。

已有更新模块的用户可检查到公开公告；未安装该模块的旧版仍须先下载完整ZIP安装。此次验证远端内容可读，不替代其他机器实际弹窗/安装验收。本机安装继续为同一候选，正在运行的窗口仍需用户手动重启；此前长任务和跨平台未决项不关闭。

## R064 无Git安装的提交身份与temp目录

2026-09-20用户明确：其他电脑从GitHub ZIP安装，没有Git或历史目录；更新必须按远端提交hash获取同一提交的ZIP。本规则替代R047仅按发布序号判断更新的约定，不使用clone、fetch或用户工作区的Git状态。

Node更新服务从GitHub API读取main的40位SHA，再锁定该SHA的公告、清单及ZIP。成功安装并核对磁盘清单后，在用户数据目录写`typora_code_update_identity.json`回执，记录SHA、发布序号和资产清单摘要；仅与当前磁盘清单一致的回执有效，手工换装不同资产后自动失效。相同SHA不重复安装，同序号不同SHA提供更新；远端序号较低仍不自动降级开发候选。首次ZIP安装没有SHA时，若远端清单与已安装清单完全相同且序号一致，建立资产等价身份，无需重复下载安装；否则提供ZIP更新。等价身份不是声称掌握原下载包的Git历史。回执缺失、损坏可重新建立；下载、取消、校验或安装失败不推进回执。

发布公告继续展示具体修复；同序号提交补充目标短SHA与GitHub提交说明（纯文本）。安装前重新核对当前身份以拒绝重复/过期任务；已有窗口即使版本号相同也必须手动重启加载新提交，不热替换UI。

默认下载、解压、worker与日志位于宿主传入的用户数据目录`temp/typora_code_updates/<任务ID>`，目录缺失自动创建。这里是Typora用户数据temp，不是受保护的程序安装目录；创建缓存不会申请管理员权限。永久身份回执与临时缓存分离，清理temp不丢失已安装SHA。旧缓存不迁移或自动删除，避免碰到仍运行的旧worker。下载缓存策略不更改用户配置/备份的位置，也不能豁免安装目录的Windows权限；确需写受保护入口时沿用R063的一次解释与UAC。

验收关联TC-update-protocol、TC-update-concurrency、TC-update-install及TC-update-ui：无.git且子进程PATH没有Git的真实ZIP事务；首次身份、相同SHA、同序号新SHA、旧远端、失败回执不变；temp自动创建、跨窗口20/100/1000争抢、公告及手动重启提示。网络用固定SHA响应替身，真实在线发布和跨账户UAC不得冒称实测。

R064本轮交付：2026.09.20.14普通权限安装与检查通过，29资产一致、6项宿主/配置保护不变。最终4更新套件及20/100/1000档通过，安装包无.git、子进程PATH无Git；真实ZIP事务与Electron公告交互通过，网络为替身。构建占用失败及恢复复验均留证，用户待手动重启、未推送。详见[证据](../enhancements/tests/evidence/update_commit_20260920.json)，此前未决边界继续保留。

### R055 本次增量发布设计（2026-09-20）

本次用户再次授权同步全部修改信息、提交并推送。发布候选为已验证安装的2026.09.20.14，包含R061折叠槽位、R062托管网页、R063权限续装和R064提交ZIP更新。首页、模块说明、功能矩阵与安装指南描述当前能力，历史验收保留原日期和当时状态；不修改既有证据来声称补做过实测，也不因说明同步新增功能版本。

先fetch核对origin/main与HEAD，确认可快进后普通push，不沿用早前历史重写的强推方式。提交前复核消息、目录差量、29项资产、版本公告及安装一致性；推送后读取远端OID与固定提交公告/清单。失败保留已提交候选与错误信息，不标记已发布。仅文档变更无需重复安装，本机继续使用同一候选；发布不代表用户现有窗口已经重启或此前跨平台/UAC/长任务问题已验收。
