# R034.1 Markdown渲染差异

2026-09-23用户要求：Markdown比较不能只有源码，需把左右版本排版后着色，直接聚焦可读内容。Markdown双版本比较默认进入渲染视图；同一顶栏用预览图标切换源码，统一差异设置保存偏好。普通源码文件及单版本阅读保持原行为。

## 交互和职责

比较身份、Git读取、错误和刷新继续由现有Git文档服务拥有；Monaco的异步差异结果是唯一变更范围来源。渲染视图不自行读工作树、保存文件或启动第二次Git扫描。按完整Markdown块映射源码行；未改块作为左右对齐锚点，变更块左红右绿，新增或删除侧留出对应空位。标题、列表、表格、引用和围栏保持结构；颜色之外显示删除/新增语义标签。工具栏前后变更及F7定位当前视图，打开原文件与整文件动作保留。选区暂存/撤销只在源码侧操作，渲染态明确禁用；查找切到源码定位，不能操作隐藏选区。

共用预览主题提取与代码/图表渲染，Shadow DOM隔离原生编辑增强。历史HTML净化，链接只呈现文字和目标，不自动打开外部页面；图片显示替代文字和地址，避免用当前图片冒充历史图片。Front Matter及不可见定义变化显示源码块，防止丢失变更。修改仅排版语法时仍标记所属块。当前阶段提供块级着色，不承诺逐词着色、完整Typora数学/HTML扩展或历史附件读取。

一次比较共用滚动容器，成对块按较高一侧对齐；窄视口仍保留左右两栏。切换模式保存各自滚动；刷新/切设置/关闭使用代际拒绝迟到渲染并释放图表资源。差异计算未完成显示等待；过大内容（两侧合计超过1MiB UTF-8或单侧超过10000个lexer token）保留源码视图并提示，不阻塞正文。格式化和DOM构建按批次让出线程，不扩大上一轮大仓库内存路径。

## 来源与验证

颜色采用已锁定Monaco的`vs/platform/theme/common/colors/editorColors.js`：`diffEditor.insertedTextBackground`浅色`#9ccc2c40`、暗色`#9ccc2c33`，`removedTextBackground`两种主题`#ff000033`；块级运用为本产品适配。图标复用已核对Codicons的`preview`，工具栏几何和交互沿用R034。渲染Markdown双栏不是声称VS Code内置源码差异等价。

验证真实Monaco和Git比较中的标题/段落/列表/表格/围栏/引用/定义、纯新增/删除/不变、相同块重复、CRLF、刷新/取消迟到、20次切换、主题/窄窗/缩放、源码选区与历史只读、宿主正文不变及安装卸载。当次证据与未覆盖平台在交付时补充。

## 本次交付验证

2026-09-23交付2026.09.23.11（R034.1）：Markdown双版本比较默认并排排版，完整变更块左红右绿、对应行对齐；复用现有差异计算及工具栏，支持切回源码、差异定位与刷新，渲染态禁止隐藏源码选区写操作。构建、完整check及最终静态/部署补验、202套目录和5组UI通过；渲染专项24项、原始Typora55项通过，含600段功能压力、20次切换、主题/窄窗/缩放和真实Git刷新。相同候选两类隔离卸载重装、65卸载断言通过；本机38+24资产一致、5保护文件不变、check OK，实际卸载仅只读预检。用户窗口未重启，保存后正常重启加载。块级着色、历史图片替代展示及容量/平台边界见[设计](git_markdown_diff.md)和[当次证据](../enhancements/tests/evidence/git_markdown_diff_20260923.json)。
