# TyporaCode 协作规则

## 工程边界

TyporaCode 是独立维护的 Typora 工作台增强工程。所有实现、构建、测试、运行说明和开发交接均在本仓库维护。进入仓库先读本文件、[README](README.md)、[增强模块说明](enhancements/README.md) 和 [开发交接](docs/development_handoff.md)。

本工程不依赖原知识库的目录结构、AGENTS、根脚本或本机绝对路径。不得回到原仓库开发、维护双份代码或增加跨仓库同步流程。上游研究缓存属于本仓库忽略的 `.cache/`，不构成运行依赖。

## 修改纪律

- 用户需求台账保存在 Git 忽略的 `.cache/issue_tracking/requirements.md`。开始工作先读台账；每条新反馈先追加编号、原意、验收条件和状态，再实施。后续消息默认补充既有任务，不能覆盖未完成项；重复反馈并入原编号并保留复现日期。
- 完成项必须附本次验证证据和提交／安装状态；未经验证不得标为完成。收尾逐项核对本轮及此前未完成项，未解决项写明原因和下一步。台账丢失时从当前对话与 `docs/feedback_review.md` 重建，不能把历史通过记录当成本次复查结果。

- 说明、注释和交接使用清晰中文；先说明实际行为、触发条件和验证结果。
- 自有代码的文件、目录、类型、函数、变量及接口字段统一英文 `snake_case`；常量允许 `UPPER_SNAKE_CASE`。语言、系统和第三方强制的接口名保留上游形式，不扩大例外。
- 编辑前检查 `git status --short` 和相关差量，保护已有未提交内容。不得覆盖、清空、取消暂存或提交用户的无关改动。
- 使用当前目标架构，查清真实调用关系后重构；不擅自增加旧接口转发、双运行、双写或废弃路径别名。
- 全部功能遵守职责分层：UI 独立组织呈现，共享业务操作通过命令／服务调用；同一业务状态和配置只由一处管理，原生能力集中适配，事件与资源按所有者统一清理。此规则覆盖文件、搜索、大纲、Git、终端和窗口管理，不局限于资源管理器。抽象依据真实共享职责，不做无用途的通用框架，也不把独立领域塞进一个大模块。
- 文档与脚本读写显式使用 UTF-8；长期文档不写个人目录、盘符、共享地址、凭据或令牌。
- UI 修改需检查同类面板的几何、主题状态和键盘交互；不得以局部截图或旧测试通过宣称完整验收。底栏等同一布局区域发生重复偏移时，先修共同布局契约并回归所有同类控件；新增底栏控件必须复用共享组、操作及文本角色，不在各模块分别增加margin/top补偿。 原生状态类不能直接扩散到独立菜单；复查必须覆盖实际文字行盒、图标和非悬停可见性，不能只比较外层矩形。
- 普通控件使用公共工厂的默认交互，视图通过 `acquire_workspace_interaction(root)` 登记语义控件范围，动态新增按钮自动接入。统一层管理悬停颜色、默认4px控件圆角和键盘焦点，各 UI 保留自己的尺寸、选中/展开与禁用状态；圆形、分裂按钮等独立形状通过 `--workspace-interaction-radius` 声明，不能以局部直角默认覆盖共享规则。独立差异通过角色、CSS 变量或显式 `none` 边界声明；不为普通按钮复制另一套 hover 规则。正文及第三方编辑器不纳入全局重绘。
- 信息浮层复用共同外观与可复用的延迟、边界、焦点和清理机制，领域只拥有内容及异步数据；需要不同延迟时使用局部选项。标签和底栏共用内容居中规则，分别拥有区域高度。新增 UI 要验证动态加入、局部覆写、禁用/选中优先级和销毁恢复；完整契约见[工作台交互设计](docs/workspace_interaction.md)。
- 滚动条基础宽度、圆角和原生三态颜色由 `workspace_scrollbars.css` 统一；Monaco/xterm/阅读缩略图仅集中适配滑块形状，滚动及主题状态留在原所有者。新增面板不得复制基础规则，保留隐藏滚动条并验证真实绘制；任务进度与滚动条按语义分别核对。
- 工作台直接在 Typora 启动时加载静态样式和常驻脚本，不注册社区插件，不因打开文件或切换文件夹重建工作台。修改启动流程时验证首次绘制、单次初始化、监听器与 DOM 清理、未保存文档和运行入口一致性。
- 2026-09-20用户重新授权社区插件能力接入：先保证现有TyporaCode UI启动与交互，布局保持不变；社区API通过适配层接入同一工作台，不恢复第二套社区核心或UI。自有命名按snake_case，第三方强制ABI保留兼容。官方Typora更新后重新执行标准安装，原生启动图标不变；详见社区插件和启动设计。此约定覆盖此前禁止社区插件接入的对应限制。
- 不随意删除用户文件或笔记；可能影响用户数据、环境或不可逆操作时先说明具体范围。

## 需求与设计文档管理

- **每条需求必须有对应的需求设计文档或可精确定位的设计小节，并持续维护。** 此要求适用于全部功能，也适用于缺陷修复、性能优化、UI、配置、架构、文档和交付规则，不局限于文件或终端。
- 新反馈先登记稳定需求编号、来源日期、原意与验收条件，再在[需求设计索引](docs/requirements_design.md#当前需求设计索引)关联权威设计章节。设计文档放在版本控制内的 `docs/` 或已有对应功能文档中；会话台账与临时证据继续放在 Git 忽略的 `.cache/issue_tracking/`，不能以临时台账代替正式设计。
- 设计至少说明问题与目标、范围和不做事项、交互与失败／取消行为、模块职责与状态所有者、实现方案及重要取舍、验收和回归方法。按需求规模裁剪，小修复可更新原设计小节；相关需求可共享同一权威文档，不能每次反馈复制一份设计或只贴一个文件链接便视为设计完成。
- 实施前先补充设计；需求澄清、范围变化和实现调整发生时，同步更新对应设计、索引和台账，注明日期、原因及被替代约定。后续消息不能冲掉其他未完成项。缺少事实时标注待设计或待核对，保留未决问题，不凭空补出宿主接口或验收结果。
- 收尾逐条核对“需求 → 设计 → 实现 → 验证 → 交付”。设计覆盖与实现进度分别记录；仅有设计、部分实现或旧验证均不能标为需求完成。迁移、重命名或拆分设计文档时同步维护所有需求链接，未实现部分继续保留独立子项。
- 正式索引管理编号与设计入口，领域文档管理设计正文，本地台账管理进度和当次证据，[反馈记录](docs/feedback_review.md)管理交付历史；不维护多份相互独立的完成清单。台账缺失时先从正式索引恢复编号和文档关联，再核对对话与交付记录恢复状态。

## 定稿布局与功能范围

- 2026-09-09 用户已明确停止持续扩充和全量 VS Code 界面移植。以用户提供的周一截图和 `59412a2` 的布局、功能范围为参考，固化已稳定的优化，只修复已知问题；不得继续以“一比一”为由增加面板、菜单、设置页或改变默认操作。
- 保留原生七类菜单、连续活动栏、文件树、独立大纲、多文档与分栏、侧栏搜索预览及现有 Git 审阅。搜索单击在下方预览、双击打开。2026-09-12 后续授权已把终端改为底部面板并支持搬移到编辑器，文件操作保留原生能力并补充缺项；详见下文和当前功能说明。未被新需求覆盖的 Open Editors 分区、Command Center 菜单重组和 SCM 额外筛选框仍不引入。
- 这是在当前稳定实现上收回布局和功能变化，不是整库回退到旧提交。必须保留直接启动、异步取消、未保存草稿保护、文件切换和缩略图稳定性等已验证修复。
- 变更先说明对应的已知 bug，检查同类区域并验证，不进行未被要求的界面重设计。当前验收范围见 [功能矩阵](docs/workbench_parity.md)。

- 最新明确授权覆盖上述冻结中的顶栏限制：本轮按用户新要求恢复35px单行顶栏：左侧为 Typora 文件、编辑、段落、格式、视图、主题、帮助七类菜单，中间为后退、前进和文件搜索，右侧复用宿主窗口按钮。菜单由本地 renderer 组织，只调用已核对的 Typora API，不使用整棵 `Menu.popup` 或修改 ASAR；能力与动态状态以实际接线为界，不声称完整原生菜单等价。菜单在顶栏下方按可用高度滚动，支持 Shift+滚轮。
- 按用户最新要求，C/C++ 大纲统一接入本机 clangd，通过 LSP 使用当前内存正文和工程编译配置，点击符号精确定位；已移除 C/C++ Tree-sitter 路径。其他五种语言继续内置离线解析，Markdown 保留原生标题目录。代码大纲的设置入口支持 clangd 路径、项目相对编译数据库目录和后备参数，失败不覆盖原设置。详细范围见 [代码大纲与解析环境](docs/source_outline.md)。

- 用户后续明确授权搜索性能优化、文件标签原生拖出建窗、拖入另一窗口合并，以及侧栏工具等既有可拖动对象的左键交互；这几项按 [搜索性能](docs/search_performance.md) 和 [拖动与多窗口](docs/drag_and_windows.md) 的当前边界实现，不扩展为任意控件都可移动。
- 2026-09-11 用户进一步要求历史版本阅读、提交全部改动入口、清晰的文件行动作及VS Code式紧凑diff顶栏；宽窄视图按编辑组实际可用宽度自动切换。对应入口与边界见[Git功能说明](enhancements/git_graph_features.md)，不把这些授权扩展为完整VS Code扩展宿主。
- 2026-09-12 用户明确重新授权 VS Code 式文件菜单、资源管理器文件操作与缩进，以及底部终端面板和终端操作／配置。这些新要求覆盖此前对应的冻结限制。打开文件与文件夹必须使用宿主系统选择窗口；不能用路径输入框替代。逐项记录、实现并验证，不能把菜单外观视为功能完成。
- 同日进一步明确：保留 Typora 原生文件操作，对比 VS Code 补充缺失能力；只有现有结构无法承载时才重构。保留原生导入、导出、打印等实际操作，不为追求菜单一致而删除它们。

- 2026-09-12 后续明确要求新增顶部“终端”菜单，保留原生七类菜单；视图开关显示实际勾选。底栏重复的大纲操作取消，仍从活动栏和视图菜单进入。对应设计见 `docs/terminal_operations.md`、`docs/workspace_view_menu.md` 与 `docs/statusbar_layout.md`。

2026-09-19后续授权已恢复资源管理器的“打开的编辑器”和时间线分区（R037），并增加按工作区恢复文件及真实空编辑区（R040.2/R052）。这覆盖前文对应冻结限制，当前范围以领域设计和功能矩阵为准；R054只修复首次展示与侧栏移交时序，不扩充布局。

## 已核对的设计参考

- 已核对的 VS Code 版本、主题与设计来源见 [界面基线](docs/vscode_design_baseline.md)。这些资料用于修复具体图标或控件问题，不再授权覆盖上述定稿布局；用户升级 VS Code 也不自动改变产品界面。
- 每次修改图标、文字、颜色、尺寸、间距、圆角、布局或缩放，先查固定版本的对应源码和有效配置，再修改。提交应能追溯到上游文件、规则或常量、采用值、目标控件和验证结果；不能凭截图估算或个人审美替代源码证据。
- 新修复的产品图标使用已核对的官方 Codicons，保留原始路径及状态语义；按用户 2026-09-10 的最新要求，Explorer 文件与真实文件标签使用固定 VS Code 的 Seti 原始字形、关联及明暗颜色；该主题未定义文件夹图标，文件夹仅保留展开箭头。此要求只覆盖图标，不恢复 Open Editors、预览标签或 Modern 布局。禁止自行绘制近似图形或用文字箭头替代已有官方图标。
- 大纲活动按钮按用户 2026-09-10 的明确要求保留原生 `fa-list` 目录图标及原节点，不替换为 `symbol-class` 或其他近似图标。
- 左下设置入口保留既有 `fa-cog` 外形。2026-09-20用户进一步授权R060：左侧新增扩展图标/侧栏，齿轮共享菜单提供Typora偏好、插件设置和扩展；配置复用插件实际注册的SettingTab。此约定替代齿轮仅打开原生偏好和禁止插件入口的旧限制，不恢复第二套社区工作台。
- 相对文档链接以链接所在文档的父目录解析，文件交给宿主之前检查真实目标；打开失败不得清空当前文档或替换未保存草稿。链接悬停提示延迟为用户指定的 1 秒，只展示目标，不修改正文 DOM 或触发跳转。
- 工作台字号、编辑器字号、窗口缩放和系统 DPI 分别核对；不使用任意整体缩放或增大图标补偿布局问题。比较截图必须注明相同视口、DPI、缩放及状态。
- 核对默认、展开、折叠、活动、非活动、悬停、焦点、禁用、窄侧栏和长名称。复用同一规则覆盖同类控件；源码依据缺失的值标为待核对，不宣称一比一完成。
- Git Graph 保留已验证的功能与布局，矩阵记录现有范围和缺陷回归，不作为继续扩充的任务清单。许可限制下只提取设计事实并独立实现，不复制受限源码。

## 稳定性与测试追踪

2026-09-19：后续开发统一遵守[稳定性测试架构](docs/stability_testing.md)。需求、设计、实现、分类用例与运行证据必须关联；实施前分析全部关联模块，过程中发现的问题按[问题索引](docs/stability_issues.md)分类去重，不能只在日志里记流水账。测试分单元、功能、系统，分别记录实现验证与压力验证；按成本使用20/100/1000档，真实记录执行数、环境与失败，不用替身结果代替原生平台验收。新需求或验证事实改变开发方式时及时更新相应开发skill及文档。

## 构建与验证

复用现有 `enhancements/package.json` 命令。在 `enhancements/` 执行：

```sh
npm run build
npm run check
npm run check:ui
```

目标 UI 测试可使用 `npm run check:ui -- test_workspace_titlebar.cjs` 等现有筛选入口。修改后先跑相应目标测试，提交前按实际影响补齐检查；构建失败、环境未具备或未执行的检查必须如实记录，不用旧结果替代。

多人并行时明确文件所有权；共享构建产物的生成串行执行，避免并发覆盖。临时测试、截图和上游缓存放在忽略目录中。提交前运行 `git diff --check`，检查实际差量与暂存内容。不要因文档小改新建重复实现型测试。

## 功能修改后的本机安装

- 2026-09-14 用户明确要求：功能新增、修改或修复必须包含本机安装交付，方便用户直接验证。完成本次构建和相应检查后，按[安装与恢复指南](docs/installation.md)安装已验证的候选版本，不停在源码修改或隔离测试通过，也不重复询问是否安装。仅修改文档或协作规则时无需重新安装。
- 安装前核对当前已安装版本、本轮差量和候选资产；共享工作区存在其他尚未验证的修改时，基于已验证安装基线构造本轮候选，不能把无关改动一起装入用户环境。使用现有安装、备份与检查入口，不另建绕过校验的部署路径。
- 安装后执行对应检查并核对安装资产与本次候选一致。区分“源码已修改、构建已通过、资产已安装、运行窗口已加载、功能已验证”；安装成功不代表当前运行中的窗口已经加载新版本，需要重启时明确告知用户，不擅自关闭用户窗口或丢失未保存内容。
- 收尾必须说明是否安装、安装检查结果、用户如何加载本次版本及仍需实机验证的项目。安装失败或受阻时先处理可解决的问题，并明确报告阻塞原因；不得以“修复完成”掩盖未安装状态，也不得让用户在未加载修复的旧窗口中重复验收。

2026-09-21用户明确：每次功能候选的安装交付必须同时验证卸载，不得用安装检查替代卸载验证。相同候选在隔离安装位置及用户数据目录通过公开入口执行安装→检查→卸载→重新安装→检查，覆盖有效备份恢复及无兼容备份时入口撤销，核对正文、配置和备份保留；补跑卸载失败、取消与回滚用例。用户实际环境默认仅做卸载只读预检，不能为开发验收卸载正在使用的环境。分别记录隔离实际卸载、本机预检和最终本机安装状态。

## Git 协作

提交采用中文结果的 Conventional Commits，例如：

```text
fix(workspace): 统一侧栏与窗口按钮几何

- 共用侧栏和窗口控件的几何规则，保留各自状态与命令
- 说明本次实际验证结果及未覆盖边界
```

允许类型为 `feat`、`fix`、`refactor`、`perf`、`security`、`content`、`docs`、`test`、`build`、`ci`、`release`、`revert`、`chore`。范围可省略，标题必须包含中文。2026-09-19用户明确：每次提交的标题后空一行，正文必须逐行使用 `- 详情` 列表，简单改动也不能省略；说明具体变化、原因及有依据的验证/限制，不只重复标题或补造结果。此要求替代此前“只有标题不足时补正文”。按可独立验证的结果提交；提交前用 `python scripts/check_commit_messages.py --message-file <消息文件>` 检查，再以 `git commit -F <消息文件>` 提交并核对实际正文。历史处理与检查边界见[提交说明设计](docs/git_commit_messages.md)。

正文是面向读者的修改总结：说清触发场景、原问题及修改后的行为，按实际改动取必要信息。不要把修改文件清单、模块名、执行步骤或“同步文档/增加测试/构建安装”作为明细主体；这些可由diff或验证记录查看。验证只写与结果判断有关的实际证据和限制，不用它填充功能总结。文档、测试或规则本身是提交目的时，说明内容/覆盖/约束具体如何改变。格式检查只检查语法，提交前仍需逐条审阅含义。

2026-09-13 用户明确要求：每轮完成后，及时提交已验证、需要入库的代码、测试、设计与对应构建，不把前轮已完成的改动长期留在未提交状态。开始下一项工作前先核对上一项收尾，按可独立验证的结果提交。阶段实现已经可以审查、但仍有环境或平台验收缺口时，可以连同已知边界和后续验收项提交；不得把提交等同于需求全部完成或安装验收通过。

只暂存已审查的明确文件。未经用户授权不得 push，不把发布、强推或改写已交付历史视为普通编辑。不得借仓库迁移处理用户无关改动。

## 已验证的宿主约束

- 必须保留宿主 `#title-text` 等标题节点的连接状态，不能因外观隐藏而删除宿主直接访问的节点。
- 底栏含嵌套的 `.footer-item-right`；插入控件必须确认参照为直接子节点。阅读视口统一扣除底栏覆盖区，大纲、缩略图不得各自猜测可见区域。
- Markdown 自动大纲更新只调用共同的标题判定；同标题不重写选中项，保留边缘滞回，不能重新引入原生延迟回调的双重高亮。
- 原生新窗口入口仅使用已核对的 `app.openFile(null, {mountFolder, anchor})`。初始化 anchor 必须是安全的文内 `#` 片段，不能让宿主把移交标识交给外部协议。文档通过随机令牌的内存频道交接；接收确认和来源身份复查前不关闭原标签，不写临时正文或伪装已保存状态。
- 当前实现、各次验证及安装边界统一见 [反馈复查记录](docs/feedback_review.md)；旧提交的通过数量不能替代新构建验证。

## 快捷动作与上游行为核对

2026-09-13用户明确要求：普通便捷图标点击即执行自动化动作，禁止统一导入“预览操作／执行此操作”的额外确认。修改入口前必须核对固定上游版本的命令注册、有效配置、目标解析、启用条件和完成后刷新，而非只对齐图标。已经能从仓库识别的remote、upstream、分支及对象由服务读取，不让用户重复输入；确实存在多个目标才提供一次选择。多选筛选器的确定是提交选择草稿；文件丢弃、强推等破坏性动作的确认按用户最新具体要求及对应上游设计独立处理，不套用到fetch、refresh、普通pull／push。UI复用同一命令服务，状态、配置和失败反馈仅由所属服务管理。

验收应覆盖工具栏、底栏、快捷键和更多菜单中同一动作的全部真实调用方，使用专属临时仓库及本地bare远端验证自动目标、单次执行、错误、过期／取消和刷新，不能等用户逐图指出遗漏后才补。产品中的用户点击授权不代表AI可在开发验收时推送、拉取或丢弃用户真实仓库文件。

## 原地更新与版本公告

2026-09-19 用户要求可选ZIP原地更新，并明确选择“立即安装、手动重启生效”。维护发布前读取 [更新设计](docs/workspace_update.md)：功能／修复公开交付必须递增 `enhancements/release.json` 的 sequence，写清用户可见变化，重新构建并一同提交公告、源码和发行清单。内部文档变化无需新版本；未推送候选不能声称其他用户可获取。不能把逐个Git提交均视为可用发行版。

更新继续复用标准安装事务、备份和校验；多窗口提醒去重与安装互斥分开验证，不强制关闭窗口。用户草稿、工作台设置和已加载版本保持原有归属；明确区分磁盘安装成功与手动重启加载。Windows PowerShell 5.1辅助脚本含非ASCII时保留UTF-8 BOM，并用真实5.1执行，不能以PowerShell 7解析代替。验收与平台边界通过统一测试目录关联R047。
